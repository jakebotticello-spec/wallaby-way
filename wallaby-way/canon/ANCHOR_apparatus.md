# ANCHOR — apparatus-build track
*v38 · 2026-06-11 (S54 "Conduit" — FIRST WET READ: Griffin thread assembled + judged both arms, §6 PASS; comprehension architecture proven + canonized (two-step, breadth-then-depth); the apparatus grew a corpus callosum) · prior v37 · 2026-06-10 (… → S50 "Combine" (THE READ) → S51 "Continuance" (the re-read wave) → "Tally" parallel-track FULL FLOOR CENSUS → S52 "Catechism" (THE v3 FLOOR + THE PROBE + THE CONFLUENCE) — **THE FLOOR IS UNIFORMLY SCRUB-v3: 13 CLASSES, ALL 3 SNAPSHOTS, ALL 29,396 MESSAGES, 2026-06-10 (29,396 v3 rows appended; 1,346 redactions; verify 7/7; PK extended to 4 columns — row identity is msg_uuid+scrub_version, PK-ENFORCED) · ★ ROWS ≠ MESSAGES: 58,792 rows / 29,396 distinct — every message-meaning count is COUNT(DISTINCT msg_uuid), ALWAYS · BOTH CENSUS DISPOSITIONS CLOSED (strays relocated + ledgered; delta-1 superseded by floor-wide v3, and uuid-proven read: 23/7/1/0) · PILE SHRAPNEL: 118/118 CLEAN vs the 13 v3 classes · INJECTION SPOT-CHECK: 3/3 CLEAN, the S51 expectation confirmed · THE RETRIEVAL PROBE RAN: 8,288/8,288 indexed, graded 2-3/10 raw answer surface, FOUR failure classes named with receipts — and Q4 (the Griffin texture) reproduced the EXACT worst case Progenitor §12 predicted at S26: the texture path is load-bearing, evidence-grade, twice-confirmed · ★ THE CONFLUENCE v1 ENTERS CANON: the synthesis chain restated against Progenitor v4's RATIFIED design — the ‖ restored (fence-synthesis AND texture/collation are PARALLEL mirrored paths converging on ONE Judge; the serial "fence→texture→…→retrieval" phrasing of S43–S52 canon was shorthand drift, named and corrected) · THE JUDGE IS QC — the bar-keeper, never a reader/synthesizer/ranker; the retrieval reranker is lowercase and is NOT the Judge (Jake's ruling) · retrieval is RUNTIME (Progenitor §10's three cases), not a chain stage; the recall net serves both synthesis candidate-gathering and runtime ask · TOOLS: runner v1.6 (permanent integrity check, DISTINCT-aware, §14 constants printed, --list; 12/12) + chunk_whale v1.1 (6/6), landed**): floor mutation: NONE — the overlay APPENDED (the sanctioned path; accrete-forever); ONE schema canon event: the 4-col PK (preflight: 0 dependents; triggers untouched; Jake-run DDL). **A completion claim always carries the full board (405+30+3+2=440), never a bare "done."** Corpus-state bible: `canon/FLOOR_COUNTS.md` (★ S52 RECUT — rows-vs-messages discipline; cite it, never re-derive). Node identity = conv_uuid + anchor_msg from CONTENT; filenames NON-AUTHORITATIVE. NEXT: ★ PIPELINE v1.8 WITH SCRUB_VERSION=3 (13-class set) MUST SHIP BEFORE THE NEXT DELTA FREEZE or the uniform-v3 line breaks → B2 recall plumbing (strata tags + lexical channel + chunked embeddings, $0 — chain infrastructure per Confluence §8) → S3 TEXTURE PILOT: the Griffin thread, end to end, the chain's slice-7, Jake-gated at §14 constants → S2 fence-chain pilot → scale ‖ both streams → S4 → THE JUDGE holds the bar (Confluence §6, five criteria — register is criterion five and it FAILS entries) → THE QUESTION (Jake draws when ready; holstered until then) → next delta freeze on Jake's call, ALL guards + v1.8. The breadth IS the function; the Why/Doctrine/Whales/Confluence are FRAMEWORK — wet is the spec (§7).*
>
>
> **★ S52 "Catechism" — THE v3 FLOOR + THE PROBE + THE CONFLUENCE (the apparatus got clean, got measured, and got its answering spec) ★**
> *Receipts: `runs/floor_overlay_v3_S52/` · `runs/recon_S52/` · `runs/retrieval_probe_S52/` · `runs/tool_pass_S52/`. Spend: **$0** — no API fires. Three parallel CC windows + one fresh, collision-ruled throughout.*
>
> - **SCRUB-v3, WHOLE FLOOR:** Jake's "no more guesswork" ruling ended the v2 label collision (baseline "v2" = the S20 SYNTHETIC drill; delta-2 "v2" = S50 production-at-ingest — same label, two pattern sets, caught via the S20 receipt and confirmed empirically by the dry run: ~1,100+ unredacted production-class strings in baseline). v3 = the definitive 13-class set (10 × pipeline v1.7 + 3 Supabase — S37 flag-#4 CLOSED). Dry-run 1,346 redactions / 118 convs → Jake gated → **29,396 v3 rows appended, dry-run-vs-landed deltas ALL ZERO, verify V1–V7 7/7 PASS** (incl. 20-row 0-survivor sample + live PK duplicate-rejection proof).
> - **THE PK CANON EVENT:** the original 3-col PK forbade multi-version rows (the extractor anticipated the overlay; the table did not — OC's inference corrected on receipt). Preflight: 0 FKs / 0 views / 0 dependents; immutability triggers PK-independent. Jake ran the DDL as owner. New invariant: row identity = `(snapshot_id, conv_uuid, msg_uuid, scrub_version)`, PK-enforced — a double-fired overlay is now a constraint violation, not silent corruption. **Tally's zero-dup receipt RETIRED by design; ROWS ≠ MESSAGES is the new conflation fuse; COUNT(DISTINCT msg_uuid) is law (runner v1.6 bakes it in).**
> - **RECON CLOSED 4/4:** delta-1 uuid-dispositioned (23 harvested / 7 stubs / 1 empty / 0 hollow = 31 — read status uuid-PROVEN, legitimacy ruled by Jake: corpus, co-equal) · injection spot-check 3/3 CLEAN (S51 expectation confirmed) · the 6 `.stale_S50` strays relocated + ledgered (`_STALE_S50_MOVES_S52.md`; harvested 800 exact, quarantine 77) · **pile shrapnel 118/118 dispositioned, ZERO hits** (111 harvested + 7 whale-catalog via Jake-sanctioned one-time scripted scan — denominator-completeness enforced after a compaction-era "ALL CLEAR over 111/118" was rejected and the scope extension run).
> - **THE RETRIEVAL PROBE (move 1, the session's reason):** structural index 8,288/8,288 (anchored regex; cross-receipts FLOOR_COUNTS) → local-embedding rung graded by Jake against 5 ground-truth questions: **2-3/10 raw answer surface**, signal present in top-50 for 4/5. Failure classes, receipted: (1) boot-echo strata pollution (reference-layer-read nodes dominate ranking), (2) 45% truncation at the 256-token window, (3) no lexical channel (lore has proper nouns; dense embeddings dissolve names), (4) **Q4 — the Griffin texture ABSENT from top-50: the EXACT worst case Progenitor §12 wrote at S26, reproduced on instruments ten days later. The texture path is load-bearing, predicted-then-measured.** Embeddings demoted to recall net; precision belongs to comprehension.
> - **★ THE CONFLUENCE v1 → CANON** (`canon/The_Confluence_v1_2026-06-10.md`): S2–S5 restated against the RATIFIED Progenitor v4 — the ‖ restored (S2 fence-synthesis AND S3 texture/collation are parallel mirrored streams converging on ONE Judge; the serial chain phrasing of S43+ was drift, corrected in place), retrieval returned to runtime (§10's three cases), the Judge re-seated as QC holding a five-criterion bar set in advance (traceability · inverted admission · comprehension evidence — the Griffin test is the canonical form · verbatim spans · REGISTER, failable), and **§7 the heart clause: every paid reader boots the Why + Doctrine + inverted admission as FRAMEWORK — wet is the spec** (slice-7's 41-vs-1 is the standing proof; the austere reflex is the documented lineage failure mode, and S52 watched it surface in OC mid-session — clocked by Jake, owned, and nailed to the boot sequence so neither builder's unreliable memory carries the reminder again). Griffin = S3's pre-registered acceptance test, chosen by canon at S26 and independently by Jake at S52.
> - **VOCABULARY RULINGS (Jake):** the Judge is QC/realness — never content decisions; the retrieval reranker is lowercase, different layer. Distinct-value audit addendum declined (occurrence-count explanation accepted). Provenance answered $0 from the reference layer: the pipeline lived in S24–S27, Progenitor v1→v4 in one day, ratified S27, texture path born at the S26 rewrite.
> - **TOOLS LANDED (Jake pushed):** `apparatus_batch_read.py` v1.6 — integrity check permanent + DISTINCT-aware (T3d proved the v3 row-count hazard), §14 constants as named code constants printed at every gate, `--list`; 12/12. `chunk_whale.py` v1.1 — seam-manifest name derived from conv_uuid; 6/6.
>
>
> **★ FLOOR CENSUS — parallel track "Tally", 2026-06-10 (the count of record; the S51 interim retired) ★**
> *$0, read-only, full re-derivation — floor via SELECTs, disk via Python structural reads; nothing inherited from the interim, then reconciled against it. Receipts: `runs/floor_census_2026-06-10/` (census_A_floor_raw.md · census_B_manifest_raw.md · census_C_followup_raw.md + check_4788ce54.py). Canon pass authored by Tally at Jake's explicit instruction (his ruling supersedes the ignition's routing clause), verified + landed by Jake.*
>
> - **EVERY MAJOR FIGURE CONFIRMED LIVE:** 440 headers (= 440 distinct conv_uuids — one header per conv proven) / 29,396 messages (= 29,396 distinct msg_uuids — ZERO dupes) / 3 snapshots / exactly 2 floor tables / **29,836 total rows now a live-query receipt** (the interim's "arithmetic, not a receipt" caveat is dead). Per-snapshot: 294/22,801 · 31/1,337 · 115/5,258 — all six exact.
> - **THE BLAST RADIUS IS FLOOR-VERIFIED:** exactly **12** multi-snapshot convs, uuid-for-uuid the S51 set (the 11 ledgered partials + c00ef343), zero header-side multi-snapshot rows, and **no 13th** — every header-vs-actual delta in the floor IS the continuation set, fractions matching the ledgers digit-for-digit. 3 zero-message headers = the 3 hollow stubs exactly; 0 orphan messages.
> - **NODES 7,771 CONFIRMED THE HARD WAY:** per-file diff vs the S51 census — 399 files identical + f018b1f8's 17; the lone mismatch (`4788ce54`, 25 naive vs 24 anchored) resolved by the discriminating test: ONE inline literal `**Salience:**` inside node content, not a node header. **METHOD RULE EARNED: a node is the line-anchored `**Salience:** <TAG>` header (`^\*\*Salience:\*\*\s+\w+`, MULTILINE); naive substring counts over-count.** Sidecar pairing 400/400 clean.
> - **THE 517 LIVE-RECEIPTED** (first re-derivation since S33): 593 raw across the 21 S33-era catalog files − 76 in the two `confirm_*_whole` alternate renderings = **517 exact** (chunk files + single results are canonical; the wholes are excluded alternates — and 48b4110a whole=36 vs chunks=145 is the input-length-degrades-the-reader finding, visible in fossil form). Both node pools are census-grade as of today.
> - **TWO NEW FACTS, DISPOSITIONS OPEN (carried in NEXT):** (1) six `.stale_S50` files (3 conv pairs: 955cce09 · cda6d5d1 · fcbd24fa) sit in `nodes/harvested/` covered by NEITHER S51 ledger — provenance guess: an S50-era rename-in-place step that preceded the S51 quarantine move; data-safe, needs a ruling. (2) **delta-1 (31 headers / 1,337 msgs) is ENTIRELY scrub_v1** — baseline and delta-2 are v2; the v2 secret classes (GOCSPX/GitHub/AWS/JWT) have never run against that slice.
> - **File manifest landed in FLOOR_COUNTS** (now permanent): harvested 400 .md + 400 sidecars (+6 strays) · catalogs 39 (21 S33-era + 18 S51-era mirrors of harvested copies, not double-counted) · quarantine 70 (46 corpses + 22 partial-move files matching `_PARTIAL_MOVES_S51` row-for-row + 2 ledgers) · manifests 2 · runs/ 14 dirs + 3 files · graveyard present.
>
> **★ S50 "Combine" — THE READ + THE SNAPSHOT-BOUNDARY FIND (the apparatus finally read — then quarantined its own work) ★**
> *Source of truth: `history/handoffs/Chat_Session_Handoff_2026-06-10_apparatus_S50_to_S51.md` — this block is the tight cut; the handoff carries the full record.*
>
> - **SCRUB v2 PROVEN** (`apparatus_freeze_pipeline.py` v1.7, SCRUB_VERSION=2; +5 pattern classes): proof-fired on a real GOCSPX conv; **264 redactions at ingest** at the 6-9 freeze. Secret track CLOSED: 5 distinct GOCSPX creds found — 4 dead with RecruitMail, the Pyris one rotated by Jake 6-10.
> - **THE 6-9 DELTA FROZEN:** +115 convs / +5,258 msgs, zero deletions, zero trigger rejections, Stage-3 verify-clean. **Floor 325/24,138 → 440 headers / 29,396 messages (3 snapshots)** — append-only, exact match to the diff projection.
> - **RUNNER → v1.5:** canary `--max-tokens` flag; collect-path silent-drop fixed (`missing_parents` surfaced); DONE-footer check broadened (`"--- DONE:"` → `"DONE:"`).
> - **WHALE `a8bf895f` FULL-STRIPPED** (the strip-ALL variant — all 4 echo blobs tombstoned with md5+msg# pointers; Jake-gated for known-worthless bulk; precedent, NOT the default): 8.57MB → 410KB (95.7%) → **16 nodes**, end_turn, via the proven S33 testcall path.
> - **THE READ:** 238-conv combined worklist (S49 baseline + 6-9 delta) → 237 requests, `msgbatch_01PaHAJTQLjUSFDzSsMN8tQw`, 232 succeeded / 5 errored → **190 persisted / 1 truncated / 41 quarantined. $70.72 console day-total vs the $78.65 gate** — under, with a clean denominator (every one of the 238 dispositioned by name in the handoff §3 table).
> - **WAVE-1 REPAIRS (7):** 6 ANCHOR-catalog repairs under the snap-if-unambiguous-else-drop rule (**0 snaps / 8 drops** — 2-candidate UUID ambiguity is structural) + `6674652b` re-persisted (29 nodes) after the DONE fix.
> - **★ THE SESSION'S REAL MONSTER — THE SNAPSHOT-BOUNDARY BUG, found AFTER persist:** `floor_extract.py` rendered only ONE snapshot per conv — every multi-snapshot continuation conv read PARTIAL, silently, LOOKING CLEAN. All 9 then-known continuations were cut at the boundary; 8 partial-derived catalogs sat in harvested/ at session close. **S50 quarantined its own work rather than let a plausible-looking pile lie** — the wave-2 extract fix was authored and handed to CC; its proof became S51's first business. Nothing lost; the floor is whole; the lens was the defect.
>
> **★ S51 "Continuance" — THE RE-READ WAVE + THE TBD CLOSE (the cleanup landed; the pile is whole; the canon tuned) ★**
>
> - **WAVE-2 PROVEN:** the patched extractor (ALL snapshots, deduped on msg_uuid, highest scrub version, ordered created_at) proved on `176476ae` (211→223 msgs) and `c00ef343` (1→37 msgs).
> - **BLAST RADIUS: 12, not 9 — ZERO WHOLE.** The census found 3 beyond the known 9: `39589260` (8/109) and `5d0eba7c` (52/60) — both S47-ERA reads — and `2526703b` (48/80, a missed-supersede). 11 partial catalogs moved to quarantine (ledger: `nodes/quarantine/_PARTIAL_MOVES_S51.md`; supersede record: `_SUPERSEDED_S51.md`).
> - **THE RE-READ BATCH:** `msgbatch_0141kqFFbKmiuMs3VQpnBcEX` — 15 requests / 15 succeeded / **14 persisted / f018b1f8 quarantined. Integrity checks 11/11.** `176476ae` density-bounced pre-fire (1,096,220 tok @ ×0.72 > 1M) → chunk lane.
> - **THE WHALE LANE:** `82bbd8f1` STRIPPED (1 blob, 1,000,147 B, 70.57% dropped) → **27 nodes**; `ce1e79e1` STRIPPED (3 × 302,824 B blobs, 59.1%) → **19 nodes** — receipts exact vs pre-named cuts (`runs/whale_strip_S51/`). CHUNKS: `3ef82921` ×2 → **61 nodes** (S50's 64K role-break case RESOLVED — **input LENGTH breaks reader role, not output budget**); `176476ae` ×3 @ 1.4 B/tok → **62 nodes**. Registry: `canon/whale_registry.md`.
> - **★ f018b1f8 — THE INJECTION FINDING, RESOLVED (the corpus closed with it):** the conv's ROOT human msg is an embedded live session-ignition prompt (**2,982 B, msg `019e64dc-6fbb-7fab-b7d7-4484d614fe98`**) — **the reader obeys payload content** (2 junk fires before the mechanism was named). The persist guard contained it as STUB quarantine; the **defang path PROVEN**: 1 block cut (2,982 B, working copy only, floor untouched) → testcall → **end_turn, 17 nodes (9M/8F/0T), $1.55, persisted + harvested** (receipt `runs/defang_f018b1f8_S51/`). A NEW failure class: injection victim, not a size whale. **UUID NOTE — the prefix-twin transposition:** an earlier S51 record cited `019e64dc-6fbb-73a1-…` as the injecting msg; that is the ASSISTANT reply that OBEYED it. Prefix-twin UUIDs TRANSPOSE — any UUID gating a cut gets role + byte-size verified against the rendered block before the blade moves (the defang HALT caught this, working as built). Tombstone rule earned: name the CLASS of removed content, never paraphrase its imperatives (a descriptive tombstone left a followable ghost — mild residue, accepted + documented).
> - **EMPTIES CONFIRMED:** `d2cd71e3` (9 msgs) + `d85b4100` (18 msgs) — `content_blocks` null AT FLOOR LEVEL, verified with the patched extractor. Not the snapshot bug wearing a mask; nothing to read.
> - **HARVEST 4a:** 8 catalogs / **185 nodes** into the pile (`a8bf895f` sidecar generated from the floor, its 16 anchors validated).
> - **THE NODE CENSUS:** **7,771 nodes in harvested/** — 7,754 at census (4,399 MOTION / 3,189 FENCE / 166 TEXTURE, `runs/node_census_S51/census.md`) + f018b1f8's 17 (9M/8F/0T, receipt). S33-catalog nodes (517) counted separately by design. The FLOOR_COUNTS node-yield [TBD] is CLOSED.
> - **THE 29,175 UNIT-FIX:** the "~29,175 convs" carried in canon for the 6-8-26 export survived two reconciliation sessions; one-line arithmetic + measurement settled it at **424 convs / 28,836 msgs**. Landed mid-session — and routed through the WRONG HAND (CC editing canon), the second such occurrence; lesson logged, CANON-HANDS rule now in `wallaby-way/CLAUDE.md`.
> - **THE DOUBLE-FIRE INCIDENT:** two CC instances ran one task; persists overwrote at 05:22:31 — data valid, receipts broken, spend doubled (~$1.71). COLLISION rule landed (one task = one window = one output dir; <1hr-fresh output-dir check before any paid fire). Spend S51 ≈ **$11–12 actual** (incl. the duplication + the $1.55 defang read) [ADDENDUM-1 fix: was $9–11 pre-receipt]; project lifetime **~$177** (Jake, console).
> - **FORWARD RULE FOR SYNTHESIS:** node identity = `conv_uuid` + `anchor_msg` from CONTENT; **filenames are NON-AUTHORITATIVE** — the pile carries two naming generations (bare-UUID S50 batch · `result_*`/chunk-suffixed S51), both valid, documented, never parsed for identity.
> - **QUEUED:** full floor census next session (supersedes the FLOOR_COUNTS interim block) · injection spot-check of 2–3 ignition-led piled convs · `chunk_whale.py` naming patch (hardcodes `d9d05961_seam_manifest`) · runner v1.6 (permanent integrity check + rate constants) · **THE SYNTHESIS CHAIN + the retrieval test.**
>
> **★ S48→S49 — THE UNCRAPPENING (the canon caught lying; the house cleaned; the words made true) ★**
> S48 did not run a delta. It found the project's own canon was overstating its completion state ("THE CORPUS IS READ" when 202/325 is read), found a live dead-project secret in the git history, and converted both into structural fixes across three parallel windows + a reconciliation. What landed:
>
> - **THE TRUE CORPUS STATE — 202 / 325 READ, 123 PENDING.** Floor = 325 convs / 24,138 messages (294 baseline + 31 delta). Harvested pile = 202 convs / 3,743 nodes (`MERGE_MANIFEST_S47.md`). **123 unread**, broken down + verified against the floor by snapshot + date: **73 baseline CHUNK_LATER** (the big-conv backlog S39 deliberately parked off `batch_list_S44.csv` — the bulk of real remaining work), **22 genuine delta** (post-May-25, the actual new material), **28 mostly-hollow** baseline stubs (msg_counts near 0 — eyeball-then-skip, probably correct exclusions). 325 − 202 = 123. The math closes; nothing is missing; a third was knowingly parked and the banner called it "read."
>
> - **THE REPO REORGANIZED + CLEAN-REBUILT (S48, 3 parallel carpentry windows: Cabinetry → Chamfer).** The tree was made self-documenting — read a directory name, know its contents. New structure: `active/` = 6 canon files only; `wallaby-way/` = the project home (`canon/` live canon · `nodes/{harvested,catalogs,manifests,quarantine}` finished output · `scripts/` live tooling + `reserve/` · `runs/<date>/` dated output · `inspect-later/` the unjudged 2nd node store · `secrets/` · `source/` logical home for the big immutable data, physically unmoved); `history/` = the record (handoffs, superseded specs, old anchors — contents NEVER path-rewritten, verbatim); `graveyard/` = dead-but-labeled; `reviews/` = panel output. The git history was rebuilt from a clean tree (not filter-repo) to dissolve the leak (below); the old remote was deleted; the repo renamed `jakebotticello-spec/wallaby-way` (local folder stays `C:\claude-reference`). 7 live scripts repointed for the move (smoke-tested green, $0). **History files describe the layout as it was when written — do NOT "fix" their paths; the `history/` README is the spelunker's note.**
>
> - **THE DEAD-SECRET LEAK — CLOSED BY THE REBUILD.** Two pre-reorg tracked files (`_large_variance_*.txt`) held a `GOCSPX-` OAuth secret + Supabase JWTs for project `yxtqjnxqxrhlnmlbfzjn` — **RecruitMail, a DEAD project. Both moot, no rotation needed.** The clean rebuild dissolves the leak (those files don't come along). A recurrence scan also caught live floor-DB creds in 16 untracked `graveyard/d9-review-debug/` files — at-rest, NEVER pushed, now covered by the `graveyard/` wholesale gitignore. NOTE: the floor DB project (`slkmqnsyodkyfwqoicrd`, rotated S47) is a DIFFERENT, still-live project — untouched.
>
> - **THE AUDIT (S48, "Cinder") — the drift catalogued, ranked by blast radius.** A full reconciliation across ~48 sessions hunting ONE defect class: *a true local number/status narrated as a global done by dropping the denominator/config/surface that bounded it.* It appeared 8+ times (the "corpus is read" banner, the stale "206," the S46 "corpus clean" false-negative, the S39 "proven/paced-run" optimism, the whale breakdown, the S37 double-count trap, the floor-count conflation). The build spine is REAL and [VERIFIED] — the reads were never the problem; the RELAY is where truth leaked. The cure is format, not care: rules where the lie doesn't fit (now landed in JAKE-RULES §5.1).
>
> - **THE CANON RECONCILED (TWW CCC S3 "Concordance").** This banner recut to v33 (killed "THE CORPUS IS READ" at the source — banner + footer); whale breakdown corrected 69/54/7 → **62M/62F/6T** (the 69/54/7 was the reader's self-reported DONE-line tally; 62/62/6 is the parsed `**Salience:**`-block truth from the manifest); the dead "206" killed in place in the CHANGELOG; current-state path pointers updated to the new tree; the five floor numbers pinned in the new `canon/FLOOR_COUNTS.md`; JAKE-RULES §5.1 took the denominator + reconcile-don't-inherit + correct-in-place + proven-needs-config + clean-needs-surface rules; a `wallaby-way/CLAUDE.md` authored for CC. History prose left verbatim — only wrong *facts* and stale *current* pointers destroyed.
>
> **S49 FIRST MOVES (canon is now true — get back to reading):** (1) **STATE RECONCILE ($0)** — confirm the new structure on disk, the floor still answers 325/24,138, harness v1.3 + `build_worklist.py` v2.1 + freeze pipeline have correct post-reorg paths (or flag what's broken). (2) **RESOLVE THE TWO OPEN GATES before any paid read** — **C7 / Sonnet-vs-Opus equivalence** ($0 disk check: locate the S40 grade artifact, or the equivalence stays asserted-not-proven; the 202 are already harvested on Sonnet so this is a confidence gap, not a blocker) and **Stage C dead-or-deferred** (the 1M-Sonnet whole-read may obviate it — DECIDE, don't leave ambiguous). (3) **THE DELTA PASS** — freeze the unfrozen full export in `source/deltas/` (append-only, new snapshot, floor untouched) → delta worklist → read the 22 genuine delta + decide the 73 chunk-later backlog (gated, costed, only if the chunk pipeline is confirmed runnable — see FACT 5 below) → 28 hollow get eyeball-then-skip. (4) **THE SYNTHESIS CHAIN — once, on the complete pile:** fence-synthesis → texture/volume → cluster-validation → the Judge → retrieval engine. **GATE every paid call with the real number next to the button.**
>
> **★ FACT 5 — THE CHUNK PIPELINE built-vs-not is UNRESOLVED [SUSPECT].** S39 said Stage C (overlapping-window chunking) was NOT built; yet big convs (`48b4110a`, `ea900330`) WERE chunk-read and ARE in the pile. So *some* chunked reading happened — but whether a REUSABLE, RUNNABLE chunk pipeline exists for the 73 unread big convs is unconfirmed. **[S49 CENSUS UPDATE: this is RESOLVED as moot — see the S49 block directly below. All 123 unread convs fit WHOLE on input; there is nothing to chunk. `chunk_whale.py` stays proven-on-whales and parked.]**
>
>
> **★ S49 "Concord" — THE CENSUS (the duct-tape baseline replaced with a disk-confirmed one) ★**
> S49 ran a full disk-and-floor census to kill a baseline Jake called "held together with hopes, dreams, and duct tape." Every corpus count is now confirmed against the live floor and the actual files on disk — not inherited from prose or a banner. What it settled:
>
> - **THE COUNTS ARE CONFIRMED, NOT INHERITED.** Full disk + live-floor census (`runs/groundtruth_S49/`). Floor = **325 headers / 24,138 messages** [→ `canon/FLOOR_COUNTS.md`]. Read = **202 convs** (194 harvested + 8 whale, disjoint pools, 0 overlap) / **3,743 nodes** (3,226 harvested + 517 whale) — the harvested figure is the disk `**Salience:**`-marker count, EXACT to `MERGE_MANIFEST_S47.md`. Unread = **123 convs** (120 valid for batch + 3 hollow stubs excluded by the skeleton gate). Worklist verified clean: 0 over-claim, 0 miss vs the floor-minus-read set. **202 + 123 = 325.** The math closes against the floor.
>
> - **THE COST-UNIT ERROR — KILLED.** The worklist's `proxy_est_tokens` is **NOT tokens** — it is `json.dumps(content_blocks)` char-sum ÷ 3.5, which approximates *rendered payload chars*, not tokens. Real tokens ≈ rendered_chars × 0.32. Three S49 estimates were wrong before a direct render settled it: **$93.40** (proxy treated as tokens) → **$29.36** (wrong factor) → **$48.50** (measured across all 123 real renders — authoritative). The batch cost for the 120 valid convs is **~$48.50** at Sonnet batch rates (sum rendered_chars 72,169,893 × 0.32 → 23.1M tok; $1.50/Mtok in + 8% out @ $7.50/Mtok). The lesson — a field NAMED for a unit is not proof it holds that unit — is now JAKE-RULES §5.1.
>
> - **ALL 123 FIT WHOLE ON INPUT — NO CHUNKING, EVER, FOR THIS SET.** Max conv = `3ef82921` @ ~619K real tokens; the >1M band is EMPTY across the full set (not a sample — all 123). The old "CHUNK_LATER" worklist verdict was the **on-sub catalog OUTPUT ceiling (~101K tok)**, NOT the 1M API INPUT limit — two different walls conflated. This RESOLVES the Stage-C / FACT-5 "is the chunk pipeline runnable" gate: **it does not need to be — there is nothing to chunk.** `chunk_whale.py` remains proven-on-whales and parked; it is not needed for the 123.
>
> - **THE NON-SIZE BATCH RISK — the 32K OUTPUT cap (Batch_Read_Spec §3).** A conv truncates not on input size but on **node DENSITY**: tool-heavy convs generate fat catalogs and can exceed the 32K output cap (≈84 nodes). **~20 convs are at risk** (TU≥200 & msg≥100), led by **`d3ff9ed1`** (TU=920, CB=1763 — the corpus's single highest-risk conv). **CRITICAL STRUCTURAL FINDING: the size-leaders and the density-leaders are NON-OVERLAPPING populations** — the biggest convs by input are low-density and safe; the truncation risks are mid-sized and tool-heavy. A size-based sample misses the truncation set entirely. Recoverable: the batch runner MUST check `stop_reason == "max_tokens"` per result and HALT+re-fire the offender at a higher cap; never persist a truncated catalog (it would be a §5.1 "read = complete when it wasn't" failure baked into data). Now a HARD requirement in `Batch_Read_Spec_v1.1` §3.
>
> - **DISPOSITIONS (ratified, → `graveyard/`; the MOVE is a separate CC step, this is the record of it):**
>   - **inspect-later store (24 nodes; `nodes-S34/` + `variance/` + `variance_tuned/`):** all 24 are superseded S34 reads of dead-project convs (LRN / RecruitMail / LRNHQ context-loads); every one has a newer canonical counterpart already in `nodes/harvested/`; 0 new read coverage; none carry `.parents.json` edges (the harvested versions do). They are old drafts of pages already held clean. The `variance/` + `variance_tuned/` dirs (20 runs of `4d88185f`) are variance-experiment artifacts. → all labeled-not-deleted to `graveyard/`.
>   - **the 6-8-26 export (`deltas/6-8-26/`):** a re-pulled FULL-account export (**424 convs / 28,836 msgs measured** [S51 fix: was "~29,175 convs" — a unit error that survived two reconciliation sessions; measured S51] = Jake's entire history at pull time, NOT a corpus delta). It is STALE (missed traffic since) and DO-NOT-USE. → labeled to `graveyard/`. Re-pull a FRESH export at delta time.
>
> - **THE DELTA-INGEST MODEL (recorded so it's not re-derived).** A fresh export = Jake's entire message history. The model: pull-fresh → diff against the current CORPUS (the floor) → the diff IS the delta → read the diff through the apparatus → process into an APPEND on top of the corpus. It runs COMBINED with any baseline-unread batch when a delta pass happens (one batch, current in one shot). The 6-8-26 export was an early attempt, now stale; re-pull at delta time.
>
> - **NODE YIELD OF THE 123 READ:** `[TBD — resolves on first unread-batch read]`. The census settled every count except this; node count is post-read by nature.
>

> v31 left the reader cut and graded but UNFIRED at scale. S43–S47 built the batch harness, fired it across the over-ceiling corpus, triaged every guard trip, and merged the result. The "206" the v30/v31 banners carried is DEAD — the real numbers are below.
>
> - **THE HARNESS BUILT + THE BATCH FIRED (S43→S45).** `pipeline/apparatus_batch_read.py` (now **v1.3**) built per `Batch_Read_Spec_v1.1`: per-conv `floor_extract` ($0, key unloaded) → skeleton-gate → assemble batch request (v4.1 deployable cached system prompt, `custom_id=conv_uuid`, temp 0, max_tokens 32000, NO tools) → Message Batches API submit WITH the batch-not-synchronous guard → poll/collect → `scrub_text()` → `persist_node_file()` (atomic, verify-on-write) to the flat pile. **RESUMABILITY KEY = COMPLETE ARTIFACT** (DONE line + tally), not file-exists. The frozen paid list was 189 convs (`pipeline/s39/batch_list_S44.csv`, line 47). **181 fired / 177 persisted clean / 4 quarantined** on the first full run. v1.2 (S45) made the persist loop resilient — a guard trip quarantines the ONE conv (`pipeline/s39/quarantine/<uuid>.quarantined.md`) instead of HALTing the batch.
>
> - **★ THE QUARANTINE TRIAGE — ALL 4 RESOLVED TO ZERO-OPEN (S46 "Crucible" → S47 "Cauterize").** Four convs tripped guards. Their disposition:
>   - **3 of 4 — TRANSIENT, RE-FIRED CLEAN (S46).** `68c3fe4c`, `e49e2627`, `20e2e718` tripped ANCHOR-NOT-IN-PARENTS-MAP. **★ ROOT CAUSE (a $0 floor read overturned an inference):** all of `68c3fe4c`'s emitted anchors were REAL messages, correct conv, present in the baseline floor — the reader was RIGHT; `floor_extract` handed persist a short/partial parents_map during the S45 collect sweep. **TRANSIENT, not systematic** (all 3 re-fired clean on the SAME floor/extractor — a map-builder bug would not self-heal). GOOD for the delta batch: it does not inherit a known bug. Re-fired via Route A (temp line-47 swap to a 3-row list, fire, revert — line 47 net-zero), batch `msgbatch_011q7ecPGt5cYNEHNNyfCpnd`, PERSISTED 3 / QUARANTINED 0, ~$1.70. Minor harness gap logged (collect's missing-check guards a conv with NO parents entry but not one with an empty/partial dict — hygiene note, not a fire).
>   - **★ THE TURD — `567956f0` — STRUCTURAL POISON PILL, HAND-AUTHORED (S47).** This was framed a "coin-flip" by S46; the S47 canary FALSIFIED that. Canary `msgbatch_01Uxo82Vc5ou2fPhQ9AwwA9K` re-quarantined STUB (guard class CHANGED from S44's anchor-not-in-map → the reader confabulated MORE Wix implementation code, 0 nodes, 0 DONE line — different junk, junk both times). **A $0 floor read of the 12-message payload found the mechanism: MSG 2 is ~94% of the payload — four embedded `[TOOL_RESULT: conversation_search]` blocks dumping excerpts from OTHER convs — which swamps the thin outer conversation and makes the reader continue it rather than catalog it.** Same class as the whale problem (payload shape defeats the reader), caused by tool-result bloat not raw length. The conv has REAL content (an LRN/RecruitMail handoff ending on a confirmed Wix-backend-identity breakthrough), so it was **HAND-AUTHORED** by OC (2 nodes, 1 FENCE / 1 MOTION; literal email/client-id described not enshrined), landed + verified by Read, quarantine corpse retired to `_RESOLVED_567956f0.quarantined.md`. **A NEW NAMED FAILURE MODE: reader-continuation-on-embedded-tool-result-bloat. A third fire will not help — manual handling is correct.**
>
> - **★★ TWO LIVE SECRETS FOUND + CLOSED (S46 caught one, S47 found the second).** The harvested_nodes plaintext corpus CAN carry literal secret values, not just narrative descriptions.
>   - **Google OAuth client secret** (`GOCSPX-…`, RecruitMail/LRN — DEAD project) was frozen verbatim into `harvested_nodes/abe64eb8-…md` line 277. Google alerted on the public exposure (commit `2d1af78`). S46 contained the repo: history purged (`git reset --soft 6553b32` + force-push, clean HEAD `58d5766`), `harvested_nodes/` permanently gitignored, NEVER to be pushed again. S47 SCRUBBED line 277 (both client-id + secret → `<…REDACTED>` tokens, verified by Read). Project dead/offline → value moot; scrub is corpus-hygiene so the literal doesn't ride into downstream nodes.
>   - **★ THE S46 CORPUS SCAN FALSE-NEGATIVED — and the REAL cause was NOT what the S46 handoff thought.** S46's `GOCSPX-` grep returned "CORPUS CLEAN" while the secret sat in plain view at line 277. The handoff blamed an "older-format secret with no GOCSPX- prefix"; S47's re-scan proved that WRONG — it IS a `GOCSPX-` secret, and `Select-String` returned zero on a re-run too: a **PowerShell encoding false-negative (BOM / non-ASCII surroundings make `Select-String` silently skip the match).** **STANDING LESSON: `Select-String` cannot be trusted as a definitive "clean" on this corpus — a clean verdict from it is UNPROVEN; verify scrubs by Read, not by re-grep.**
>   - **Supabase floor DB password** (`SUPABASE_DB_URL` connection string, project `slkmqnsyodkyfwqoicrd`) found in plaintext at `pipeline/secrets/floor_db.env`. This file is gitignored-by-design and NEVER leaked — it is the harness's floor-read credential and MUST live there. It was a local-disk plaintext-hygiene item, not a breach. Jake ROTATED the password anyway (updated `floor_db.env` + 1Password). **NOTE for future OC: do not flag a gitignored must-exist credential file as a leak — it is working as designed.**
>
> - **★ THE MERGE — 202 CONVS / 3,743 NODES, GROUND-TRUTH COUNTED (S47, this session's terminal move).** Authored `harvested_nodes/MERGE_MANIFEST_S47.md` — the flat pointer pile: one row per cataloged conversation pointing at its node file(s) with the ACTUAL on-disk `**Salience:**`-block count, NOT the reader DONE-header. **It INDEXES; it moves/copies nothing** (S33 killed the stitch step — nodes self-identify by conv_uuid + anchor_msg). Composition: 193 sidecar'd UUID nodes (177 batch + 1 canary `4d88185f` + 12 banked + 3 S46-re-fired) + 1 hand-authored (`567956f0`) + 7 result_/whale catalogs + 2 confirm-only convs (`48b4110a`, `ea900330`, pointing at the CHUNK catalogs, NOT the `_whole` — chunks are canonical, whole are confirmation artifacts) + the 130 whale nodes (route-from-registry, cross-checks exact). **★ A COUNT-PASS INVERTED A HYPOTHESIS (disk over OC's read):** 40 files had stated-total ≠ M+F+T-sum; OC theorized "header undercount, M+F+T is truth"; ground-truth `**Salience:**`-block counting proved the OPPOSITE — the STATED header is truth in 41/44, M+F+T was the TEXTURE-overcount (TEXTURE nodes labeled after the running total). Two genuine anomalies corrected in the manifest: `9bf5733a` = 32 (header said 31), `confirm_48b4110a_chunk_03` = 19 (neither stated number was right). Final manifest TALLY: **202 conversations / 3,743 nodes**, row-sum re-scanned = tally, whale cross-check = 130 OK, 0 unknown UUIDs. **THE PILE IS WHOLE.**
>
> **INVARIANTS UNCHANGED (stated explicitly per convention):** Floor immortal (325 headers / 24,138 messages, NEVER touched). Whales CLOSED (130 nodes, route from registry, never re-read). D9 / FK-resolved / append-only all STAND. v4.1 reader unchanged (the batch fired it). No floor mutation, no downstream geometry change across S43–S47. v27–v31 all still in force; v32 is a HARVEST+MERGE banner over them.
>
> **S47→S48 FIRST MOVES (the delta, then the synthesis chain — ONCE, on the complete pile):** (0) **STATE RECONCILE** — fresh cache-busted pull; confirm HEAD, repo visibility, harness v1.3, line 47 = `batch_list_S44.csv`, `harvested_nodes/` gitignored, and the S47 artifacts present (`MERGE_MANIFEST_S47.md`, the hand-authored `567956f0` node, the scrubbed `abe64eb8`). (1) **★ AUDIT THE DELTA PIPELINE before running it ($0)** — the delta path is freeze → worklist-diff → read (harness, BUILT + PROVEN, same reader) → append. The READ stage is the proven v1.3 harness; the FREEZE (`apparatus_freeze_pipeline.py` v4 — delta/scrub-vN spec'd) and WORKLIST-DIFF stages are spec'd-but-status-UNCONFIRMED. Spec'd ≠ built is a documented trap — audit which stages are real BEFORE running, and build any gap as a REUSABLE delta pipeline (every future export is a delta; build it clean once). (2) **THE DELTA PASS** — delta-freeze ~2 weeks of post-May-25 sessions (append-only, new snapshot) → delta worklist → delta batch (same harness) → merge ADDITIVELY into the pile. **The delta APPENDS BEFORE the synthesis chain** — so the corpus-wide comprehending passes run ONCE on the complete pile, no second round on the 2 weeks. (3) **THE SYNTHESIS CHAIN — once, on the whole pile:** fence-synthesis (Reconciliation 1) → texture/volume pass → cluster-validation (Reconciliation 2) → the Judge → retrieval engine (Progenitor §10–§11). Author Progenitor §12/§13 carry-forwards on the CONFIRMED pipeline. Fix the dangling `Boot_ScopeReader.md` ref → v4.1. (4) **END-OF-PROJECT HYGIENE (deferred, not now):** full ANCHOR v32→v33 ceremony with the complete CHANGELOG archaeology, dead-branch (`s33-whale-path`) deletion, `pipeline/` scratch cleanup (incl. `batch_list_S46_refire3.csv`). The routine banner recut (this v32) is NOT that ceremony.
>
> **DO-NOT-RELITIGATE (S43–S47 — all settled stands; rule-4 SUSPENDED, surface if off):** the batch RAN (181 fired / 177 clean) and the pile is MERGED (202 convs / 3,743 nodes, ground-truth counted — the manifest is the downstream entry point); the "206" was a PRE-RUN target estimate and is DEAD (use 202/3,743 from `MERGE_MANIFEST_S47.md`); all 4 quarantined convs are RESOLVED (3 re-fired transient, the turd hand-authored — a structural poison pill, NOT a coin-flip, do NOT re-fire it); the S45 short-map was TRANSIENT not systematic (proven by $0 floor read; delta does not inherit it); both leaked secrets are CLOSED (OAuth scrubbed+moot, floor DB rotated — and `floor_db.env` is a gitignored must-exist file, NOT a leak); `Select-String` is UNTRUSTED on this corpus (encoding false-negative — verify by Read); the manifest points at confirm-CHUNKS not confirm-wholes; the count-pass settled stated-header-is-truth / M+F+T-was-the-overcount (don't re-derive). + ALL prior settled stands (v27–v31 + carried). A NEW reason required to reopen. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*
>
> *[the v31 banner immediately below remains current beneath this banner — v32 records the S43–S47 batch-run + quarantine-close + secrets-close + merge; v31's reader-cut, v30's corpus-read resolution, v29's Stage A, v28's cold-store close, and v27's chunking ruling are all fully in force.]*


# ANCHOR — apparatus-build track
*v31 · 2026-06-07 (apparatus S42 "Cordon" — **THE READER IS CUT FOR REAL, GRADED HONEST, AND GOOD · v4.1 LANDED (was DRAFT at v30) · A CODE-REVIEW GATE CAUGHT A PAID-CALL-BURNING DEFECT BEFORE IT COST A DIME · THE FALSE-GREEN GRADE-PROBE EXPOSED + REPLACED · OUTPUT-SCRUB BUILT · SPEC → v1.1 · NEXT IS THE HARNESS, THE CANARY, THE RUN**): NO invariant moved, NO floor touched, NO downstream geometry changed — v27's chunking-holds-corpus-wide, v28's cold-store close, v29's Stage A, and v30's corpus-read resolution all stand FULLY IN FORCE. This is an EXECUTION banner over v30, not a replacement: v30 RESOLVED the architecture and DRAFTED the reader; v31 CUT the reader for real, proved it good by a hand-read grade of the actual deployable, and cleared the last correctness debt before paid spend. The breadth IS the function (the auxiliary brain); the austere reflex AND its over-engineering twin are the documented lineage bugs — read the Why/Doctrine/Whales as FRAMEWORK before working.*
>
> **★ S42 "Cordon" — THE READER CUT, GATED, GRADED (a gate-and-correct session; the batch did NOT fire — that is S43) ★**
> S42 took the S41 v4.1 *drafts* through a real review gate, caught a genuine paid-call-burning defect, corrected it, ran the FIRST honest grade of the actual reader, patched one minor slip, and built the output scrub. The reader is now RESOLVED, GRADED, and GOOD. What remains between here and a harvested corpus is the batch harness, the canary, and the run.
>
> - **v4.1 LANDED (was DRAFT at v30).** Both twins committed on main: the deployable `pipeline/test_call_system_prompt_S40.md` and the reference `apparatus/Boot_ScopeReader_v4.1_2026-06-06.md` (reader v4.1 → **v4.1.1** post-placement-correction). The batch fires v4.1, NOT v4.0. v4.0 + its deployable `test_call_system_prompt_S32.md` stay TOMBSTONED-NOT-DELETED.
> - **★ THE CODE-REVIEW GATE CAUGHT A REAL DEFECT (the session's highest-value moment).** `/code-review` on the v4.1 deployable found the PRECISION INVARIANTS block had been inserted INSIDE the one-shot example section (after the last example node, fenced by `---`). Two failure modes: (a) a model reproducing "exactly this structure" could ECHO the invariants block into its output; (b) the block's `---` fences are byte-identical to the `--- DONE:` sentinel → an echoed block trips `persist_node_file()`'s STUB-DETECTED / ANCHOR-NOT-IN-PARENTS-MAP guards → burns the paid call, persists nothing, halts. At 206 calls an intermittent echo = paying for reads that yield zero artifacts (a silent-overspend class). **THE RECUT** moved the two precision invariants OUT of the example and INTO the instruction body (folded into PER-NODE METADATA, where the UUID + TEXTURE-count rules already live — binding top-level directives, no `---` fences, no echo surface). Zero bare `---` lines remain in the deployable. The invariant TEXT is unchanged in substance; only PLACEMENT moved. Killed review findings C1/C2/C4/C5/C6/C8; C7 (prompt-injection from conv content) accepted-as-risk (tally + blindness guards cover it); C3 = the probe fix below.
> - **★ THE FALSE-GREEN — all three prior grade probes fired the WRONG file.** `_grade_probe_s40.py` fired `test_call_system_prompt_S32.md` (v4.0, NO invariants); `_grade_probe_precision.py` fired S32 + appended the invariants as a RUNTIME STRING hardcoded in the `.py`. **Neither ever fired the real deployable file.** The "Sonnet graded richer / 4/4 on e831e30b" evidence the lineage cited (recorded in the v30 banner) was Sonnet reading a stitched synthetic, NOT the artifact that ships — a false-green confirmed in committed code. Built `pipeline/s39/grade_recut_S42.py` — an HONEST probe that loads the REAL deployable from disk, no stitching, fires Sonnet 4.6.
> - **THE FIRST HONEST GRADE (the gate — CLEARED).** Fired the real recut deployable on `claude-sonnet-4-6` against `e831e30b`: clean `end_turn`, NOT truncated, **$0.37**, 6 nodes / 0 drops. **QUALITY (read by hand, not just scored):** coherent, dense, real FENCEs with checkable predicates, tail-densest (context-rot check passes) — NOT thin/flat. Good catalog. **COMPLIANCE:** 5/5 *substantive* cross-session pointers preserved verbatim. **★ CANON CORRECTION:** the "45-UUID conv" claim (v30 banner) was INFLATED — 40 of the 45 UUIDs were `conversation_search`/`recent_chats` index dumps the reader CORRECTLY skipped per the TRUNCATED-BLOCKS rule; only 5 were real referenced conversations. So **e831e30b was a WEAKER invariant test than canon believed** — it tested UUID-density-as-noise, not UUID-density-as-pointers. 0 TEXTURE nodes present (the count invariant was untested on this conv).
> - **THE UUID-SLIP PATCH (both twins).** The grade caught NODE 4 abbreviating `9f75fa12` to its 8-char prefix on a SECONDARY mention (the conv was fully preserved in NODE 2, so resolvable — but it is the exact failure mode the invariant targets). Patched the UUID-preservation invariant in BOTH `test_call_system_prompt_S40.md` and `Boot_ScopeReader_v4.1_2026-06-06.md`: write the FULL UUID on EVERY mention, including repeated/secondary mentions, never abbreviate to an 8-char prefix. Additive tightening only (the grade already met it 5/5 on primary mentions); OC's read: NO re-grade needed — it adds a constraint the reader nearly met, doesn't restructure. S43 may confirm-fire if it wants certainty; low-risk. *[S43 NOTE: this patch was authored S42 but did NOT land on HEAD with the S42-close PR — S43 move #0 caught it absent in both twins; re-authored + landed S43, see the v32 footer when written.]*
> - **`scrub_output.py` BUILT + TESTED (the §8 OUTPUT scrub).** The existing scrub (`apparatus_freeze_pipeline.py` PATTERNS, locked S12) is INPUT-hygiene only — no Supabase patterns, no text-in/text-out callable; the output surface (node catalogs + CC report text) was UNSCRUBBED. Built `pipeline/scrub_output.py`: imports the locked S12 PATTERNS (single-source), appends 3 Supabase patterns (`sb_secret_`, `sb_publishable_`, project-ref URL — all `{20,}` floor to avoid prose false-positives), exposes `scrub_text(s)->str` + `scrub_file(path)->str` (returns text, does NOT write — caller owns persist). Tested 15/15; the UUID-survival case passes BY CONSTRUCTION (a msg_uuid's dashes break every `{20,}` run). NOT yet wired into a harness — wiring is S43 move #2 (this closes the §7d flag-#4 scrubber-gap on the OUTPUT surface; the floor-overlay half remains a separate build-list item).
> - **`Batch_Read_Spec` → v1.1 LANDED.** The PREPAY GATE in spec v1 was a Jake-statement S40 over-hardened into an invariant; Jake overruled it at source (S41). v1.1 downgrades the prepay gate (auto-reload carries), reinforces the batch-not-synchronous guard as the real load-bearing one, reconciles the env-path, and fixes the count note. `apparatus/Batch_Read_Spec_v1_2026-06-06.md` kept as tombstone; **v1.1 is current.**
> - **ENV-PATH corrected in canon (S42, disk-confirmed):** the paid key lives at ROOT `anthropic_billing.env`; the floor DB creds at `pipeline/secrets/floor_db.env` (`SUPABASE_DB_URL` only); there is NO `.env` at `pipeline/secrets/` (that path is the conceptual REFUSED wall, not a populated file). The WHERE-THE-CODE-LIVES + `.env`-billing-trap lines carry this correction. *(S42 NOTE: the edit was CC-authored — Jake flagged it crossed the CC-must-not-author-canon line; correct change, wrong hand; corrected going forward — see STANDING RULES below.)*
>
> **INVARIANTS UNCHANGED (stated explicitly per convention):** Floor immortal (325 headers / 24,138 messages, NEVER touched). Whales CLOSED (130 nodes, route from registry, never re-read). D9 / FK-resolved / append-only all STAND. No floor mutation, no downstream geometry change this session. v27 / v28 / v29 / v30 all still in force; v31 is an EXECUTION banner over them.
>
> **S42→S43 FIRST MOVES (the build — what's between here and a harvested corpus):** (0) **STATE RECONCILE** — fresh pull, confirm HEAD carries all S42 work; if the every-mention UUID patch or anything else is missing it's local-on-Workhorse, land it first. (1) **★ RE-CUT THIS ANCHOR → v31** (this banner — done early, fresh context; fix the footer-stack v21-lead drift, record S42, OC-authored / Jake-lands). (2) **★ BUILD THE BATCH HARNESS** (PLAN MODE first) `pipeline/apparatus_batch_read.py` per `Batch_Read_Spec_v1.1`: per-conv `floor_extract` ($0, key unloaded) → skeleton-gate → assemble batch requests (v4.1 deployable cached system prompt + per-conv payload + `custom_id=conv_uuid` + NO tools) → **Message Batches API submit WITH the batch-not-synchronous guard** (assert endpoint, confirm batch-id, HALT if unconfirmable — NEVER yet tested; the canary tests it) → poll/collect → **`scrub_text()` on each catalog BEFORE persist** (wire `scrub_output.py`) + scrub CC's report output → `persist_node_file()` to the flat pile. **RESUMABILITY KEY = COMPLETE ARTIFACT** (DONE line + tally match), NOT file-exists (`4d88185f` proved a half-written file can exist). The S32 `apparatus_api_testcall.py` is the SYNCHRONOUS Opus ground-truth caller — NOT the batch tool, do not reuse. Model `claude-sonnet-4-6` from ONE source of truth. (3) **★ THE CANARY** — fire the harness as a BATCH OF ONE on a tool_result-HEAVY conv (ideally `4d88185f`, the known reader-bail case) so one paid call proves the plumbing AND probes the named failure mode AND yields a meaty catalog. The batch path's FIRST real firing (the S42 grade was SYNCHRONOUS — proved the READER, not the PIPE). ACCEPT: batch-id confirmed, collect works, custom_id matches, scrub runs, persist lands COMPLETE, AND a human QUALITY read of the catalog. Log the canary'd conv BANKED → batch = 205. (4) **★ BUILD + FIRE THE BATCH** (PLAN MODE) — only after the canary clears + its catalog is read. Same harness, full list. GATE all paid spend. Resumable. (5) **CLEANUP-AT-MERGE** — supersede the bad 3-node `harvested_nodes/4d88185f` copy when re-read (if the canary IS `4d88185f`, folds into move #3). (6) **POST-BATCH WATCH** — eyeball `4d88185f`'s v4.1 result for the tool_result bail mode. (7) **COLLECT + MERGE** — paid + 14 banked free + 130 whale → flat pointer pile. (8) **DOWNSTREAM UNCHANGED** — fence-synthesis (Recon 1) → texture/volume → cluster-validation (Recon 2) → the Judge → retrieval engine (Progenitor §10–§11). NO QUARANTINE. ~$25 downstream-rerun budget is a JAKE-ESTIMATE, NOT LAW.
>
> **DO-NOT-RELITIGATE (S42 — all settled stands; rule-4 SUSPENDED, surface if off):** the reader is GRADED GOOD on the REAL deployable (read by hand, not just scored) — don't re-grade without a new reason; a grade/probe that fires the wrong file is a FALSE GREEN (the worst kind — confirm the artifact under test IS the artifact that ships); compliance-green ≠ quality-good (read the catalog against intent every time); the prepay gate is DOWNGRADED (auto-reload carries — do not re-enshrine a Jake-said-once number as law); the batch-not-synchronous guard is load-bearing + NEVER yet tested (the canary tests it); the reader twins are HAND-SYNCED (patch both together; single-source is a named end-of-project item); batch = 206 (−1 per canary); env paths settled (root `anthropic_billing.env` paid / `pipeline/secrets/floor_db.env` floor / no `.env` at secrets/). + ALL prior settled stands (v30/v29/v28/v27 + carried). A NEW reason required to reopen. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*
>
> *[the v30 banner immediately below remains current beneath this banner — v31 records the S42 reader-cut + first-honest-grade + scrub + spec v1.1; v30's corpus-read resolution, v29's Stage A, v28's cold-store close, and v27's chunking ruling are all fully in force.]*


# ANCHOR — apparatus-build track
*v30 · 2026-06-06 (apparatus S39 "Courier" → S40 "Catalyst" → S41 "Comptroller" — **THE CORPUS-READ ARCHITECTURE IS RESOLVED — ALL EXECUTION, NO OPEN ARCHITECTURE · CORPUS SPLITS BY SIZE: 38-FREE-ON-SUB / 181-PAID-BATCH · MODEL LOCKED TO SONNET 4.6 + PRECISION ADDENDUM (READER v4.1) · S41 CLOSED EVERY FREE-SIDE QUESTION + FOLDED 24 ORPHANS INTO THE BATCH + DRAFTED v4.1**): NO invariant moved, NO floor touched, NO downstream geometry changed — v27's chunking-holds-corpus-wide, v28's cold-store close, and v29's Stage A all stand FULLY IN FORCE. This is the EXECUTION banner: substrate + read-law + both read mechanisms are built and proven; what is left is the BUILD. The breadth IS the function (the auxiliary brain); the austere reflex AND its over-engineering twin are the documented lineage bugs — read the Why/Doctrine/Whales as FRAMEWORK before working.*
>
> **★ S39 "Courier" — THE ON-SUB DELIVERY CEILING FOUND (STRUCTURAL) + THE FREE-ARM LOOP PROVEN ★**
> Stage B ran. The on-sub workflow mechanism has a **HARD ~13K-char delivery ceiling per `agent()` call** — STRUCTURAL, not tunable: the workflow runtime has no `fs`/`Buffer`/`require`, and `agent()` returns output-capped model text (proven on a 574KB target at 2.3% fidelity). Schema-passthrough and chunked-schema were both KILLED on data (don't re-attempt — DO-NOT-RELITIGATE). So the corpus SPLITS BY SIZE at the ceiling: **38 sub-ceiling convs run FREE on-sub** (the S39 loop proven on 14 banked convs, key UNLOADED, $0); **181 over-ceiling convs run PAID via the Batch API**. The S39 on-sub loop (`pipeline/s39/onsub_loop.js` + `floor_extract.py` + `persist_guard.py`) is proven and intact on main.
>
> **★ S40 "Catalyst" — MODEL LOCKED + THE TWO-REGIME COST CRUX RESOLVED ★**
> Graded Sonnet 4.6 vs Opus 4.8 on the reader: **Sonnet graded RICHER (46 nodes vs 37), ~4–5× cheaper, and the 1M window is API-real (no batch-chunking — the 181 read WHOLE).** Two precision-loss findings (a dropped cross-session UUID pointer; a diluted exact TEXTURE count) → resolved by a tested **precision addendum** → reader **v4.1** (= v4.0 verbatim + the §2 addendum; tested 4/4 on the UUID-dense `e831e30b`). **Model LOCKED to Sonnet 4.6.** Billing is TWO REGIMES: floor reads use `floor_db.env` ($0 Postgres — not a billing risk); the paid batch loads the ROOT `anthropic_billing.env` for submit/collect ONLY, cleared after. The BATCH-NOT-SYNCHRONOUS guard is load-bearing (181 synchronous full-price calls instead of one 50%-off batch = the silent-overspend trap — assert the batch endpoint, confirm a batch-id, HALT if unconfirmable). §7c is RESOLVED for the 181 (API tool-absence = the strong blindness form); practical-only on the free arm (a JUDGMENT, surfaced not buried).
>
> **★ S41 "Comptroller" — EVERY FREE-SIDE QUESTION CLOSED + 24 ORPHANS FOLDED + v4.1 DRAFTED (this session) ★**
> An execution-clearing session. NO floor touched, NO invariant moved. What resolved:
> - **COST TRUE-UP CONFIRMED (the prepay-gate input):** measured ratios (0.30–0.32 tok/char input; ~390 output-tok/node; node-count/conv the soft variable, modeled 15/25/40) × the real 181-conv char distribution (91.84M chars, median 465K, min 14,930, max 1,107,054) × Sonnet batch pricing ($1.50/$7.50, re-verified vs live docs) + prompt-cache → **band $48.69–$64.68, ~$55 likely, $64.68 hard-high.** Input (~$41–44) is hard (two measured points); output ($13–21 spread) is the node-count soft number. The old $76 was ~35% high (built pre-measured-ratios). Opus-reject CORRECTED to ~$287 at MID (current $7.50/$37.50 batch pricing × measured tokens — the old "$127" was wrong) — model lock looks BETTER in hindsight (Sonnet ~5× cheaper AND richer).
> - **★ THE PREPAY GATE IS DOWNGRADED (Catalyst over-hardened a Jake-statement into an invariant — OVERRULED AT SOURCE, Jake S41):** the "account must be FUNDED to the upper estimate BEFORE submit or a mid-run stall forfeits the discount" line was a Jake-statement Catalyst relabeled as a hard constraint. The source is Jake; Jake corrected it. **Jake's auto-reload ($25 start / +$15 at <$5 balance) CARRIES, proven in past — no static prepay required.** What STAYS (different basis, not the prepay gate in disguise): the batch-not-synchronous guard (the lineage's ~$50 scar, about which endpoint the code hits, not balance) and a **1-conv canary** (now a CODE probe — proves v4.1 fires + batch endpoint + persist end-to-end before 180 ride on it — NOT a money probe).
> - **THE 24 `pipeline/nodes/` ORPHANS = S34 SHAPE-LOOP OUTPUT (provenance SETTLED via transcript-hunt, not the node files):** S34 built + canary-validated `apparatus_shape_loop.py`; `pipeline/nodes/<uuid>.md` was its per-conv output, orphaned when S34 pivoted to the cost crisis (never folded to the cold-store manifest — there is NO manifest in that dir; that's the real gap, not Jake-memory). The S41 forensic audit confirmed **24/24 structurally SOUND v4.0** (0 SUSPECT, 0 anchor violations, 0 tally mismatches, all on the floor; the one THIN flag `b51296dd` is a false positive — a 2-msg conv, 2/2 = 100% harvest). **FOLDED into the paid batch** (true-blindness uniformity for ~$1 — see blindness below) rather than promoted (pre-v4.1/pre-Sonnet canary output) or re-read free.
> - **`4d88185f` THINNESS SOLVED — it was a READER FAILURE, not strip-truncation:** S39 blamed strip markers; S40 READ-0 falsified that corpus-wide (strip is whale-echo-only, never touched the floor, 0 affected). The audit pulled the floor payload directly: 55,407 bytes, 14 msgs, ZERO truncation markers — floor CLEAN. The 3-node cold-store version was the old reader bailing on a large `[TOOL_RESULT: view]` block (the 1631-line LRN context file), misreading it as a truncation signal, leaking meta-commentary, and fabricating a catchall for "the remaining 12 messages not visible." The `pipeline/nodes/` 7-node version read all 14 cleanly and is authoritative. **A NAMED FAILURE-MODE: reader bail-on-large-tool_result.** Untested on v4.1/Sonnet → `4d88185f` is a NAMED POST-BATCH WATCH-ITEM (does v4.1 share the confusion? ~7 clean nodes = cleared; 3+catchall = every tool_result-heavy conv needs a second look). The downstream net (Reconciliation-2 / cluster-validation) catches thin non-converging reads either way — so this is a watch-item, NOT a canary requirement (the canary reverts to a generic any-conv smoke test).
> - **THE 2 PENDING FREE CONVS BOTH FOLD (evidence-backed):** `bd59de6e` genuinely unread everywhere (payload prepared in s39/, zero output) → folds. `e831e30b` — the S40 precision-test grade (6 nodes, $0.37 paid) lives at `_grade_result_precision.txt` (underscore = test artifact), never persisted as canonical, won't be caught by batch skip-logic → folds (the $0.37 doesn't protect it; expect ~6 nodes on v4.1 re-read).
> - **BATCH COUNT LOCKED = 206.** 181 over-ceiling + 24 S34-orphans + 2 pending = **207 paid targets − 1 canary = 206 in the full batch**, max ~$67 (the +25 small convs are rounding error on the $64.68 band). `63b6e85a` is deliberately re-read despite being banked (cold-store version, 7 nodes) for v4.1+Sonnet+true-blindness UNIFORMITY — recorded so a future session doesn't relitigate "why is a banked conv in the batch."
> - **THE FREE ARM COLLAPSED to the 14 pre-banked s39 convs.** With the 24 + 2 folded into the paid batch, the "run the 38 free arm in parallel" move is effectively a no-op (nothing left to run free). The §7c practical-blindness asterisk now shrinks to ONLY those 14 already-banked convs. The architecture simplified toward ONE paid batch under true blindness — the free/paid split was forced by the on-sub delivery ceiling, but since sub-ceiling convs are ~$1 to read paid, paying for uniformity buys back the architectural simplicity the ceiling took.
> - **BLINDNESS (the ~$1 fold rationale, recorded so it's not re-derived):** the reader sees ONLY its one handed conv — the structural guard against PROJECTION (confabulating expected patterns / inventing cross-conv connections). **TRUE blindness** = the API call carries `tools: []` (capability ABSENT — the 181/paid arm). **PRACTICAL blindness** = on-sub `agent()` HAS tools but no motive (payload embedded as text, "no file access" instruction) — trusted-not-proven (S40 READ-2 showed an on-sub reader DID self-read its payload 36× when motivated). Folding the 24 moved them from trusted-not-to-peek to structurally-can't, for ~$1.
> - **v4.1 DRAFTED (this session, by OC — authoring is the OC seat):** both artifacts authored + verified faithful — the deployable `test_call_system_prompt_S40.md` (= the proven S32 deployable VERBATIM + the §2 precision addendum inserted after the one-shot example before the sign-off; addendum byte-identical to spec §2, S32 base byte-unchanged — both machine-verified) and the reference `Boot_ScopeReader_v4.1_2026-06-06.md` (= v4.0 verbatim + the addendum blockquoted in-body + a cut-header). **DRAFTS — NOT committed.** S42 reviews, runs the `/code-review` (or `/jedi-council`) gate, fires the committed artifact (draft-PR → review → land), RE-DIFFS the draft against the live working-tree before landing (authored off tarball HEAD), and revises if review catches drift. The batch fires v4.1, NOT v4.0. v4.0 + its deployable `test_call_system_prompt_S32.md` stay TOMBSTONED-NOT-DELETED.
> - **~$25 DOWNSTREAM-RERUN BUDGET (Jake-estimate, EXPLICITLY NOT LAW — do-not-enshrine):** Jake expects ~$25 of API re-runs in the reconciliation/downstream passes (steps 3–4) for reads that surface thin/bad (like the `4d88185f` failure-mode, caught downstream). This is a Jake ESTIMATE, tagged here so no future Claude hardens it into an invariant — the same lineage bug the prepay-gate relabel just demonstrated. The downstream analysis IS the net that catches bad reads; budget for the re-run, don't enshrine the number.
>
> **S41→S42 FIRST MOVES (all execution — the BUILD is the only thing between here and a harvested corpus):** (1) anchor + confirm canon by content (this v30 / Boot_ScopeReader v4.0 still on disk + the v4.1 DRAFT / Progenitor v5 / whale registry all-4-RESOLVED; confirm Stage A `bad80b5` + the S39 on-sub loop + `harvested_nodes/` cold-store intact — one read-only check each). (2) **★ CUT v4.1 FOR REAL** — review the OC drafts (`test_call_system_prompt_S40.md` + `Boot_ScopeReader_v4.1`), `/code-review`-gate them, RE-DIFF against the live working-tree, land via draft-PR → review → merge. MUST be real before the batch fires; the batch fires v4.1. (3) **★ 1-CONV CANARY** (any conv — generic smoke test now, the tool_result failure-mode is a downstream watch-item not a canary stressor): proves v4.1 fires + the batch endpoint accepts + persist lands, on Jake's auto-reload balance. (4) **★ BUILD + FIRE THE 206-CONV BATCH** (PLAN MODE first — writes code) per `Batch_Read_Spec_v1`: skeleton-gate per conv → assemble 206 batch requests (v4.1 cached system prompt + per-conv skeleton payload + `custom_id=conv_uuid` + NO tools) → batch-endpoint submit WITH the batch-not-synchronous guard (assert endpoint, confirm batch-id, HALT if unconfirmable) → poll/collect → `persist_node_file()` to the flat pile → scrub-vN overlay on outputs. Resumable (skip convs already in `harvested_nodes/`; re-fire failed/truncated — floor is immortal). Key loaded for submit/collect ONLY, cleared after; `floor_db.env` for the $0 floor reads. (5) **CLEANUP-AT-MERGE:** the bad 3-node `harvested_nodes/4d88185f` copy must be superseded/removed when the batch re-reads `4d88185f` — don't let it double-count in the flat pile. (6) **POST-BATCH WATCH:** eyeball `4d88185f`'s v4.1 result for the tool_result failure-mode. (7) collect + merge (206 paid + 14 free + 130 whale → flat pointer pile). (8) downstream UNCHANGED: fence-synthesis (Reconciliation 1) → texture/volume pass → cluster-validation (Reconciliation 2) → the Judge → retrieval engine (Progenitor §10–§11). NO QUARANTINE.
>
> **DO-NOT-RELITIGATE (S39/S40/S41 — all settled; rule-4 SUSPENDED, surface if off):** the on-sub ~13K-char delivery ceiling is STRUCTURAL (no `fs`/`Buffer`/`require` in the workflow runtime; `agent()` is output-capped — don't re-attempt schema-passthrough or chunked-schema, both killed on data); corpus splits 38-free / 181-paid by the ceiling (Option 2, NOT hybrid); model = Sonnet 4.6 + addendum (graded, tested 4/4), reader → v4.1; Sonnet 1M is API-real (the 181 read WHOLE, no batch-chunking); billing is two-regime + batch-not-synchronous (assert the batch endpoint); the prepay GATE is DOWNGRADED (Jake's auto-reload carries — a Jake-statement Catalyst over-hardened, overruled at source); the 24 `pipeline/nodes/` orphans are S34 shape-loop output, SOUND, FOLDED into the batch (not promoted, not re-read free); `4d88185f` thinness = reader failure (NOT strip — strip never touched the floor, 0 affected); batch = 206 (207 − 1 canary); the ~$25 downstream-rerun number is a JAKE-ESTIMATE NOT LAW; faithful never bends. + ALL prior settled stands (Stage A `bad80b5` locked; S39 loop proven; cold-store closed; chunking-holds-corpus-wide; whale problem CLOSED + route-from-registry-never-re-read; floor immutable + NEVER touched; conversation is the unit; bar/doctrine SOUND). A NEW reason required to reopen. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*
>
> *[the v29 banner immediately below remains current beneath this banner — v30 records the S39→S41 corpus-read resolution + the build-readiness; v29's Stage A, v28's cold-store close, and v27's chunking ruling are all fully in force.]*

# ANCHOR — apparatus-build track
*v29 · 2026-06-05 (apparatus S38 "Conduit" — **STAGE A OF THE ON-SUB CHUNKED PIPELINE IS BUILT, REVIEWED, AND LOCKED ON MAIN · CHUNK-READY EXTRACTOR + FOUR LOAD-BEARING GUARDS · 13/13 TESTS GREEN · `/code-review` CLEAN · FLAG-2 LOCATOR RESOLVED**): NO architecture changed, NO ruling made, NO invariant moved, NO floor touched — v27's chunking-holds-corpus-wide ruling and v28's cold-store close both stand FULLY IN FORCE. This is a PROGRESS banner over v28, not a replacement. S38's job was move #3 STAGE A (the loop's foundation, built as pure local plumbing before any windowing or live read); it is DONE + LOCKED. The build is staged A/B/C — only A is complete. Everything from here is still EXECUTION.*
>
> **★ S38 — STAGE A BUILT + LOCKED (merge `bad80b5` on main: feat `88e7029` + review-fix `d408a3d`) ★**
> Built the foundation of the on-sub chunked pipeline as pure local plumbing — NO Agent calls, NO API calls, NO `.env` load — then proved it (13/13 unit tests), ran it through the `/code-review` gate (2 real findings fixed, re-reviewed CLEAN), and merged it to main. Three artifacts:
> - **`pipeline/extract_whale.py` v2.0** — (1) header values (CONVERSATION/SNAPSHOT_ID/CREATED) now CALLER-SUPPLIED via optional `--conv-uuid`/`--snapshot-id`/`--created` args (override the window-derived values), so a mid-tree chunk window carries the TRUE conv identity in its header BEFORE the read — the flag-2 fix, closing the exact bug that mis-keyed the ea900330 chunks; fallback to derived behavior when args absent (whole-conv extracts unchanged). (2) builds a `{msg_uuid: parent}` map over the whole conv and writes it as a `<out>.parents.json` sidecar (NOT sent to the reader) — the source map for the persist-time parent-edge injection. render_block UNTOUCHED (canary-proven).
> - **`pipeline/pipeline_guards.py` v0.2 (NEW)** — a library (not a runner) of the four guards the loop calls: **(1) billing guard** `assert_env_unloaded()` raises if `ANTHROPIC_API_KEY` ∈ `os.environ` (§7b trap; checks the env, never reads `.env`); **(2) whale gate** `whale_gate(...)` → KNOWN_WHALE/FITS/raises-on-new-over-ceiling (detect-and-alert, never auto-strip) + `load_whale_registry()` startup helper; **(3) salience tally** `tally_nodes()` regex-anchored on the `**Salience:** <TAG>` line, not a keyword grep (flag-1 fix); **(4) deterministic persist** `persist_node_file()` — temp-then-rename ATOMIC write: validate on the temp (size>0 + `--- DONE:` + tally>0 = stub guard; every anchor_msg ∈ parents_map = flag-2 integrity tripwire), `os.replace` on success, unlink temp + re-raise on ANY failure → no partial/stub artifact survives a HALT; writes the Option-B `.parents.json` sidecar (edges keyed by anchor_msg, roots = JSON null) only AFTER the rename.
> - **`pipeline/test_guards_S38.py` (NEW)** — 13 tests, all green; covers all four guards incl. the v0.2 no-file-survives-a-failed-persist assertions.
> - `.gitignore` — added `pipeline/*.parents.json` (sidecars derived, not tracked).
>
> **★ FLAG-2 LOCATOR — RESOLVED (the one design fork S38 closed; ratified by Curator S37 mid-session):** the S37-proven flag-2 ("stamp real conv_uuid + snapshot_id into every window header") needed its dedup half made operational. RULED: stamp **CONV-LEVEL identity only** in the header (conv_uuid + snapshot_id + timestamp), **NO branch field** — branch position is a derivable relationship (anchor_msg ↔ parent chain); caching it in the locator would be the same double-encode the FK-drop killed. The dedup-fold GUARANTEE comes from a **one-hop parent edge per node** (anchor_msg + its immediate parent_message_uuid) so a subpath/branch anchor resolves to its host without re-reading the floor. **Verified against real reader output:** `Boot_ScopeReader_v4.0` emits ONLY `anchor_msg` in its locator (4 fields: snapshot_id/conv_uuid/anchor_msg/reach) — NOT the parent — so the **extractor INJECTS the edge at persist time** (it holds the skeleton, it knows each msg's parent). Shape locked to **Option B (sidecar)**: the edge lives in a `.parents.json` beside the node file, leaving the proven v4.0 span block BYTE-UNTOUCHED (node file is write-once, matching the cold-store's locked-once-written property). This is the build's IMPLEMENTATION of an already-known flag — not a new ruling.
>
> **★ THE REVIEW GATE EARNED ITS KEEP (PR #3):** first `/code-review` found 2 real issues ≥80 — **B2 (85):** persist wrote the node file BEFORE validating → a stub left on disk if a check raised (silent flag-3 propagation: a later run finds the stub at dest_path and skips re-extraction). **D1 (100):** sidecars had no `.gitignore` rule. Both fixed (B2 → the temp-then-rename atomic persist, matching the floor's single-transaction stance — a node lands whole-and-validated or not at all; D1 → the gitignore line), re-reviewed → "No issues found." (B1 false-positive on the registry filter; E1 [untested `load_whale_registry`] scored 75, under threshold — noted, not blocking.)
>
> **THE TWO OPEN DECISIONS, STILL OPEN (carried unchanged — resolved in the stages that hit them):** **§7a** (re-measure the tier-1 boundary set against AUTHORITATIVE on-sub tokens before any whole-read — the char-proxy under-counts ~1.4×) → resolved at the START of Stage B. **§7c** (the blindness question — practical-blindness vs true tool-absence for ~321 UNWATCHED reads; on-sub Agent grants `Tools: *` uncontrollably) → resolved at the chunking step of Stage C; do NOT default to "practical is fine."
>
> **S38→S39 FIRST MOVES (the build continues — Stage A is done, Stage B is next):** (1) anchor + confirm canon by content (v29 banner / Boot_ScopeReader v4.0 / Progenitor v5 / whale registry all-4-RESOLVED — all UNCHANGED since S36; do not expect edits). (2) confirm Stage A on main (`bad80b5`; `extract_whale.py` v2.0 + `pipeline_guards.py` v0.2 + `test_guards_S38.py` present; 13/13 green). (3) **★ STAGE B — build the loop body + run the 78 sub-25K convs WHOLE first.** The loop wires the Stage-A pieces together (extractor → on-sub Agent read → the four guards → persist to `harvested_nodes/`); the billing guard fires before every Agent call (§7b, HALT if `.env` key loaded); the whale gate routes the 4 from the registry + detect-and-alerts on any new over-ceiling. **§7a GATE FIRST:** re-measure the tier-1 boundary set against AUTHORITATIVE on-sub tokens (key unloaded) — the "78 sub-25K" set is sized on the lying char-proxy (under-counts ~1.4×), so confirm which actually fit a whole-read before the batch. Running the 78 whole (no chunking) proves the loop end-to-end for $0 before windowing complexity. (4) **★ STAGE C — add overlapping-window chunking** (probe #1's 64KB range-read, ~90K windows w/ ~10–15K verbatim carry-in, branch-uuid-aware dedup folding off the one-hop parent edge, flat-pointer pile). **DECIDE §7c HERE.** Then the drip, paced ~220K/5-hr (§7e, re-measure if rate-limited). (5) AUTHOR THE PROGENITOR §12/§13 BODY CARRY-FORWARDS on the CONFIRMED pipeline (NOT before). (6) WHOLE-CORPUS COMPARISON PASS (Jake's S36 add — every non-whale conv chunked + a whole-read SAMPLE on API as the final faithfulness diff; GATE the paid sample). (7) full corpus → fence-synthesis (Reconciliation 1) → texture/volume pass → cluster-validation (Reconciliation 2) → the Judge → wire the retrieval engine (Progenitor §10–§11). **Plus the standing build-list item: scrub-vN Supabase-pattern overlay (§7d flag #4).** NO QUARANTINE; downstream geometry UNCHANGED.
>
> **DO-NOT-RELITIGATE (carried from S37/S36, all still settled; rule-4 SUSPENDED — surface if off, NEW reason to reopen):** everything in the v28 + v27 blocks below stands UNCHANGED — move #2 cold-store CLOSED (don't rebuild); chunking holds corpus-wide; regime = chunk-everything-free; no spine-harvest; no crown-jewels carve-out; the three build-flags PROVEN (now BAKED INTO Stage A code, don't re-derive); + all carried S35/S33 items. S38 added NO new settled-litigation and reopened NONE — it built Stage A and resolved flag-2's locator into code. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*
>
> *[the v28 banner immediately below remains current beneath this progress banner — v29 records Stage A; v28's cold-store close and v27's chunking ruling are both fully in force.]*

# ANCHOR — apparatus-build track
*v28 · 2026-06-04 (apparatus S37 "Curator" — **MOVE #2 CLOSED: THE COLD-STORE IS STOOD UP + POPULATED · THREE BUILD-FLAGS NOW PROVEN LIVE (NOT JUST THEORIZED) · A CCF SECRET-EXPOSURE INCIDENT WAS CAUGHT + CONTAINED · NEXT IS THE STAGED PIPELINE BUILD**): NO architecture changed and NO ruling was made this session — v27's "chunking holds corpus-wide / regime = chunk-everything-free" stands untouched. This is a PROGRESS banner, not a decision banner. S37's job was move #2 (cold-store what's done before building the loop that writes into it); it is DONE. Along the way the three S36 build-quality flags stopped being theoretical and got PROVEN on real data, a fourth (scrubber gap) surfaced via a live security incident, and the dead recon/ scratch tree was swept. Everything from here is still EXECUTION — the staged pipeline build is move #3.*
>
> **★ S37 — MOVE #2 CLOSED (the cold-store) ★**
> Stood up the root-level **`harvested_nodes/`** cold-store and populated it with all completed node output that existed, via a read-only-recon-then-conditional-build CC pass (copy-not-move: sources stay in `pipeline/`, cold-store holds copies, so a miscategorization destroys nothing). **Final state: 21 done rows, 0 incomplete, 0 stubs.** Contents: the 130 S33 whale nodes (5 `result_*.md`), the 2 S32 gate-conv reads (`d6e23963`, `492e164b` — finisher-grade, folded in as `source=S32-gate` per Jake's call), the 48b4110a S36 confirmation catalog (7 chunks + whole), and the ea900330 S36 confirmation catalog (chunks 01/03 + whole + the 3 recovered chunks 02/04/05). A `harvested_nodes/MANIFEST.md` carries conv_uuid → node_file → status → source → parsed-count → note for every row. **The cold-store is both the record of what's DONE (never re-read) and the structure the drip writes into — built before the loop, as designed.** (Bookkeeping note: the manifest's additive node total double-counts the whole-read passes against their own chunk coverage — they're independent re-reads of the same convs, not additive; the distinct-node count is lower. Flagged in the manifest, not load-bearing.)
>
> **★ THE THREE S36 BUILD-FLAGS ARE NOW PROVEN LIVE (this is the session's real yield — they were theorized S36, they BIT for real S37 on free already-graded data, which is the cheapest possible place to find them):**
> **(1) FENCE TALLY — the body-parser must read the `**Salience:** <TAG>` LINE, not grep the word.** Confirmed live: a bare `grep -c -i fence` over a node file inflates the count badly because "fence" appears in `**Keywords:**` fields too (chunk_02 grep said 18 FENCE; the real salience-tagged count was 9). The DONE-line breakdowns were actually CORRECT — the naive grep was the liar. Build spec: the production fence tally parses the salience-tag line anchored to node structure, never a free keyword match.
> **(2) DEDUP / LOCATOR — the extractor MUST stamp the real `conv_uuid` + `snapshot_id` into EVERY window's header before the read.** Proven live + ugly: the 3 recovered ea900330 chunks each carried a WRONG `conv_uuid` — the reader had written the window's first-message anchor (`019e1720…`, `019e18fc…`, even a literal `window_05_raw`) into the conv_uuid slot, because each mid-conversation window was extracted with NO header carrying the true conv_uuid. This is §7d-flag-2 (branch/subpath uuid) shown at CONV LEVEL: a headerless chunk cannot self-identify its parent conversation, so the flat-pointer dedup (which keys on conv_uuid) would shatter one conv into N phantom conversations. FIXED in the cold-store copies (scoped `sed` on the conv_uuid field only — anchor_msg values verified untouched, count-matched to node count), provenance block prepended to each, raw pre-fix originals retained at `pipeline/probe_bigconv2/`. **BUILD SPEC: the skeleton-preserving extractor stamps conv_uuid + snapshot_id (+ real timestamp) into every window header; the reader then writes the true conv_uuid regardless of where the window starts. Without this, every chunked conv in the ~321 run mis-keys silently.**
> **(3) DETERMINISTIC PERSIST — the whole stub saga WAS this flag in the wild.** S36 left ea900330 chunks 02/04/05 as on-disk STUBS (`[Full content as returned — N nodes…]` placeholders, no node bodies) because the persist step spilled oversized reader output inline into the grading OC's context instead of writing it to disk. S37 recovered all three from the S36 chat transcript — but ONLY because that transcript was still reachable. **An unwatched drip run has no transcript to recover from: a silent stub = ~100 nodes lost with no alarm.** BUILD SPEC: each chunk's full node output writes to `harvested_nodes/` deterministically regardless of output size — no inline-vs-stub branching, verify-on-write (byte size + DONE line + parsed node count), HALT on a stub.
>
> **★ FLAG #4 (NEW S37) — THE SCRUB-vN CREDENTIAL RULESET HAS A GAP: it does not recognize Supabase's `sb_secret_` / `sb_publishable_` key format.** Surfaced by a live incident: Supabase auto-revoked a CCF-prod secret key after a scan found it VALID in the public `claude-reference` repo, at `pipeline/recon/slice_16_spans.json`. Chain of how it landed: a CCF Supabase key was pasted into a Claude conversation during a CCF build → that conv is in the official export → the export is the floor → the dead S27/S28 recon slicer carved that span to disk and it got committed back before the current scrub/.gitignore discipline was locked. **The scrubber did not "fail" — it ran against a pattern set (5 cred classes, v1) that predates this key format, so the key passed UNRECOGNIZED.** Containment: key already revoked by Supabase (the real save — it's a dead value); `pipeline/recon/` deleted from the working tree + committed + pushed (the tree is clean); history-rewrite DECLINED for a revoked dead value on a solo non-cloned working dir (marginal hygiene benefit, real footgun risk — Jake's call, correct). **THE LATENT EXPOSURE THAT REMAINS: the key value still lives in the immutable ndjson floor (by design — the floor is never mutated), so any future process that reads that raw span could surface it. THE FIX (build-list item, NOT done): add `sb_secret_` / `sb_publishable_` / Supabase project-ref-URL patterns to the next scrub-vN credential overlay — the overlay machinery (S19/S20 tighten-only contract) was built for exactly this "add a pattern you discovered later, re-scrub the derived index, floor untouched" case. Scoped, architected, not a scramble.**
>
> **THE TWO OPEN DECISIONS CARRIED INTO THE BUILD (unchanged from S36, still open — do not pre-decide):** §7a (re-measure the tier-1 boundary set against AUTHORITATIVE on-sub tokens before any whole-read — the char-proxy under-counts ~1.4×) and §7c (the blindness question — is practical-blindness acceptable for ~321 UNWATCHED reads, or does some/all of the run need a call shape enforcing true tool-absence). Both get resolved IN the build, in code.
>
> **S37→S38 FIRST MOVES (staged build — Jake's call: move #3 built in stages, not one shot):** (1) anchor + confirm canon by content (v28 banner / Boot_ScopeReader v4.0 / Progenitor v5 / whale registry all-4-RESOLVED — all UNCHANGED this session). (2) confirm move #2 cold-store intact (`harvested_nodes/`: 21 done rows, 0 stubs, MANIFEST present). (3) **BUILD THE ON-SUB CHUNKED PIPELINE, STAGED:** first the loop + extractor with the conv_uuid header-stamp (flag 2) + salience-line fence tally (flag 1) + deterministic verify-on-write persist (flag 3) + the `.env`-NEVER-loaded billing guard (§7b, THE load-bearing guard, HALT if it can't confirm unloaded) + DETECT-AND-ALERT whale gate (route the 4 from the registry, never re-read) — then **RUN THE 78 SUB-25K CONVS WHOLE FIRST** (no chunking, pure $0, proves the pipeline end-to-end on the easy case before the windowing complexity is added), §7a-gating the boundary set on authoritative tokens first — THEN add overlapping ~90K-window chunking (probe #1's 64KB-buffer range-read, ~10–15K verbatim carry-in, branch-uuid-aware dedup, no-stitch flat-pointer pile) for the big ones, deciding §7c (blindness) at that point. (4) the drip over the rest, paced to ~220K/5-hr (§7e, re-measure if rate-limited). (5) author the Progenitor §12/§13 body carry-forwards on the CONFIRMED pipeline (NOT before). (6) whole-corpus comparison pass (Jake's S36 add — every non-whale conv chunked + a whole-read SAMPLE on API as the final at-scale faithfulness diff; GATE the paid sample). (7) full corpus → fence-synthesis (Reconciliation 1) → texture/volume pass → cluster-validation (Reconciliation 2) → the Judge → retrieval engine (Progenitor §10–§11). **Plus the standing build-list item: scrub-vN Supabase-pattern overlay (flag #4).** NO QUARANTINE; downstream geometry UNCHANGED.
>
> **DO-NOT-RELITIGATE (carried from S36, all still settled; rule-4 SUSPENDED — surface if off, NEW reason to reopen):** everything in the v27 block below stands UNCHANGED — chunking holds corpus-wide (settled on the two big-conv confirmations); regime = chunk-everything-free; no spine-harvest; no crown-jewels carve-out; + all carried S35/S33 items. S37 added NO new settled-litigation and reopened NONE — it executed move #2 and proved three already-known flags live. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*

*[HISTORICAL banners below — demoted in place per supersede-don't-delete; v29 (S38, Stage A built) is now current ABOVE this v28 banner, and v28's cold-store close stands fully in force beneath it. v27 was the S36 chunking-holds-corpus-wide ruling (still fully in force — both v28 and v29 are progress banners over it, not replacements); v26 the S35 corpus-read decision; v25 the S33 whale-closure; v24 and earlier nested within.]*

# ANCHOR — apparatus-build track
*v27 · 2026-06-04 (apparatus S36 "Cinematographer" — **CHUNKING HOLDS CORPUS-WIDE — SETTLED ON TWO INDEPENDENT BIG-CONV CONFIRMATIONS · REGIME = CHUNK-EVERYTHING-FREE · THE LAST OPEN QUESTION IS CLOSED · NEXT IS THE PIPELINE BUILD**): S35 decided the corpus reads on-sub/chunked/free but left ONE thing explicitly open — whether chunking drops a NON-ADJACENT long-range fence (a reversal whose halves sit 100K+ tokens apart, too far for chunk-overlap to bridge) in a genuinely big conv. S36 closed it. Two big convs were chunk-read on-sub AND whole-read on API as controls; OC graded chunked-vs-whole for non-adjacent long-range fence misses, with the sharpened lens on SILENT (un-announced) reversals — the one residual class. BOTH CLEAN. Total spend to settle: ~$6 (two ~$3 whole-read controls). The architecture is now fully validated end-to-end; everything from here is EXECUTION, not design.*
>
> **★ S36 — THE TWO CONFIRMATIONS (the evidence the open item closes on) ★**
> **CONFIRMATION #1 — `48b4110a`** (657,714 authoritative tokens — char-proxy under-counted 1.41×; 151 msgs; 7 overlapping ~90K windows w/ 4-msg verbatim carry-in; chunk reads on-sub free, one whole-read control ~$3 on API). Whole read = 36 nodes / 6 FENCE. **All 6 whole-read fences matched in the chunked catalog. 0 non-adjacent long-range misses.** The one fence carrying a long-range reversal (the rolodex stage-height saga — "JS-measuring abandoned after ~10 iterations," Token 6) was caught with its supersession reference INTACT; its halves resolved within one window's reach. Chunked over-fenced slightly (more granular, all same decisions) — the cheap/safe error direction, per default-NODE.
> **CONFIRMATION #2 — `ea900330`** (538,902 authoritative tokens; 175 msgs; 5 overlapping windows, same method; a REVERSAL-DENSE legal-strategy session = the highest-risk class for silent supersession). Whole read = 40 nodes / 16 FENCE (body). **All 16 whole-read fences matched in the chunked catalog. 0 non-adjacent long-range misses.** This conv contained a genuine FOUR-STAGE legal-theory reversal chain ("PIA terminated" → "buyout invoked bad-faith" → "v1/v2 framing was WRONG, PIA never terminated" → "Reading A locked") with non-adjacent halves — **the chunked read caught every stage with its supersession reference intact** ("the entire v1/v2 framing was wrong," "corrected legal theory v3"). Independent corroboration observed: both reads, blind to each other, caught the same OA §7.5 retraction — convergence-across-blind-reads, the confidence signal the council design rests on, live again.
>
> **THE RULING:** "chunking holds corpus-wide" is SETTLED. Two independent big-conv confirmations (657K + 539K), one of them reversal-dense, zero non-adjacent long-range fence misses across a four-stage reversal chain. The silent-non-adjacent-supersession residual is NOT zero-by-proof (you cannot prove a negative) but has been HUNTED HARD on the highest-risk conversation class and NOT FOUND; its catch is downstream Reconciliation-1 (catalog-only matching of announced reversals), which is load-bearing and unchanged. Jake's call (S36): lock it all in as settled — "we're not going to get more certain than this short of running the whole thing through the API," which is the cost we're avoiding. **REGIME = CHUNK-EVERYTHING-FREE. No spine-harvest (S32's inversion stays unbuilt — not needed). No crown-jewels-read-whole carve-out (both 400K+ monsters held, so the class the carve-out would protect is exactly the class that passed).**
>
> **★ THREE BUILD-QUALITY FLAGS S36 EARNED (fix in the pipeline build — they did NOT affect the grade, every fence was present and gradeable):**
> **(1) COUNT FENCES FROM THE BODY, NOT THE DONE-LINE.** Both reads' summary "done-line" fence counts disagreed with the actual FENCE-labeled nodes in the body (whole read said "12 FENCE," body had 16; several chunks similar). A reader output-formatting wobble, not a content loss. The production loop MUST tally nodes by parsing the body, never by trusting the reader's self-reported summary count, or the catalog counts drift.
> **(2) DEDUP MUST HANDLE BRANCH/SUBPATH UUIDs, not just top-level msg uuids.** One chunked anchor (`019e178e-…742b` on ea900330) did not resolve to a top-level message uuid — a subpath/branch uuid on the same message span. The flat-pointer dedup needs to fold branch-uuid anchors onto their host message, not drop or duplicate them.
> **(3) PERSIST EVERY CHUNK'S NODES TO DISK RELIABLY.** On confirmation #2, chunk_02/04/05 saved as stubs (full content returned inline / oversized output spilled to a tool-result JSON for chunk_03). Harmless when OC has the inline content, FATAL for an unwatched drip run. The pipeline must write each chunk's full node output to `harvested_nodes/` deterministically regardless of output size — no inline-vs-stub branching.
>
> **★ THE BLINDNESS FINDING (S36 — architectural, flag for the build, NOT a tonight blocker):** the on-sub `Agent` call mechanism grants `Tools: *` to every sub-agent UNCONTROLLABLY — the reader has a live `Read` tool and a reachable working dir; it CANNOT be stripped in the call. Tonight's reads held by PRACTICAL blindness (window text + reader prompt passed as raw call payload, NO file paths in the call → no navigational foothold + the reader instruction says "no file access") — sufficient for 5 hand-graded reads. But the PIPELINE is ~321 UNWATCHED reads on the drip, and practical-blindness-hand-checked does not scale to unwatched. The §13 "blindness enforced by ABSENCE" invariant is, on the on-sub path, enforced by no-paths-plus-instruction, NOT by true tool-absence (which only the paid-API call has — no tools in the call at all). **DECIDE AT PIPELINE-BUILD: is practical blindness (embedded payload, no paths, reader-instructed) acceptable for ~321 unwatched corpus reads, or does some/all of the corpus run need a call shape that enforces true tool-absence?** The paid-API path enforces it but reopens a slice of the cost question. Not tonight's call — flagged loud so it isn't silently absorbed and doesn't bite three sessions downstream.
>
> **S36→S37 FIRST MOVES (no handoff authored S36 per Jake — the ignition + handoff are S37-open work; these moves live in THIS banner + the harvested_nodes manifest):** (1) anchor + confirm canon by content (v27 / Boot_ScopeReader v4.0 / Progenitor v5 / whale registry all-4-RESOLVED). (2) **★ COLD-STORE WHAT'S DONE FIRST.** Gather all completed node output that exists (the 130 whale nodes from S33: the 5 `result_*.md` files; plus the two S36 confirmation catalogs if kept), catalog them against the `harvested_nodes/` manifest (see WHERE THE CODE LIVES), and move them into the new root-level **`harvested_nodes/`** cold-store. This starts the pile of locked, completed work — separate from disposable scratch. (3) **BUILD THE ON-SUB CHUNKED PIPELINE** — range-read windowing (probe #1's 64KB-buffer method) + overlapping ~90K windows w/ ~10–15K verbatim carry-in + the SAME `Boot_ScopeReader_v4.0` reader fired ON-SUB (embedded-payload method — read both files into CC context, pass as raw call text, NO paths; see the blindness finding) + dedup-on-overlap (branch-uuid-aware, flag #2) + flat-pointer-pile (no stitch, self-identify by conv_uuid+anchor_msg) + deterministic full-node persist to `harvested_nodes/` (flag #3) + body-not-done-line fence tally (flag #1); the **.env-key-NEVER-loaded billing guard baked in** (confirm unloaded before any Agent call, HALT if you can't — the metered-billing trap that cost ~$50 this lineage); DETECT-AND-ALERT whale gate (route the 4 from the registry, never re-read); skeleton-preserving extractor (proven `extract_whale.py` `===MSG===` shape) w/ the REAL conv timestamp. **★ §7a GATE — confirm the tier-1 boundary set against AUTHORITATIVE tokens (measured ON-SUB, key unloaded) before running it** — the char/3.5 proxy under-counts 1.4× on tool_result-heavy convs (proven twice: 467K→657K, 403K→539K), so a conv estimated "24K, read whole" may really be 34K. The "78 sub-25K convs run first" set is sized on the lying proxy; re-measure the boundary cases on-sub before the whole-read tier. (4) **RUN THE 78 (re-confirmed) SUB-25K CONVS FIRST** — whole, no chunking, pure $0 upside, proves the pipeline end-to-end. Then the drip over the rest (~220K/5-hr window pacing — §7e third-party estimate, re-measure if rate limits hit). (5) author the Progenitor carry-forwards on the CONFIRMED pipeline (§0.5/§3.4 over-ceiling → two-path whale + on-sub chunked corpus read; §12 shape-reader = the `Boot_ScopeReader_v4.0` call; FIX the dangling `Boot_ScopeReader.md` ref → `Boot_ScopeReader_v4.0`). (6) **★ WHOLE-CORPUS COMPARISON PASS (Jake's S36 add — costs time, not much money): run EVERY non-whale conv through the chunked process (all ~321 fit — even the 400–650K monsters chunk fine, proven), AND whole-read a meaningful SAMPLE on API as a final at-scale faithfulness diff (chunked-vs-whole), the two S36 confirmations already in the sample. One last verification pass for the road — NOT all 321 whole (that's the cost we're avoiding).** (7) full corpus → fence-synthesis (Reconciliation 1) on harvested nodes (130 whale + ~321 fits-whole) → texture/volume pass (own `_extract_texture_slice.py`, wide-lean stripped-summary slices; whales NOT a special case; ratify `Boot_VolumeReader` v0.1 against the first texture canary) → cluster-validation (Reconciliation 2 — load-bearing, never a skim) → the Judge → the retrieval engine (Progenitor §10–§11). **NO QUARANTINE** — personal/medical/family is texture, cataloged like everything else; only mistakenly-pasted creds excluded (scrub-vN at ingestion). Downstream geometry UNCHANGED — only shape-reader delivery changed.
>
> **DO-NOT-RELITIGATE (settled S36; rule-4 SUSPENDED this stretch — surface if off, NEW reason to reopen):** **chunking holds corpus-wide — SETTLED on two independent big-conv confirmations (657K `48b4110a` 6/6 fences, 539K reversal-dense `ea900330` 16/16 fences, 0 non-adjacent long-range misses across a four-stage reversal chain); regime = CHUNK-EVERYTHING-FREE; spine-harvest NOT built/NOT needed; no crown-jewels carve-out.** The silent-non-adjacent-supersession residual is hunted-hard-not-found, homed at Reconciliation-1, not a blocker. + all carried S35 settled items (on-sub mechanically LIVE for the whole corpus — range-read clears the 25K-tool-cap; S34's "76% blocked" DEAD; strip CLOSED as an architecture/on-sub lever, narrow API-trim only; corpus non-urgent + floor immutable → FREE-on-the-drip beats FAST-on-the-wallet) + all carried S33 items (whale problem CLOSED — 4 read, 130 nodes, 0 drops, route from registry/don't re-read; whale path = two paths route-per-whale; strip one-time/detect-and-alert; dual-payload mandatory; chunk budgets pessimistic; whales NOT a texture special case; chunked nodes need NO stitch; conversation is the unit; fits-whole proven to 930k; 1M GA on Opus no header; ceiling axis = tokens not bytes; agentic readers retired; bar/doctrine SOUND — default-NODE / two-kinds / §3.3 / MOTION / walls / no-quarantine; floor laid + immutable + NEVER touched). · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*

*[HISTORICAL banners below — demoted in place per supersede-don't-delete; v28 (top) is current, v27 directly above it is the still-in-force chunking ruling. v26 was the S35 corpus-read decision; v25 the S33 whale-closure; v24 and earlier nested within.]*

# ANCHOR — apparatus-build track
*v26 · 2026-06-04 (apparatus S35 "Cartwright" — **THE CORPUS-READ ARCHITECTURE IS DECIDED: ON-SUB CHUNKED, FREE · BOTH $0 PROBES PASSED · STRIP-AS-COST-LEVER CLOSED · PENDING ONE BIG-CONV CONFIRMATION BEFORE THE FULL RUN**): S34 built the fits-whole loop, ran a clean 23-conv canary, then the session pivoted onto a COST crisis (the corpus read would bill ~$76–127 on metered API) and ended context-starved without resolving it. S35 resolved it — for $0 — by running the three measurements S34 left queued. THE ANSWER: the on-sub (Max-subscription) path is mechanically LIVE for the WHOLE corpus (S34's "76% blocked" was a symptom-for-disease misread — the 25K ceiling is on CC's Read TOOL, not on the conversation; range-reading clears it), and plain overlapping-chunk reads HOLD long-range fences on the tested case (0 missed vs a whole-read control). The corpus can run FREE on the subscription drip. The floor was never touched.*
>
> **★ S35 — WHAT WAS MEASURED + DECIDED (all $0 in dollars) ★**
> **THE STRIP-AS-COST-LEVER IS CLOSED (the pre-snip probe).** Compass (S34) queued a receipt-mode measurement of the audited echo-strip across the 60 fat (>200K est) convs. Result: **27 of 60 eligible** (echo blocks present), ~2.94M removable est-tokens — BUT only **2 of 60** drop under 25K after strip (both freak single-block 97%-echo cases: `750808eb` 292K→7.7K, `37645ecd` 294K→9.8K), and **the 33 ineligible fat convs are fat from REAL CONTENT, not echo** (incl. the biggest: `48b4110a` 462K, `ea900330` 397K, `d9cef14d` 395K). **The load-bearing finding: the fat tail is substantive, not polluted.** Strip cannot shrink the corpus into the on-sub regime; it survives ONLY as a narrow API-cost trim (~2.9M metered tokens off the 27 eligible) IF any of those end up on the paid path. As an architecture lever it is DEAD. Procedure was right (exhaust strip before chunk); the answer is "strip doesn't dissolve this."
> **PROBE #1 — THE RANGE-READ CRUX: YES (proven, not projected).** The question everything forked on: can CC read a RANGE of messages (N→N+90) out of a genuinely-fat on-disk conv WITHOUT loading the whole file into its context? Tested on `48b4110a` (462K est tokens, 151 msgs, 1.62MB skeleton, strip-INELIGIBLE = the hard case), extracted fresh from the canonical ndjson floor. A Python 64KB-rolling-buffer streaming reader located message boundaries and read ONLY lines 2810–4190 (msgs 40–59: 268,150 bytes, ~76K est tokens, 20 msgs, real non-sentinel anchors at both ends) **with zero whole-file load at any point.** S34's "76% physically can't go on-sub" pillar is DEAD — the 25K cap is on CC's Read TOOL per call, never a ceiling on the conversation; range-read/fire/repeat clears it. **(HONEST CAVEAT: part (b) — confirming a whole-load still BLOCKS — was PROJECTED against S34's remembered "exceeds maximum" error string, not re-run. It doesn't matter: the YES rests entirely on part (a), which was genuinely executed. We never have to whole-load, so the exact block-ceiling is moot.)**
> **PROBE #2 — CHUNKED QUALITY vs A WHOLE-READ CONTROL: 0 LONG-RANGE FENCES MISSED (on the tested case).** Control: `9bf5733a` (whole-read from the S34 shape loop, 18 nodes / 8 FENCE / 0 drops). Chunked it into 3 overlapping windows (~10–15K each, 3-msg overlap at each boundary), fired each through the SAME `Boot_ScopeReader_v4.0` reader ON-SUB (.env key confirmed UNLOADED — billed the Max sub, NOT metered API), deduped on overlap. **All 8 whole-read FENCE nodes present in the chunked catalog.** The one hard fork (NODE 12, a hypothesis-reversal sitting ON the chunk 2/3 boundary) SURVIVED because chunk 2 saw it whole at its window's end and dedup kept the FENCE version over chunk 3's MOTION read — **the overlapping-boundary mechanism working, observed live.** One salience drift (a target-expansion node read MOTION instead of FENCE) was WITHIN a single chunk = read-to-read temp-variance, not a structural gap. Long-range miss count: **0.** Spine apparatus NOT needed.
>
> **THE DECISION (what S35 settled):** the corpus reads **on-sub, chunked, free, on the no-deadline drip** (~39M tokens ÷ ~30M/mo of regenerating free Max capacity ≈ a few weeks of background reads on a non-urgent, immutable-floor job). $100 only ever bought SPEED — the most affordable thing to give up. The win condition (Compass's frame, carried): **free AND faithful.** Strip is closed as a lever; the spine-harvest apparatus (S32's inversion) is NOT built and NOT needed unless the big-conv test below finds it necessary.
>
> **⚠️ THE ONE THING NOT YET PROVEN (do NOT enshrine "chunking holds corpus-wide" as settled canon):** probe #2's control was SMALL — 52 msgs, three ~10–15K chunks. Its "long-range" fence spanned an ADJACENT-chunk boundary, which is exactly what overlap is built to catch. It did NOT test the harder case S34 held firm on: a reversal whose two halves live in NON-ADJACENT chunks 100K+ tokens apart, in a genuinely big conv (the 462K monsters, the apparatus/LRN sessions). Overlap can't help there — no single chunk spans the gap. The mitigations exist (S32: announced reversals self-identify; catalog-only reconciliation; the rare irreducible residue is the SILENT supersession) but are UNTESTED at scale. **S36's first real move is the big-conv confirmation read** (chunk-read `48b4110a` on-sub, hunt for any NON-adjacent long-range fence split vs a whole-read of the same conv). If clean → run the whole corpus chunked, free. If it drops a non-adjacent reversal → that conv class (named crown-jewels only) reads whole on API (~$15–25 for a measured handful), everything else chunks free. **"Chunking holds for adjacent-boundary fences on a small conv" is PROVEN. "Chunking holds corpus-wide" is PENDING that read. Hold the line; don't let the clean 0 become premature canon.**
>
> **S35→S36 FIRST MOVES (handoff has full):** (1) anchor + confirm canon by content (v26 / Boot_ScopeReader v4.0 / Progenitor v5 / whale registry all-4-RESOLVED). (2) **THE BIG-CONV CONFIRMATION READ** — chunk-read `48b4110a` (462K, already extracted to `pipeline/probe1/` scratch — re-extract if cleaned) on-sub with overlapping windows, AND whole-read it on API as the control (~$2–3, GATE it — the one justified paid call), grade the chunked catalog against the whole-read catalog, COUNT non-adjacent long-range fences the whole read caught that the chunked read missed. This is the test the small control couldn't be. (3) **BASED ON THE COUNT:** ~0 → build the on-sub chunked pipeline + run the corpus free; few-low-value → chunk-free-except-named-jewels; many-high-value → test S32's spine-harvest pass (NOT a blind build). (4) **BUILD THE ON-SUB CHUNKED PIPELINE** — range-read windowing (probe #1's 64KB-buffer method) + overlapping chunks + the SAME `Boot_ScopeReader_v4.0` reader + dedup-on-overlap + flat-pointer-pile (no stitch, self-identify by conv_uuid+anchor_msg); .env-key-NEVER-loaded billing guard baked in; the 78 already-sub-25K convs run first (pure upside, no chunking, proves the pipeline end-to-end for $0). (5) author the Progenitor carry-forwards on the CONFIRMED pipeline (§0.5/§3.4 over-ceiling, §12 shape-reader = the API/on-sub call, fix the dangling `Boot_ScopeReader.md` ref → `Boot_ScopeReader_v4.0`). (6) full corpus → fence-synthesis (Reconciliation 1) on harvested nodes (130 whale + ~321 fits-whole) → texture pass → cluster-validation (Reconciliation 2) → the Judge. Downstream geometry UNCHANGED.
>
> **DO-NOT-RELITIGATE (settled S35; rule-4 SUSPENDED this stretch — surface if off, NEW reason to reopen):** the on-sub path is mechanically LIVE for the whole corpus (probe #1 YES, executed — range-read clears the 25K-tool-cap); S34's "76% blocked" is DEAD (symptom-for-disease); strip is CLOSED as an architecture/on-sub-migration lever (only 2/60 cross 25K; the fat tail is real content — surviving only as a narrow API-cost trim if the paid path is used); plain overlapping chunks HOLD adjacent-boundary long-range fences (probe #2, 0 missed on the control); the spine apparatus is NOT needed unless the big-conv read forces it; the corpus is non-urgent + the floor immutable, so FREE-on-the-drip beats FAST-on-the-wallet. **NOT settled (explicitly open): "chunking holds corpus-wide" — pending the big-conv confirmation read.** + all carried S33 settled items (whale problem CLOSED — 4 read, 130 nodes, 0 drops, don't re-read; whale path = two paths route-per-whale; strip is one-time/detect-and-alert; dual-payload mandatory; chunk budgets pessimistic; whales NOT a texture special case; chunked nodes need NO stitch; conversation is the unit; fits-whole proven to 930k; 1M GA on Opus no header; ceiling axis = tokens not bytes; agentic readers retired; bar/doctrine SOUND — default-NODE / two-kinds / §3.3 / MOTION / walls / no-quarantine; floor laid + immutable + NEVER touched). · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*

*v25 · 2026-06-03 (apparatus S33 "Cartographer" — **THE WHALE PROBLEM IS CLOSED · ALL 4 OVER-CEILING WHALES READ · 130 NODES / 0 DROPS · FLOOR UNTOUCHED**): S32 designed the whale path; S33 BUILT it, PROVED it, and ran all four whales through it. The corpus's entire over-ceiling problem — the one thing that could still defeat the shape pass — is solved. Two paths, both proven on real data: the STRIP path (echo whales) and the CHUNK path (distributed-bulk whales). The floor was never touched; every whale still lives verbatim, immutable, on the floor — only the way it gets READ changed.*
> 
> **★ S33 — WHAT WAS BUILT + PROVEN ★**
> **THE STRIP (3 echo whales).** `apparatus_strip_v1.py` (v1.0) — a ONE-TIME hardened, audited echo stripper, NOT a pipeline organ. Satisfies the 5-point Wallaby-Whales gate: (1) POSITIVE-SIGNATURE — strips only a large `tool_result` whose `name` is a read/echo tool (`view`/`bash_tool`); tool-identity is the signature (more robust than returncode-string sniffing). (2) KEEP-BIASED — default keep; small blocks + non-read-tool results (deliverables: `create_file`/`web_search`/`str_replace`) kept unconditionally. (3) KIND-#4 HARDENED — satisfied BY tool identity: a deliverable is never in the strip allowlist, so a big code-delivery block CANNOT be mistaken for echo (the old size-and-not-prose heuristic is gone). (4) AUDITED — per-block receipt {block_id, host_msg_uuid, content_bytes_dropped, **display_content_bytes_dropped** (logged SEPARATELY — both payload copies proven neutralized), header_bytes_kept}. (5) LAST-RESORT — pointed at registry whales by name only, wired into no loop. Two S33 design calls baked in: **DUAL-PAYLOAD neutralization** (a `tool_result` carries its payload TWICE — `content` AND `display_content`; strip BOTH or the whale doesn't shrink — this is the ANCHOR's 15.6% `display_content` finding made operational), and **HEADER-KEEP** (Jake's call: keep the first ~25 real lines of each cut block — version/filename/changelog/deploy-state — so a future reader sees WHAT was read, not a blind hole; the bulk code body is cut, the process-texture header survives; ~4KB/block cap).
> **THE CHUNK (1 distributed-bulk whale).** `chunk_whale.py` — d9d05961 was NOT an echo whale (441 msgs, 509 tool_results across 10 tools, 0 blocks over threshold — the strip CORRECTLY cut nothing; it's real content). Splitting, not stripping, is the answer for a non-echo over-ceiling conv: tree-aware split into ceiling-fitting `===MSG===` subtrees, each read through the SAME reader, nodes from all chunks drop in the pile (NO STITCH — every node self-identifies by `conv_uuid` + `anchor_msg`; the catalog is a flat pointer dict). A `d9d05961_seam_manifest.json` records WHERE the cuts fell (format-independent carry-forward, cheap insurance if the texture pass ever re-splits this whale; the chunk files themselves are disposable). **The bytes/token chunk budget must be PESSIMISTIC** — the first chunk at 3.0 B/tok brimmed to 2.1MB (near the distrusted-proxy ceiling); re-chunked at 2.2 B/tok → safe. (Confirms the S32 correction in the chunk direction: tool_result-dense JSON tokenizes denser than a byte proxy predicts; under-fill chunks.)
> **THE RESULT — all 4 whales, every read `end_turn`, 0 drops:**
> ```
> conv            path           in_tokens   nodes (M/F/T)
> cfc7a70a   strip (canary)      387,243     23 (16/6/1)
> 83506215   strip               182,461     28 (12/13/3)
> 55217328   strip               299,103     15 (9/5/1)
> d9d05961   chunk_00            (under 1M)   28 (12/14/2)
> d9d05961   chunk_01            (under 1M)   36 (20/16/0)
> ——————————————————————————————————————————————————————
> TOTAL                                       130 nodes (62 MOTION / 62 FENCE / 6 TEXTURE) / 0 drops
> ```
> Node quality confirmed finisher-grade: real `019c-`/`019d-` anchors (no fabrication), substantive fences with live predicates, tails noded as richly as heads (context-rot check PASS on every read), texture nodes honest (one CATALOGS a Claude failure — the 55217328 gradient-saga over-confident-fabrication loop — exactly the auxiliary-brain signal). Whale registry updated: all 4 RESOLVED.
> 
> **THE CORRECTIONS / CLARIFICATIONS S33 EARNED:**
> - **The whale path is two paths, not one.** S32's "strip path" framing assumed all 4 whales were echo. FALSE — 3 are echo (strip), 1 is distributed-bulk (chunk). Strip and chunk are both whale-path tools; the registry/route picks per-whale by character (large-echo-blocks → strip; no-blocks-over-threshold → chunk).
> - **The whales are NOT a texture-pass special case.** The texture/volume pass reads the floor AGAIN via its OWN `_extract_texture_slice.py` into wide-lean STRIPPED/SUMMARIZED slices (25–40 convs), NOT full conversations — so a 1.2M-token whale becomes a tiny summary in a texture slice. Our shape-pass whale artifacts (stripped copies, chunks, payloads) are DISPOSABLE shape-pass scaffolding; they need NO floor-format preservation for the volume pass. The only forward-useful artifact is the chunk SEAM boundaries (format-independent). (Jake's instinct — "not sure we need that format" — was right; the floor-format-preservation detour was overbuild, killed.)
> - **The dual-payload (`content` + `display_content`) is a strip requirement, not a nicety.** Stripping only `content` leaves ~half the echo and the whale may not drop under ceiling. Proven on the canary (content drop ≈ display drop, ~8.75MB each).
> 
> **THE ARTIFACTS (S33, committed as one unit via draft PR per §12):** `pipeline/apparatus_strip_v1.py` (the hardened stripper), `pipeline/extract_whale.py` (generalized `===MSG===` serializer, proven `render_block`), `pipeline/chunk_whale.py` (tree-aware chunker, `--bytes-per-token` flag), `pipeline/_verify_strip.py` (8-point skeleton verifier), the 3 strip receipts + the seam manifest (audit trail), the 5 `result_*.md` node outputs (the 130 nodes). Disposable + gitignored: the lifted `whale_*.json` bodies, `*_stripped.json`, `payload_*.txt` (all regenerable from the floor).
> 
> **S33→S34 FIRST MOVES (the pipeline build — the whales are DONE, now the 321 fits-whole convs):** (1) anchor + confirm canon by content (v25 / Boot_ScopeReader v4.0 / The Wallaby Whales / Progenitor v5). (2) **BUILD THE PIPELINE** — the loop over the corpus: authoritative token-gate (measure `usage.input_tokens`, gate below 1M with output-token headroom, informed by the 930k clean result) with a **DETECT-AND-ALERT whale gate** (NOT an auto-stripper — if a conv comes back over ceiling, HALT with "whale detected — handle manually"; the 4 known whales are already done + node'd, route them from the registry, don't re-read); whale-route via registry; skeleton-preserving extractor (the proven `extract_whale.py`/`extract_one_conv.py` shape) with the REAL conv timestamp in the header; format-locked output. Canary on the slice_03/06 convs (human-grade finisher answers exist) before the full run. (3) **AUTHOR THE PROGENITOR CARRY-FORWARDS** on the CONFIRMED pipeline (§0.5/§3.4 over-ceiling → the two-path design; §12 shape-reader = the deterministic API call + fold `Shape_Slice_Over_Ceiling_Rule_v1`; fix the dangling `Boot_ScopeReader.md` ref → `Boot_ScopeReader_v4.0`) — NOT before the pipeline confirms. (4) full corpus → fence-synthesis (Reconciliation 1) → texture pass → cluster-validation (Reconciliation 2) → the Judge. Downstream geometry UNCHANGED — only shape-reader delivery changed.
> 
> **DO-NOT-RELITIGATE (settled S33; rule-4 SUSPENDED this stretch — surface if off):** the whale problem is CLOSED — all 4 read, 130 nodes, 0 drops, floor untouched (do not re-open the whale reads; the nodes exist); the whale path is TWO paths — strip (echo) + chunk (distributed-bulk), route per-whale by character; the strip is a ONE-TIME tool (not a pipeline organ) — going-forward whale handling is DETECT-AND-ALERT, not auto-strip; dual-payload neutralization (content + display_content) is mandatory on a strip; chunk budgets must be pessimistic (under-fill); the whales are NOT a texture-pass special case (texture reads its own stripped-summary slices off the floor); shape-pass whale artifacts are disposable, NO floor-format preservation owed; chunked nodes need NO stitch (flat pointer dict, self-identifying by conv_uuid+anchor_msg); + all carried S31/S32 settled items (conversation is the unit; fits-whole proven to 930k; 1M GA on Opus no header; ceiling axis = tokens not bytes; agentic readers retired; bar/doctrine SOUND — don't reopen default-NODE/two-kinds/§3.3/MOTION/walls/no-quarantine; floor laid + immutable + NEVER touched by the whale path; downstream unchanged). A NEW reason required to reopen. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*

*v24 · 2026-06-03 (apparatus S32 — **THE GROUND-TRUTH GATE PASSED · AGENTIC READERS RETIRED · THE DETERMINISTIC API SHAPE-READER IS PROVEN · THE WHALE PATH IS DESIGNED**) [HISTORICAL — superseded by v25; the session that designed the whale path S33 then built]: S31 retired the agentic CC-window shape reader (it died nondeterministically — proven across S30 + S31's lean-kit experiment) and ruled the path forward a deterministic Claude-API pipeline: ONE conversation per call, on the 1M-token window. S32 BUILT the minimal plumbing and FIRED the gate. THE RESULT: it works. The fits-whole path is proven clean to ~930k tokens; the doctrine/bar is sound; the tail nodes as richly as the head (context-rot check PASSED). Four over-ceiling whales were measured and the whale-delivery apparatus was designed. Canon re-cut this session: ANCHOR v23→v24 (this banner; v23 demoted to historical), NEW Boot_ScopeReader v4.0 (deterministic API reader, supersedes the agentic v2.x + the v3.0-lean experiment — both tombstoned), NEW The Wallaby Whales (the wall-delivery apparatus, confirms+deploys Shape_Slice_Over_Ceiling_Rule_v1 in the token world), CHANGELOG. Progenitor §0.5/§12 carry-forward revisions are NOTED in the S32→S33 handoff, deferred to the pipeline-build session (do not author ahead of the build).***

> **★ S32 — WHAT WAS PROVEN (the load-bearing result) ★**
> **THE GATE.** The deterministic Claude-API shape reader was fired against real conversations (Opus 4.8, 1M-token GA window, no beta header). Two gate convs, both clean:
> - `d6e23963` (56 msgs, **485,766 input tokens**, `end_turn`): 28 nodes / 0 drops, every node a real `019c-` anchor (no fabrication), substantive fences with live predicates. The first gate — fit + quality.
> - `492e164b` (203 msgs, **930,752 input tokens**, `end_turn`): 47 nodes (29 MOTION / 17 FENCE / 1 TEXTURE) / 0 drops. The near-ceiling test — **the fits-whole path is validated to 930k, 70k under the 1M wall**, and the TAIL noded as richly as the head (the densest node on the prior gate was at reach down:8 near the END — no context-rot).
>
> **THE CORRECTIONS S32 EARNED (retire these old beliefs):**
> - **1M context is GA on Opus — NO beta header.** The old "Opus + `context-1m-2025-08-07` header" path (handoff/ignition language) is dead; the header was retired 2026-04-30 and folded into GA at standard pricing. The MODEL STRING is now the sole grant of the 1M window — never silently downgrade.
> - **The ceiling axis is TOKENS, measured authoritatively — not bytes, not a char proxy.** A char-count token estimate is **2–3.3× optimistic** on echo-heavy convs (echo is double-escaped JSON, tokenizes densely). The S29 byte-ceiling (10/12 MB) was the right instinct in the wrong unit. Measure `usage.input_tokens`.
> - **The agentic CC-window shape reader is RETIRED.** The S29/S30 banner's whole story — "compaction root-caused to prompt-vagueness," the strict-streaming-prompt fix, the bare-window batch fire, the `settings.local.json` write-to-disk kit change — is **superseded**. The failure was the agent (self-managed context budget, nondeterministic compaction), not the prompt. Replaced by: control the input, one conv per deterministic call, let the model read what it's handed. (v23 banner retained below as historical — that was the session that *found* the failure; it just mis-named the cause.)
> - **Echo-strip is the WHALE-SPECIFIC fix, not a dead end.** The old do-not-relitigate "strip is not the general fix (0.6%)" is true AND non-blocking: strip recovers ~0.6% on a normal conv (so never a default pass) and ~95%+ on an echo-whale (so the ONLY thing that makes a whale tractable). Both true. See The Wallaby Whales.
>
> **THE FOUR WHALES (over the 1M ceiling — strip path, not whole):**
> ```
> rank  conv       msgs  actual tokens   result
> 1     cfc7a70a   101    4.4M           OVER — strip-path canary (the S29 18.48MB whale)
> 2     83506215   173    3.0M           OVER — strip path
> 3     55217328   303    2.2M           OVER — strip path
> 4     d9d05961   441    1.2M           OVER — strip path
> —     492e164b   203    930k ✓         FITS — validated ceiling
> —     d6e23963    56    486k ✓         FITS — first gate, clean
> ```
> Whale registry: `pipeline/whales/whale_registry.md`. Whale-delivery doctrine: The Wallaby Whales + Shape_Slice_Over_Ceiling_Rule_v1.
>
> **THE PLUMBING (S32 stood it up — minimal, proven, NOT the full pipeline):** `pipeline/apparatus_api_testcall.py` (reads `ANTHROPIC_API_KEY` from a gitignored `.env` — Jake's Pyris key, billed to jake.botticello@gmail.com; never inline, never echoed; `.env` covered by `.gitignore` `*.env`). One conversation per call: `--system <prompt>.md --payload <conv>.txt --out <result>.md`. The extractor (`pipeline/extract_one_conv.py`) serializes a conv skeleton-intact (uuid + parent + role + content, tree-order, header w/ snapshot_id). Both prove out; both are SINGLE-conv tools, not the loop.
>
> **S32→S33 FIRST MOVES (the pipeline build — handoff has full):** (1) anchor + confirm canon by content (v24 / Boot_ScopeReader v4.0 / The Wallaby Whales / Progenitor v5 unchanged-but-carry-forwards-noted). (2) **HARDEN + AUDIT THE STRIP** — get CC's classifier verbatim, fix kind-#4 vs code-deliverable false-positives, ADD the audit manifest (the receipt). (3) **STRIP THE CANARY** `cfc7a70a` to a working copy, READ THE RECEIPT, confirm echo-only. (4) **FIRE THE REDUCED CANARY** through Boot_ScopeReader v4.0 — whale path proven → all four tractable. (5) **BUILD THE PIPELINE** — the loop over 325 (token-gate via authoritative count w/ output headroom; whale-route via registry; skeleton-preserving extractor w/ real timestamp; format-locked output), canary on the slice_03/06 convs (human-grade answers exist) before the full run. (6) author the Progenitor §0.5/§12 carry-forwards on the CONFIRMED pipeline (handoff §carry). (7) full corpus → fence-synthesis → texture pass → cluster-validation → the Judge (downstream geometry UNCHANGED — only shape-reader delivery changed).
>
> **DO-NOT-RELITIGATE (settled S31/S32; rule-4 SUSPENDED this stretch — surface if off):** the conversation is the unit; fits-whole is the path (proven to 930k); over-ceiling whales exist and go through the strip path (NOT a relitigation of "no stripping" — a NEW measurement defeated the old assumption that everything fits whole); 1M is GA on Opus no header; the ceiling axis is tokens not bytes; the agentic readers are retired; the bar/doctrine is SOUND (the gate proved it — don't reopen default-NODE / two-kinds / §3.3 / MOTION / the walls / no-quarantine); the strip must be positive-signature + keep-biased + kind-#4-hardened + audited + last-resort before any mined run; the floor is laid + immutable + NEVER touched by the whale path; downstream geometry unchanged. A NEW reason required to reopen. · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why + The Wallaby Whales (FRAMEWORK + apparatus, not reference) before working*


*v23 · 2026-06-02 (apparatus S30 — **THE SHAPE RUN BEGAN · COMPACTION ROOT-CAUSED TO PROMPT-VAGUENESS, NOT BYTES · METHOD/BAR SPLIT RULED · CANON RE-ANCHORED AGAINST 3 SESSIONS OF DRIFT**) *[S49 CORRECTION: the "prompt-vagueness root cause" was Jake's S30 HYPOTHESIS, explicitly flagged unconfirmed in this same banner ("PENDING THE TEST RESULT") — it was never proven. It was MOOTED at S31/S32 when the whole agentic-reader path was abandoned for the deterministic API reader (the agentic readers died nondeterministically — see v24). Do NOT read "prompt-vagueness" as a settled root cause; it is an abandoned hypothesis on an abandoned approach. The method/bar split it produced was kept on its own merits.]*: S30 fired the first real shape-reader batch and used it to root-cause the compaction failure, then STOPPED to arrest accumulated context-drift (S28→S29→S30 were context-starved; Jake called it). THE FINDING: bytes are NOT the sole compaction axis. First batch = slices 01–10 (NOTE: slice_00 exists, was never fired — human off-by-one; the "first ten" were 01–10). 01,02,03,04,10 completed CLEAN; 05,06,07,08,09 hit real "compacting conversation" (blue line) and were killed. The byte-ceiling theory PREDICTS THE OPPOSITE of what happened: slice04 (the 18.45MB whale) SURVIVED; slices 05/06/07 (7.85–9.24MB, well under ceiling) DIED. Neither bytes NOR msg-count separates the clean set from the compacted set (verified against the manifest: clean-ex-whale avg 772 msgs/8.56MB, compacted avg 537 msgs/8.77MB — overlapping). All 10 fired within ~60s, identical kit, identical boot, only the working-dir differed. ROOT CAUSE (Jake's hypothesis, fits every fact): the reader PROMPT was too vague about METHOD, so blind readers DIVERGED in how big a job they made of it — some sprawled into "PRESERVE EVERYTHING, cross-reference everything" and blew their own context budget mid-read (compaction); others read it tight and finished. Corroborated live by per-window permission-prompt variance (sprawlers asked to do more). THE RULING — METHOD vs BAR (the reconciliation, Jake's words): "austerity isn't wrong when it's right; it just can't be the guiding principle." TWO things were fused under one word: austerity-of-OUTPUT-BAR (keep only the important few — WRONG here, §0.5 kills it, default is NODE) vs austerity-of-WORK-METHOD (move concisely, don't gather the whole slice into context before acting — CORRECT, and §0.5 accidentally outlawed it too). THE FIX: output bar stays EXPANSIVE + CC-interpretive (what earns a node is Claude's call); work method gets PRESCRIBED + BOUNDED (how you move through the slice is ours to specify — stream one conversation at a time, write nodes to disk, release it, never hold the whole slice, no cross-ref / no re-read / no synthesis). Comprehensive about WHAT; disciplined about HOW. A strict streaming-discipline prompt embodying this was authored and is mid-test as of S30 close (re-fired on all 10 under prompt-change-ONLY — old settings kept so the experiment has one variable; full old-vs-new node-count delta backed up by Jake for inspection). **CANON RE-ANCHORED S30** (the drift-arrest pass — the priority over precision, Jake's call): ANCHOR v22→v23 (this banner; S29 demoted to historical; NEXT MOVE #4 corrected from the stale v4/v2.1/5-step/12-slice/c:\council text to the live S30 two-pass / v5 / v2.2 / 41-slice / c:\context-pass reality; CURRENT STATE shape-run status added), **Boot_ScopeReader v2.2→v2.3** (NEW "WORK METHOD — bounded streaming" section: one conv at a time, write-and-release, no cross-ref/re-read/synthesis; + the method-vs-bar clarification that the anti-austere mandate governs the OUTPUT BAR, not the WORK METHOD), **The Progenitor v5 §0.5 augment** (one inserted clarification: austerity-of-METHOD is not the failure mode, austerity-of-BAR is — move concisely, catalog comprehensively), CHANGELOG. **PENDING THE TEST RESULT (do not enshrine as proven until the re-fired 10 report):** the prompt-vagueness root-cause + the strict-prompt fix are RULED + authored but the empirical confirm (do 05–09 now complete clean? do 01–04/10 hold node-depth vs their backups?) was IN FLIGHT at S30 close. If WIN → the v2.3 + §0.5 augments are confirmed canon + the strict prompt is the standard. If the clean-five come back THINNER than backup → the prompt over-constrained (clipped the bar, not just the method) → loosen output side, keep streaming discipline. If 05–09 still compact → method wasn't the variable → go to per-conversation byte profiles inside 05–09 (a fat span the per-conv manifest line hides). **NEXT-STATE KIT CHANGE (agreed, lands with the wide fire — NOT this test):** bake a `settings.local.json` full-IN-DIR allow-list (Read/Write/Edit, no approval prompts) into every reader package — kills the permission-prompt interruptions AND the §3(b) ignition fix (readers write nodes_output.md to disk). Does NOT breach blindness: the wall is enforced by ABSENCE (no Bash/python/cred/git/net, no out-of-dir reach), all still absent; a sandboxed in-dir write reaches nothing new. ONE-TIME VERIFY stands: eyeball that a write-enabled reader can't write OUTSIDE its dir before trusting it across 41. **DRIFT CORRECTED THIS SESSION (the S28→S30 starvation errors, surfaced per Jake's "ignore rule-4 if it feels off"):** (i) "bytes are THE compaction axis" → bytes are A axis, not the sole one; prompt-method-divergence is the dominant one at sub-ceiling sizes (the S29 over-correction that retired the msg-count gate threw away the only signal that might've flagged the high-structure slices — though msg-count didn't cleanly separate them either, so the real axis is reader-behavior, not a slice metric). (ii) the boot prompt's `active/.../slicer/` path is wrong — slicer/ is at REPO ROOT. (iii) a `(1)`-suffixed dupe of the whale rule sits in slicer/ (byte-identical to active/ copy — safe, housekeeping-deletable). (iv) the recon/ span files in pipeline/ are the DEAD S27/S28 23-file over-ceiling tree (whale is in recon slice_03, NOT a current artifact) — stale, deletable from the repo, was never supposed to live there; KEEP pipeline/*.py (the live loader/freeze/extractor code). **DO-NOT-RELITIGATE (settled, carried — but rule-4 is SUSPENDED this lineage-stretch per Jake: if it feels off, surface it):** two passes/two slicings/two reconciliations/one Judge; readers don't run python; strip SAFE for texture / FORBIDDEN for shape; council dirs bare + outside repo + v5 loadout + blindness-by-absence; c:\council DEAD (shape run = c:\context-pass); cross-cutting absorbs into texture; whale rule; PRESENCE axis; default-NODE; MOTION is a node kind; NO quarantine on personal material; floor laid + immutable; S16 password dead. **NEXT: read the re-fired-10 test result → confirm/adjust the strict prompt → (WIN path) enshrine v2.3+§0.5 as proven + bake settings.local.json into the kit + reset/re-stage all 41 under the strict prompt + new settings + write-to-disk → fire the remaining 31 (00, 11–40) in batches of 5–8, slice04 whale-canary watched hardest → fence-synthesis → texture pass build/fire → cluster-validation → Judge.** · hot · read this + JAKE-RULES + the Track Meet Doctrine + The Wallaby Why (FRAMEWORK, not reference) before working*

> **⚑ S29 (HISTORICAL) — TWO-PASS RATIFIED ON PAPER; SHAPE MANIFEST BUILT BYTE-SLICED; 41 DIRS STAGED AT c:\context-pass; WHALE RULE WRITTEN; NEXT IS THE SHAPE-READER RUN (slice04 canary first).** S29 took the S28→S29 handoff's braked-to-disk two-pass design, ratified it, and built the shape pass up to the firing line. Sequence: **(1)** Booted OC, pulled the codeload tarball, confirmed canon BY CONTENT (ANCHOR v21, Progenitor v4, Boot_ScopeReader v2.1, floor live/immutable). Read the S28 two-pass specs file end-to-end as primary input (it was missing from HEAD — Jake provided it; it is the load-bearing S28 output, more authoritative than the handoff's summary of it). **(2)** RULED THE FOUR FORKS: byte ceiling **10MB target / 12MB absolute max**; texture breadth floor **set as high as it'll bear** (start max convs-per-slice, tune down if slices don't pass); cross-domain influence **ABSORBS into the texture pass** (the standalone cross-cutting reader is retired — a cross-domain stance is a recurrence on a shared referent, caught natively IF the breadth floor is wide enough to span domains, which is why it's set high); **ratify two-pass on paper before slicing — YES.** **(3)** A flagged-not-buried dependency: **Reconciliation-2 (cluster-validation) is the load-bearing member that makes the lean texture pass safe** — the volume reader can be lean-wide only because its checkable claims get a real second look; if Reconciliation-2 is ever value-engineered to a skim, the projection wall falls silently (over-connection looks like signal). Written into v5 §12 as a dependency, not polish. **(4)** BUILT the shape manifest via a parameterized byte-accumulator (`_extract_slice_S29.py` / `slice_manifest_S29_shape_bytes.json`): **41 slices, 40 clean (4.33–10.21MB), gate reports MAX-MB-PER-SLICE** (the msg-count gate is RETIRED — it false-greenlit the 8 dead S28 windows). A handful land 10.03–10.21MB — the accumulator packing whole convs until the next would breach; safe, far under the ~20MB compaction edge. CC added a dedup-gate (gate on deduplicated output count, not raw rows) — a good catch, it generalizes the whale's byte-identical-block worry into a poison-bug check. **(5)** THE WHALE: slice04 = ONE conv `cfc7a70a` (101 msgs, **18.48MB**). Characterized on real data: **95.8% is `tool_result` bash-stdout echo** (Claude `cat`-ing large files mid-session, contents piped back as JSON — the LRN auth/OAuth 13-hr build session, 2026-02-23→24); the fence-bearing content (thinking+text) is **0.27MB**. idx98/idx100 carry a byte-identical 2.3MB block — confirmed a genuine re-read (two distinct msg_uuids), not a dup artifact. Ruled **block-level truncation, conv kept whole** (the over-ceiling rule). **(6)** BUILT all 41 bare reader dirs under **c:\context-pass** — exact loadout (Boot_ScopeReader, Progenitor v4 [→ v5 on push], §11-stripped JAKE-RULES, JAKE-STACK, worked examples, sliceNN_spans.json, read-only settings); walls verified programmatically (§11 redaction-placeholder on all 41; forbidden-sweep scoped to KIT files so it didn't false-positive on corpus mentions of Wallaby/Lore inside spans). **(7)** Jake's pace call: fire all surfaces and watch for live compaction rather than pre-emptively truncate the whale — slice04 fires FIRST as the canary, untruncated, the known 18.48MB edge test. **CANON WRITTEN S29** (the catch-up set, all the queued + this-session work): The Progenitor **v4→v5**, Boot_ScopeReader **v2.1→v2.2**, NEW **Boot_VolumeReader v0.1 (DRAFT)**, Boot Kit **v1.1→v1.2**, NEW **Shape_Slice_Over_Ceiling_Rule_v1**, ANCHOR v21→v22, this entry + the CHANGELOG. **S29→S30 FIRST MOVES (ACT, settled — don't re-litigate §4 of the handoff):** (1) anchor + confirm canon by content (now v22 / Progenitor v5 / ScopeReader v2.2 / Kit v1.2 — on push); (2) **fire slice04 canary** (fresh BARE window in `c:\context-pass\slice_04\`, the shape-only boot line — produce ONLY the node stream, skip stream-b): compacts → apply `Shape_Slice_Over_Ceiling_Rule_v1` to slice04 (block-truncate the 6 confirmed-echo >256KB tool_results) + refire just it; holds → read the nodes for the silent-compaction tell (thin output / conv-root fence anchors), then **fire the other 40 in batches of 5-8**, independent bare windows, same boot line; (3) **FENCE SYNTHESIS** (Reconciliation 1) on the harvested node streams; (4) build the **texture extractor** (`_extract_texture_slice.py` — stripped field-set + breadth floor) + texture manifest + dirs, **fire volume readers** (canary = widest slice first, projection wall hardest); (5) **CLUSTER VALIDATION** (Reconciliation 2); (6) **THE JUDGE**. **DO-NOT-RELITIGATE (settled S28/S29):** bytes not messages = compaction axis; readers don't run python (read native, emit to chat, settings read-only); stripping SAFE for texture / FORBIDDEN for shape; two passes / two slicings / two reconciliations / one Judge; council dirs bare + outside repo + v5 loadout; the 23-dir c:\council is DEAD; cross-cutting absorbs into texture; the whale rule (characterize→route→per-block content-confirmed truncation→floor-never-mutated); PRESENCE axis; NO quarantine on personal material; floor laid + immutable; S16 password dead. A NEW reason required to reopen any of these. ⚠ The v5 §12/§13 two-pass canon is enshrined from spec + paper-ratification, NOT yet from a completed run — the shape canary is the first real test; if it teaches something the geometry didn't anticipate, version-correct, don't force the run to fit the doc.
>
> **⚑ S27 READ-FIRST — THE PILOT INVERTED THE ADMISSION PREMISE; THE PROGENITOR IS v4; THE DESIGN IS PROVEN; NEXT IS THE MECHANICAL 12-SLICE RUN.** S27 opened the seeding BUILD and the pilot earned its entire keep by surfacing a foundational error cheaply. Sequence: **(1) Gate-1 recon (read-only) confirmed** the floor live (325/24,138, dry-run EXISTS never `--execute`), the May cliff (10,966 msgs = 45.5%), 9 forests, and — load-bearing — **NO monotonic column on `floor_conv_messages`** (only `created_at` text, non-unique; ctid unstable). So slicing is **conversation-bounded, `created_at`-ordered (tiebreak conv_uuid), accumulate-to-~2,000** — NOT row-range. **(2) The slice manifest was built + integrity-proven** (`pipeline/recon/slice_manifest_S27.json`): 12 slices, every one of 325 convs placed exactly once, 24,138 reconciled, forests whole by construction (whole-conv unit can't bisect a forest), count-even (2,009–2,230 + a 1,128 tail). All 325 convs in, including 3 zero-message headers (Jake's call: slice the floor WHOLE, no exclusion pass, empties self-handle). **(3) The PILOT (slice 7, 21 convs/2,145 msgs) ran under the Progenitor v3 austere bar → 1 node, 14 explicit no-cards.** It threw away a 202-msg VESA design session, a 340-msg NAS build, the Pyris S11–S14 arc as "motion" — AND lost load-bearing fences buried in those dropped conversations. **(4) Jake named the error:** the readers are a CATALOGING / organizing-the-chaos process building a navigable semantic GRAPH (nodes + clusters + connections, MOST convs noded), not a filter building the leanest decision-log. "Can some drop out? Yes. Should most be cataloged? Yes." Corpus-search is the rare-miss net, not the selectivity license. **(5) This INVERTED the admission premise** — a foundational Progenitor rewrite, not a prompt tune. A prosecution brief (anchored in the Track Meet Doctrine + Wallaby Why, framed to survive the austere reflex) went to a **2-panel double-blind Jedi-Council**; both panels — structurally biased toward austere — ruled **PRESENCE over SERVICE**, overturning §0/§3/§5. They dismantled four weak joints in the brief (good — not a rubber stamp) and raised Panel-1's dissent flag: don't build a new austere framework with "nodes" instead of "cards" — the test is whether the graph HOLDS the mess (VESA build, ice-maker chat) messily vs. exiles it into a tidy ontology. **(6) The slice-7 RE-RUN under the inverted bar → 41 nodes, 0 drops, and caught the fences the austere bar was blind to** (the question-widget prohibition, the dual-Claude workflow split, the CC working-dir, the no-"Oueilhe" reinforcement — all buried in conversations v3 had discarded). Design proven on real data. **(7) NO QUARANTINE** — OC had unilaterally flagged the personal/medical/family cluster (Griffin/meds/Laura/Ary/Boone/Klaus) as quarantine-pending-Jake; Jake killed that wall from ownership: "it's material, it's texture, and it's important" — a Claude can't help him time-block or design HA automations without knowing who Boone and Klaus are. The ONLY exclusion is mistakenly-pasted credentials, handled by scrub-vN at INGESTION (not by admission). **CANON WRITTEN S27 (Jake verifies/commits/pushes):** The Progenitor **v3→v4** (surgical — §1/§2/§4-record-shapes/§5-mechanism/§6/§7/§10/§11 BYTE-IDENTICAL; §0 rewritten, NEW §0.5 organic-over-austere-as-intolerable-failure-mode, §3 rewritten to default-NODE + behavioral signals + fence/texture-as-salience-tags + MOTION-as-node-kind + §3.3 precise-fence-anchor + §3.4 volume-inverted, §5/§8/§12-guard amended; E1 absorbed into §3 + graveyarded; full graveyard of the dead austere text). **Boot_ScopeReader v2.0→v2.1** (the §3.3 precise-fence-anchor augment-in-pass — the ONE pilot seam: a reader anchored fences at conv-root over long trees; a fence at a conv-root is a slicer failure, invalidate-and-re-run). ANCHOR v20→v21. **S27→S28 FIRST MOVE = ACT, do not re-litigate (this is settled litigation):** (1) **re-run slice 7 under v2.1** to confirm the precise-fence-anchor fix (the v2.0 re-run already proved the bar; this confirms the anchor seam) — if a reader anchors a fence at conv-root again, INVALIDATE and re-run, not tolerable; (2) **stamp + run the other 11 slices** (1–6, 8–11) under v2.1 — mechanical: stage each dir (the all-12 staging command), open a bare CC window per slice, paste the one-liner, paste both streams back; (3) then the **downstream pipeline** (Steps 3/4/5: collation → cluster-validation → judge — boot prompts already staged). The hard architectural thinking is DONE and council-ratified. Do not re-open the axis, the bar, the no-quarantine call, or the slicing mechanism — all settled S27. **A06's ADHD-scheduler session + the Griffin/Boone/Klaus/meds/dance-schedule texture is exactly the high-value auxiliary-brain material the apparatus exists for — it is the SIGNAL, not a liability.**
>
> **⚑ S26 READ-FIRST — THE BOOT KIT IS RATIFIED v1.0; THE THREE UNCERTAINTIES ARE RESOLVED; THE PER-WINDOW PROMPTS EXIST; NEXT IS THE BUILD.** S25 left the kit at v0.2 (drafted, thrice-reviewed) with three flagged-by-its-own-author uncertainties. S26 was a ratification session — design + canon, **no floor mutation, no invariant moved.** Resolved: **(i) judged-pass loadout → portrait WALLED** (`Seeding_Council_Boot_Kit_v1.0` §3/§4a — the judged pass is the ONLY non-blind window, so it's the highest projection-RISK seat with ZERO projection-NEED; chain-assembly is date-order + supersession, the recall check is a checklist match; the one semantic recognition it needs — same-fence-across-different-words like Supabase-over-Nornic — runs off JAKE-STACK + canon, not the portrait). **(ii) JAKE-RULES §11 wall → VERIFIED ON DISK** (read the live §11 at anchor: it IS "Patterns Jake Has Flagged," portrait-shaped — day-shape/self-perception/Griffin-meds material; the S25 call was made from accurate memory; §11 is physically stripped from the council's JAKE-RULES copy, not "present but ignore"). **(iii) keyword-coverage bar → ACCEPTED with downstream-miss measurement** (Jake's call: accept-and-proceed, NOT block — the bar is unfalsifiable at lay-time because the future Claude that would test the keywords doesn't exist yet, so it's measured IN USE: a logged §10-Case-1 ambient-retrieval failure to surface a fence that exists IS a keyword-coverage miss; homed as **entry 1 on the NEW maintenance-pass list** (§9). Rationale: you can't honestly measure future-search-anticipation before the future searches exist; blocking to whiteboard a seed-time number is the molasses failure; matches house style — claim less, prove integrity where it's visible, the FK call again). **COUNCIL ARCHITECTURE DECIDED:** the windows run as **CC against the live floor** — the kit reaches a window by UPLOAD (controlled, exactly its role's set), the floor reaches it by **CC's live query of `apparatus-floor`** (its assigned slice only), and the window boots **BARE — no project instructions, no repo path.** That triad IS the physical enforcement of §13's blindness (kit-in-by-upload, floor-in-by-query, everything else out). **AUTHORED the three deployable per-window prompts** (handed to windows by upload): `Boot_ScopeReader.md`, `Boot_CrossCuttingReader.md`, `Boot_JudgedPass.md`. The kit graduated **v0.2 → v1.0 RATIFIED**; v0.1 + v0.2 stay tombstoned-not-deleted. **Standing call (Jake):** OC + Jake live-spot-check seeding output window-by-window against the convergence map as it comes back — parallel work varies hard no matter how tight the spec; convergence = confidence, divergence = where to look hardest; harvest the variance, don't fight it (the kit's §4 medicine on the kit's own output). **S26→S27 FIRST MOVES:** (1) anchor + confirm v20 banner + Boot Kit reads **v1.1** + Progenitor reads **v3** + floor live (`--dry-run` EXISTS, never `--execute`); (2) **THE BUILD — CC-against-the-live-floor, faster gear, Jake wants pace:** the seeding-slice MANIFEST mechanism (content-neutral is doctrine, the mechanism is build-work; mind the §8 slice-granularity edge — too fine collapses within-slice texture detection into a synthesis-only op) + council/window count + the synthesis + judged-pass MECHANISM + the optional `/jedi-council` output gate + the spot-audit batch cadence; and/or the engine build (catalog on-disk serialization, the keyword resolver, reach defaults, multi-hit presentation); (3) queued post-lock hardening if the build whiteboards — off-site object-locked ndjson copy (the real SPOF-close), then ROTATE the still-unrotated S16-leaked DB password.
>
> **⚑ S26 ADDENDUM — SAME SESSION, THE SEEDING PIPELINE WAS RESTRUCTURED (Progenitor v2→v3, Boot Kit v1.0→v1.1).** After ratifying v1.0, Jake reopened a real structural hole and it held up: **content-neutral count-slicing (required to kill projection) makes a corpus-wide-but-SPARSE texture land ~1× in any single ~2,000-msg slice — so it EVAPORATES between blind slicers.** No slice-reader can see frequency wider than its slice; v2/§13 mis-framed this as a granularity knob. It is structural. **Fix: texture detection LEAVES the slice-reader and becomes a downstream comprehending path.** The worst case carried into doctrine: the same texture surfaces in non-keyword-sharing language ("forgot Griffin" / "Jake pissed he missed *another* pickup") and the most acute instance shares no keyword — so the texture path must COMPREHEND, not COUNT (a keyword tally undercounts the texture at its most acute). **The 5-STEP DUAL-PATH pipeline** (The Progenitor v3 §12, surgical — §0–§11 byte-identical to v2, diff-proven; Boot Kit v1.1): **S1** scope-reader slicers DUAL-OUTPUT — pointers + a LIBERAL context-frequency-potentials stream (flag instances you can't see the frequency of); **S2** mechanical FENCE synthesis (unchanged); ‖ **S3** NEW blind COLLATION — fuzzy-over-bundle the potentials into cluster lists + FORMULATE (don't run) each cluster's negative-space query, never reads the floor; **S4** NEW contextual CLUSTER-VALIDATION — pull the real spans, RUN the baseline queries, validate→texture pointer / split false-friends, may self-merge RECORDED-not-silent, portrait-WALLED, reads only handed clusters; **S5** the JUDGE (unchanged — chains + recall + texture sanity-check). **Two pointer KINDS → two mirrored validation PATHS → one judge.** Projection defused STRUCTURALLY: S3 sees all flags but no meaning, S4 reads meaning but only handed clusters, neither has floor+portrait+open-hunt at once. NEW role prompts: `Boot_Collation.md`, `Boot_ClusterValidation.md` (+ scope-reader bumped to dual-output, + a `slicer/` dir kit `README_slicer_kit.md`). **SLICING LOCKED COUNT-based** (~2,000 msg/slice, ~12 slices, calendar-blind, forest-clean — a cut can't bisect one of the 9 multi-root trees) on the empirical cliff-shaped May distribution from a prior run (heavy-Claude-usage onset; time-window slicing would fry the May reader, count-slicing evens the load). **GATE-1 CC RECON is out** (read-only): confirm the queryable cut-on designator (ordinal vs created_at-window) + created_at distribution + conv-size + forest layout, BEFORE writing any x→y slice bound. **A PILOT SLICE runs first** through the full 5-step path, recalibrates slice size + Step-3 liberality from real data before all N are stamped. Two seams flagged-not-buried: Step-4 may need its baseline-query widened; Step-3's fuzzy-bundle ceiling (liberal enough to catch non-keyword-sharing phrasings without doing S4's comprehension) is pilot-tunable.
>
> **⚑ S25 READ-FIRST — THE RETRIEVAL ENGINE'S CONCEPTUAL DESIGN IS COMPLETE; THE PROGENITOR IS v2; THE SEEDING BOOT KIT IS DRAFTED; NEXT IS BUILD, NOT WHITEBOARD.** The substrate arc is DONE (S23 laid the floor: 325 headers + 24,138 messages, append-only ENFORCED — see the v17 footer + the FLOOR LAID block; the S23 hollow-probe scar is recorded on purpose; the FK is dropped per the FK-RESOLVED block; main was made honest at S24). S24 authored **The Progenitor v1** (the pointer-catalog law: two kinds — fence-as-lineage + texture — one append/supersede spine). **S25 designed the retrieval ENGINE over that catalog and folded the entire conceptual design into The Progenitor v2** (`active/apparatus/The_Progenitor_v2_2026-06-01.md` — §0–§9 byte-unchanged from v1, diff-verified; §10–§13 added; §9 amended). The four folded sections: **§10 trigger model** — ONE engine, three throttles (Claude-initiated ambient / re-anchor-driven / Jake-fired); the catalog rides in at BOOT via the ignition/Anchor, the three triggers query it LIVE (boot is the substrate, not a fourth trigger; a standalone boot-inject separate from re-anchor SOP is a parked edge case). **§11 span-return shape** — a hit returns Layer A (raw span text across the tree-aware reach, verbatim/scrub-redacted, never summarized) + Layer B (the §4 floor-key locator, carried alongside, never blended into substance) + Layer C (kind-context: FENCE → the FULL why-chain, non-negotiable, because evolution-vs-relitigation is only visible in the chain; TEXTURE → representative span + count + spread, the volume IS the substance); "hands over the card and the page it points to, never a book report." **§12 seeding process** — the original catalog read of the laid floor, by a council of BLIND parallel windows divided by project-scope PLUS a cross-cutting no-project reader (division in the LABOR, never the INDEX); per-batch spot-audit by Jake on the SYNTHESIZED catalog is the ONE scoped relaxation of human-as-immutability-mechanism. **§13 council boot kit** — sized for RECOGNITION-not-PROJECTION (the risk is asymmetric toward overload — projection looks like it held the bar; underload shows as catchable gaps — so when in doubt, LIGHTER); the load-bearing call: texture-readers do NOT boot with the Wallaby Why / Lore Bible (those are tone-calibration-for-talking-to-Jake, not what-counts-as-texture-in-the-corpus; booting them makes a reader confirm the portrait instead of grounding it — JAKE-RULES §1.2 turned on the council itself); chat_search + catalog-so-far + sibling-window-proposals are HARD WALLS (windows read BLIND; convergence across blind reads is the confidence SIGNAL, and only a post-hoc SYNTHESIS/merge-back pass can assemble cross-slice counts — dedup-at-read literally cannot). **Two S25-original calls beyond the S24 specs:** (1) KEYWORD-FIRST retrieval — vectors are a DEFERRED v2 sidecar (pgvector, never on the floor, per D9), added ONLY if the seeding pass proves a measured semantic gap; the fast path wants PRECISION, corpus-search already owns RECALL, vectors-in-v1 would blur the two and pull the §1 forty-half-relevant-spans failure (§9). (2) KEYWORD-COVERAGE is a seeding-quality bar — keywords are laid for future-SEARCH-anticipation, not just topical accuracy; brittleness here is exactly what would force vectors later (§8/§12). **The Seeding Council Boot Kit v0.1** (`active/apparatus/Seeding_Council_Boot_Kit_v0.1_2026-06-01.md`) operationalizes §12+§13 into the kit a council window boots with — subordinate to the doctrine (doctrine wins on conflict), references-not-restates it. It went through an S24-Claude cross-check that RATIFIED the bones and returned 6 non-structural changes for v0.2 (bless Q1 role-merge + add an independent-passes guard; upgrade Q2 synthesis-gets-no-portrait from fork to WALL; add a canonical NO-card worked example; add a content-neutral-slicing constraint as doctrine while deferring the mechanism; universalize the projection wall across all reading roles; give Jake a convergence-map audit appendix in the review model). **S25→S26 FIRST MOVES:** (1) anchor + confirm v19 banner + The Progenitor reads v2 + floor still live (`--dry-run` reports tables EXIST, never `--execute`); (2) the BUILD — boot-kit v0.2 (apply the 6 S24 changes), then the seeding-build session (how slices are drawn — content-neutral per the S24 constraint, council size, the synthesis-pass mechanism, the optional `/jedi-council` output gate) and/or the engine build (catalog serialization, the keyword resolver, reach defaults, multi-hit presentation) — all CC-against-the-live-floor, NOT whiteboard; (3) queued post-lock hardening if build whiteboards rather than ships — off-site immutable ndjson copy (the SPOF, highest — S25 scoped it: the Anthropic export is a forward-only re-pull backstop with a since-deleted-conv blind spot, so the off-site WORM copy closes exactly that gap; NAS weekly is cheap redundancy, object-locked cloud is the real SPOF-close, GitHub/Drive/second-Supabase all ruled out as too-big / not-write-once / same-trust-domain), then ROTATE the still-unrotated S16-leaked DB password.

> **DIRECTIONAL NOTE (read first).** **S20 CLOSED PASS-TWO (b): the scrub-vN OVERLAY CAPABILITY is built, proven, reviewed, and LANDED.** Two rungs, every step gated against live disk. **Rung 5 — minted the FIRST scrub-v2 overlay TINY** as a SYNTHETIC ratify-drill (Jake's turn-one intent call: no known scrub-v1 miss, so exercise the overlay PATH end-to-end on zero-stakes data before any real re-scrub is ever needed). Synthetic rule: `\bEXAMPLE\b` (case-sensitive) → `[SCRUB-V2-DRILL]` — trivial, benign, strict superset of v1. N=10 slice off baseline `scrub-v1/records.ndjson`. Three contract ambiguities ruled: **omit `conversations.scrubbed.json`** from `scrub-v2/` (it's a stage3 raw-ingest leftover, raw is wiped, the contract names exactly 3 companion artifacts — Jake blessed the omit, the one place the as-built v1 folder (4 files) disagreed with the ratified contract (3 files)); **six keys** in verify.log (5 v1 cred classes + EXAMPLE); **skip scrub_walk on conversation_header** (pure metadata). All 4 gates PASS, zero-hit (expected on 10 records), Gate-4 sha `4ef22940…` UNCHANGED. Jake ratified. **Rung 6 — scaled to the full baseline (N=23,095).** Same proven path, only N changed. The one legitimate write-inside-an-existing-overlay-dir (replace the 10-record drill artifacts with the full restatement) used **rm-then-rewrite** (unlink makes the drill→ratified turnover unambiguous; a sealed-then-mutated file is conceptually muddy on an append-only floor), **guarded** by a pre-delete assert (folder = exactly the 3 expected files AND records.ndjson = exactly 10 lines, before any unlink — positively identify the drill, never blind-delete). All 4 gates PASS, Gate-4 sha `4ef22940…` UNCHANGED both ends (rung 6 proven at full scale), output records.ndjson 367,494,524 B (sha `b54620af…`), sealed read-only. **EXAMPLE fired 3× at scale** (all conv `3ef82921…`, 2 of 3 in `display_content.json_block` — the scrubber DOES reach display_content even though the v1.1 drift-detector deliberately does NOT walk it; two passes, two depths, both correct — hold this at LOAD when the selective-strip toggle is designed). verify.log EXAMPLE:0 is CORRECT (verify scans the *output* — the tokens are already replaced; audit counts input hits, verify counts survivors — they're supposed to disagree when a rule fires). **WANT check PASS:** `_build_seen_set` on the live floor resolves **baseline → scrub-v2 (max-N), delta → scrub-v1** — the v1.5 seam confirmed on the REAL overlay, not just the S19 synthetic fixture. **Rung 7 — the code-review gate.** `code-review:code-review` reviews a GitHub PR (not a working tree), so the gate ran on a scratch-branch draft PR (#1): **"No issues found"** — 9 findings, all below the 80 threshold or false-positive. Committed `84374cd`, landed to main via `merge --no-ff`, scratch branch deleted. **The scrub-v2 overlay is SYNTHETIC (a ratify-drill), NOT a production re-scrub — it does not change production redaction policy; a real improved-scrubber pass catching a genuine v1-miss is a future scrub-v3 event.** The append-only floor + the per-rung sha gate are what made two CC mis-report "scares" non-events. **The next chapter after S20 was seed-shape LOAD — which S22 then built and all-but-completed (see the S22 READ-FIRST + FK block).**
>
> **DIRECTIONAL NOTE (read first).** **S18 BUILT, PROVED, AND SEALED PASS-TWO (a): v1.1 FIELD-LEVEL KEY-PRESENCE DRIFT DETECTION.** v1.1 closes the v1.0 type-only gap: per-object-type key allowlists (conv 7 / message 9 / 5 block types / 5 content-item types), authored verbatim from S14's population-scale characterization, with exactly ONE optional carve-out — `text.citations_grouping_mode` (~0.14%). Depth is top-level keys per object type only; `display_content`'s nested guts deliberately NOT walked. Severity is warn-not-stop. PROVEN three ways: clean gate (0 field_drift across the full 5-28 export), negative test (renamed/added/removed key + a `uuid` removed from a non-index-0 record all CAUGHT, 4/4), carve-out (both directions clean). Pipeline **v1.3 → v1.4**, committed `b5be049`.
>
> **DIRECTIONAL NOTE (read first).** **S17 BUILT, PROVED, AND SEALED THE DELTA.** The v1.3 pipeline (delta via uuid-set-difference + raw.json wipe-after-verify-PASS) was authored against `Freeze_Pipeline_Spec_v4`, dry-run-proven, then really run + sealed — `delta-2026-05-28-a61498e6`, all 7 post-seal checks PASS. The floor now holds **two snapshots: baseline (23,095 records) + delta (1,368 records = 31 headers + 1,337 messages)**, append-only, both raw originals wiped. The no-duplicate-header rule is PROVEN ON THE FLOOR: the sealed delta is 1,368 lines, NOT 1,371 — the 3 empty-baseline convs that gained their first messages attached without a new header. Committed `43306fa`. **(This invariant is the exact thing the S22 FK finding turned on — see the FK RESOLVED block.)**
>
> **DIRECTIONAL NOTE (S16).** **S16 LOCKED D9 — the substrate is Supabase, and the floor's append-only immortality is ENFORCED at the database.** The seed-shape gate ran against the real baseline `records.ndjson` (23,095 records) on a dedicated Supabase project (`apparatus-floor`, SB Pro, separate from Cypher) and PASSED 4/4. OC blind-re-read + a `/jedi-council` adversarial panel (LOCK-WITH-CAVEATS 6/10, 0 P0, 1 P1) → all six remediated with proof → Jake ratified. Append-only ENFORCED via `floor_immutable_guard()` triggers + REVOKE from app roles, proven by a live TRUNCATE/DELETE/UPDATE REJECTED. **Key reframe: the `records.ndjson` is the canonical immortal artifact; Postgres is a rebuildable derived index** (rebuild-from-ndjson 0-mismatch / 6 directions). Evidence: `harness/D9_LOCK_2026-05-29.md`. **D9 CLOSED.** **(S22 NOTE: the S16 harness's two-table schema — proven 4/4 for a SINGLE-snapshot ingest — carried an untested FK assumption that the first MULTI-snapshot LOAD exposed. The schema shape is sound; the cross-table FK is being dropped per the FK RESOLVED block. The append-only enforcement, the ndjson-canonical reframe, and the value-fidelity claims all STAND.)**
>
> **DIRECTIONAL NOTE (history).** Between v3 and v4 the project's input mechanism changed: extension-based capture of claude.ai's internal endpoints was specified, partly proven, then **refused on principle** and is permanently out of scope. **The input is now Anthropic's official conversation export, full stop.** See the **REFUSED — DO NOT RE-OPEN** wall in the GRAVEYARD.
>
> *(Earlier between-version history — v4→v15 — preserved in the enshrine footers below. Each footer is the verbatim record of what that session changed; they are not reworded.)*

## WHERE THE CODE LIVED
> **⚠ FROZEN AT S42 — pre-reorg paths, pre-S43 build status. NOT the current map.** This block describes how code locations evolved through S42, when everything lived under `pipeline/` and the batch harness was still a build target. The S48 reorg moved all of it (`pipeline/` is gone; the project lives under `wallaby-way/`), and S43–S47 built what this block calls "not yet built." **For current code locations, read `wallaby-way/CLAUDE.md`.** Kept here verbatim as the record of the path-correction history (the S40/S41/S42 inline notes are true-record of how the paths got fixed) — do NOT follow these paths as live.
*(S21 design call, applied S22 once the relocation merged — single source of truth for code location, so a future move is one edit here, not a hunt through the ignition key. This stable block is not reworded by the churning banner.)*
- **freeze pipeline** → `pipeline/apparatus_freeze_pipeline.py` (v1.6)
- **overlay drill** → `pipeline/apparatus_overlay_v2_drill.py` (v0.3)
- **production LOAD** → `pipeline/seed_shape_load.py` (**v1.2** — floor LAID S23, real-row `ctid` append-only probe; on main since the S24 merge)
- **the floor** → `apparatus-archive/snapshots/` (**OUTSIDE the git tree** — `apparatus-archive/` is wholesale-gitignored)
- **local-only secrets** → `pipeline/secrets/.env` (gitignored via `pipeline/secrets/` rule; holds `SUPABASE_DB_URL` for the apparatus-floor project) · ⚠ holds the `ANTHROPIC_API_KEY` too — loading it FORCES metered billing; on-sub reads MUST NOT load it (the ~$50 trap) *[S42 DISK-CONFIRMED — env-path correction: `pipeline/secrets/.env` is NOT the current path — the secrets file in `pipeline/secrets/` is `floor_db.env` (holds `SUPABASE_DB_URL` only; no API key). The `ANTHROPIC_API_KEY` lives at ROOT `anthropic_billing.env`. The billing-trap caution stands — guard against loading `anthropic_billing.env`, not the old `.env` path.]*
- **S16 seed-shape harness** → `apparatus-archive/harness/s16_seed_shape_harness.py` (the gate test rig — DROPs+recreates tables; NOT a loader; reference for the proven insert shapes only)
- **the shape reader (deployable)** → **v4.0:** `pipeline/test_call_system_prompt_S32.md` (the `Boot_ScopeReader_v4.0` system prompt — TOMBSTONED-NOT-DELETED). **v4.1 (LANDED S42, reader v4.1.1):** `pipeline/test_call_system_prompt_S40.md` (= S32 verbatim + the precision invariants IN THE INSTRUCTION BODY [S42 recut moved them out of the example — no `---` echo surface]; carries the every-mention-UUID patch; the batch fires THIS, not v4.0 — must match `Boot_ScopeReader_v4.1`, HAND-SYNCED, patch both together) · **the API harness** → `pipeline/apparatus_api_testcall.py` (one conv per call; loads the key — paid path only) · **★ THE PAID-API KEY LIVES AT ROOT `anthropic_billing.env`** (S40/S41 — loaded for batch submit/collect ONLY, cleared after; the batch-not-synchronous + clear-after discipline governs it) — ⚠ NOT in `pipeline/secrets/.env` (that path is the REFUSED wall, key NOT there); the $0 floor reads use `floor_db.env` (Supabase Postgres). *[S41 NOTE — change-set D: this env-path corrects the older "key in pipeline/secrets/.env" text; JAKE MUST CONFIRM the key's actual file location against disk before relying on this — OC cannot see which file holds it; the handoff/ignition assert root, the disk wins.]* · **the proven extractor** → `pipeline/extract_whale.py` (**v2.0 / S38** — `===MSG===` skeleton serializer w/ real CREATED timestamp, reused for all extracts; **NOW chunk-ready: optional `--conv-uuid`/`--snapshot-id`/`--created` stamp the TRUE conv identity into a mid-tree window header before the read (flag-2 fix); writes a `<out>.parents.json` `{msg_uuid: parent}` sidecar — the source map for the persist-time one-hop parent-edge injection; render_block untouched, whole-conv fallback unchanged**)
- **★ THE PIPELINE GUARDS (NEW S38)** → `pipeline/pipeline_guards.py` (**v0.2 / S38** — a LIBRARY the loop calls, not a runner; no Agent/API/`.env`): `assert_env_unloaded()` (§7b billing guard — raises if `ANTHROPIC_API_KEY` ∈ env), `whale_gate()` + `load_whale_registry()` (detect-and-alert; KNOWN_WHALE/FITS/raise; route the 4 from the registry), `tally_nodes()` (salience-line count, flag-1; not a keyword grep), `persist_node_file()` (flag-3 — temp-then-rename ATOMIC: validate on temp [stub guard + the anchor-∈-parents_map flag-2 integrity tripwire], `os.replace` on success, unlink+re-raise on any failure so no stub survives a HALT; writes the Option-B `.parents.json` edge sidecar only after the rename). Unit-proven by `pipeline/test_guards_S38.py` (13 tests green). Locked on main `bad80b5`.
- **the proven shape-pass scaffolding (S33, disposable/one-time)** → `pipeline/apparatus_strip_v1.py` (the hardened echo-stripper, one-time tool not a pipeline organ), `pipeline/chunk_whale.py` (tree-aware chunker, `--bytes-per-token`), `pipeline/_verify_strip.py` (8-point skeleton verifier)
- **★ THE S39 ON-SUB FREE-ARM LOOP (S39 "Courier")** → `pipeline/s39/` — `onsub_loop.js` (the proven free-arm reader loop, key UNLOADED, $0) + `floor_extract.py` (the `===MSG===` serializer the batch ALSO uses for char-length/payload) + `persist_guard.py` + `worklist.csv` (the full 321-conv candidate list: 219 FITS_WHOLE / 97 CHUNK_LATER / 5 REVIEW) + `build_worklist.py`. The free arm collapsed to the 14 already-banked convs here (S41); the loop is proven + intact on main.
- **★ THE BATCH READ SPEC + READER (S40/S41/S42)** → `apparatus/Batch_Read_Spec_v1.1_2026-06-06.md` (**v1.1 LANDED S42** — the 206-conv paid-batch build spec; prepay gate DOWNGRADED, batch-not-synchronous guard reinforced as load-bearing, env-path + count reconciled; §7 billing, §10 do-not list) · `apparatus/Batch_Read_Spec_v1_2026-06-06.md` (**v1 TOMBSTONE** — do not build from it) · `apparatus/Boot_ScopeReader_v4.1_2026-06-06.md` (**v4.1.1 reference — LANDED S42**, recut placement + every-mention-UUID patch) · the v4.1 deployable (above) · `pipeline/s41_cost_trueup.md` + `pipeline/s41_node_inventory.md` + `pipeline/s41_nodes_audit.md` (S41 reports: cost band, node-store inventory, the 24-orphan forensic audit).
- **★ THE OUTPUT SCRUB (NEW S42)** → `pipeline/scrub_output.py` (the §8 OUTPUT-surface scrub — imports the locked S12 PATTERNS single-source + 3 appended Supabase patterns [`sb_secret_`/`sb_publishable_`/project-ref URL, `{20,}` floor]; `scrub_text(s)->str` + `scrub_file(path)->str`, returns text/does-not-write/caller-owns-persist; UUID-survival proven by construction). Unit-proven by `pipeline/test_scrub_output.py` (15/15; relocated S43 from `pipeline/s39/` to sit beside the module). **BUILT-NOT-YET-WIRED** — the S43 harness wires `scrub_text()` BEFORE every `persist_node_file()` + over CC's report output (the §8 two-surface wiring).
- **★ THE HONEST GRADE PROBE (NEW S42)** → `pipeline/s39/grade_recut_S42.py` (loads the REAL deployable `test_call_system_prompt_S40.md` from disk, no stitch, fires Sonnet 4.6) + `pipeline/s39/_grade_result_recut_S42.txt` (the result: clean end_turn, $0.37, 6 nodes/0 drops, QUALITY good by hand-read, 5/5 substantive UUIDs). **★ TOMBSTONED FALSE-GREEN PROBES (do NOT use to grade):** `pipeline/s39/_grade_probe_s40.py` (fired v4.0 `test_call_system_prompt_S32.md`, NO invariants) + `pipeline/s39/_grade_probe_precision.py` (fired S32 + a runtime-string invariant append) — NEITHER ever fired the real deployable; kept as evidence of the false-green caught S42.
- **★ THE BATCH HARNESS (S43 BUILD TARGET — not yet built)** → `pipeline/apparatus_batch_read.py` (per `Batch_Read_Spec_v1.1`: floor_extract → skeleton-gate → batch-request assembly [v4.1 cached system prompt + `custom_id=conv_uuid` + NO tools] → Message Batches API submit WITH the batch-not-synchronous guard → poll/collect → `scrub_text()` before persist → `persist_node_file()` to the flat pile; resumability = COMPLETE ARTIFACT not file-exists). Model `claude-sonnet-4-6` from ONE source of truth. The `pipeline/s39/floor_extract.py` serializer + `pipeline/s39/persist_guard.py` are the proven pieces it wires.
- **★ COMPLETED-WORK COLD-STORE** → `harvested_nodes/` (**root-level, STOOD UP + POPULATED S37** — the pile of locked, finished node output, separate from disposable scratch). Holds (move #2, closed S37): the 130 whale nodes (S33's 5 `result_*.md`), the 2 S32 gate-conv reads (`d6e23963`, `492e164b`, source=S32-gate), the 48b4110a S36 confirmation catalog (7 chunks + whole), the ea900330 S36 confirmation catalog (chunks 01/03/+recovered 02/04/05 + whole), and `harvested_nodes/MANIFEST.md` (conv_uuid → node_file → status → source → parsed-count → note). **21 done rows, 0 stubs.** Going forward, every conv's chunked-read node output lands here as the drip completes. This is the "lock 'em cold" store — once a conv's nodes land here, it's DONE and not re-read. Cold-store holds COPIES; raw sources/originals stay in `pipeline/` (incl. the pre-locator-fix ea900330 originals at `pipeline/probe_bigconv2/`, retained as provenance). (Distinct from `pipeline/probe*/` and `pipeline/probe_bigconv*/` = disposable scratch, sweep after the build.)
  - **S37 NOTE — the conv_uuid header-stamp finding lives here:** the recovered ea900330 chunks proved the production extractor MUST stamp real conv_uuid + snapshot_id into every window header or chunked convs mis-key (banner v28 flag 2). The cold-store copies carry the fix + a provenance block; the build inherits the spec.

## DESTINATION
Build the apparatus's own memory system (*The Wallaby Way*): a closed, self-administered re-grounding loop that holds continuity for a stateless Claude across sessions — so Jake stops being the memory bus *and* the blind follower. It is **beta-Cypher-brain**: the same anchored/plastic strata Cypher will run on, hand-cranked now, hardening into Cypher's memory layer. Scope is **Jake's whole working surface**. A chat-Claude works small (anchor + rules), with a cold, complete, immutable **snapshot** behind it — built from official exports — that it reaches *by pointer* only when a *why* is disputed.

## WORKING STYLE (not invariants — how Jake works with the orchestrator)
*These are conventions Jake finds useful, not identity claims for the instance.*
- **Status line at the end of each reply** — backticked one-liner: turn N · ET-time · re-anchor X/4 · dest…; state…; next…. Turn counter + re-anchor cadence (~every 5 turns, 4/4 = seam-hunt/wrap warning not a guillotine) is how Jake holds thread-position across long sessions. **Timestamps: use bash `date`, never confabulate** — multi-hour gaps between turns happen.
- **Alignment check on request** — Jake may ask for a playback of project state. A check we're pointed the same way, not a loyalty test. If the playback surfaces a disagreement, say so.
- **Push back with evidence** — Jake relies on the orchestrator to catch his mistakes, not agree. Terse, peer-to-peer, no apology theater.
- **NEVER ask Jake for implementation/technical state** [S22] — Jake is a non-coder founder (JAKE-RULES §1.1); floor row-counts, file structure, what's-built-vs-spec'd, branch state are NOT his to recall from memory. Those questions go to CC against disk or to OC reading the files. Asking Jake "is X resident / what does the pipeline do at line Y" is aiming the question at the wrong brain — the exact memory-bus trap the apparatus exists to kill. Bring Jake the *intent-level fork* in destination-why terms; OC + CC own the mechanism. (Earned S22: OC drifted into asking Jake which git branch a commit landed on — a question Jake had told every Claude he can't answer.)
- **Prose, not the widget** — answer in prose; never the ask_user_input tool.

## INVARIANTS — WITH WHY

# header↔message foreign key — RESOLVED S22 (option (c): drop the cross-table FK, enforce no-orphans at the gate. Ratified by Jake 2026-05-31.)
**THE FINDING.** The S16 schema gave `floor_conv_messages` a composite foreign key `(snapshot_id, conv_uuid)` → `floor_conv_headers(snapshot_id, conv_uuid)`. This was proven 4/4 at S16 — but ONLY against a single-snapshot (baseline) ingest. The first real MULTI-snapshot LOAD (S22) hit a `ForeignKeyViolation` immediately: a delta message for conv `5d0eba7c…` carries `snapshot_id=delta-2026-05-28-…`, but that conv's header was written at BASELINE (`snapshot_id=baseline-2026-05-25-…`) and — per the **no-duplicate-header invariant** — was correctly NOT rewritten in the delta. The composite FK demands message and header share a `snapshot_id`; the no-duplicate-header rule GUARANTEES they will not, for every appended conversation. **The FK encoded an assumption the floor's own ratified invariant makes false.** It was never wrong-on-purpose; it was an untested S16 default the delta exposed the moment both snapshots loaded together.
**THE DECISION — (c): drop the cross-table FK; enforce referential integrity at the load + gate, not in the schema.** Three options were on the table. (a) Re-key the FK to `conv_uuid` alone (would require restructuring the header PK so conv_uuid is globally unique — true under the no-dup-header rule, but it double-encodes that invariant into the schema structure and welds an assumption into immutable bedrock). (b) Write a synthetic duplicate header for appended convs into the delta — **REJECTED, hard: it directly violates the ratified no-duplicate-header invariant** (proven on the floor at S17: the delta is 1,368 lines not 1,371). (c) Drop the cross-table FK entirely; the loader proves "every message's conv_uuid has a header somewhere on the floor" before it commits, and the verification gate re-proves it.
**THE PRINCIPLE (why (c), not just which (c)).** This is the SAME stance the floor already ratified for the message tree at D9: `parent_message_uuid` deliberately has **NO self-referential FK**, with the reasoning written into the lock — *tree integrity is "a PROVEN property of the gate, not a schema-enforced one that could mask floor inconsistency."* A schema constraint enforces a relationship but can also MASK incoherence — it fails one insert without telling you the whole floor is consistent. The floor's philosophy is: prove integrity at the gate, where the whole picture is visible, not at the row level. (c) makes the header↔message link obey the same stance the parent↔child link already does. (a) would split the project's own philosophy — one structural relationship schema-enforced, the other gate-proven, for no principled reason except an S16 accident. **(c) is also the humbler schema**: it claims less, bakes no invariant into the PK structure, and leaves room if we ever learn something new about how snapshots relate — a virtue on a thing that must be immutable. The cost (the DB no longer catches an orphaned message) does NOT vanish; it MOVES to the gate, where the floor keeps all its other integrity proofs.
**THE FIX (S23, loader v1.0→v1.1):** in `seed_shape_load.py`, remove the `CONSTRAINT fk_header FOREIGN KEY …` clause from the `floor_conv_messages` DDL; add a pre-commit orphan-check inside the single transaction — build the set of header `conv_uuid`s, assert every message `conv_uuid` is in it, refuse+rollback with a clear error if any message has no header on the floor. Everything else in the loader is unchanged and proved out at S22 (overlay resolution, counts, single-transaction structure, post-load hardening, owner append-only re-prove). The FK is the only line that moves.
**DO-NOT-RELITIGATE:** (b) is dead (violates a ratified invariant). The choice between (a) and (c) was made on principle (coherence with the D9 parent_uuid precedent + humbler-schema), not on which made the error disappear — all three would. (c) is ratified. Re-opening it should require a NEW reason, not a fresh-eyes re-derivation of the same three options.

# THE PROGENITOR — the law of the pointer catalog (v1 AUTHORED S24 · v2 FOLDED S25 · v3 REWROTE §12/§13 S26 · v4 INVERTED THE ADMISSION BAR S27)
**File:** `active/apparatus/The_Progenitor_v5_2026-06-02.md` — the governing doctrine for the retrieval layer's pointer catalog. **v5 supersedes v4** (v4/v3/v2/v1 stay on disk, tombstoned-not-deleted, per the spine — v5 rewrote §12/§13 to two-pass; §0–§11 byte-identical to v4 except a §3.4 over-ceiling pointer): §1, §2, §4 record-shapes, §5-mechanism, §6, §7, §10, §11 are BYTE-IDENTICAL to v3 (the engine, the two kinds, the dumb-index/smart-reader line, the triggers, the span-return — did not move); v4 INVERTED §0 (one-line law: card-catalog-of-the-load-bearing-few → comprehensive semantic GRAPH, default NODE), ADDED §0.5 (organic-over-austere as a standing INTOLERABLE failure mode — read before §3), REWROTE §3 (default-NODE admission, behavioral keep/drop signals, fence/texture DEMOTED to salience-tags, MOTION as a light node kind, §3.3 precise-fence-anchor, §3.4 volume inverted to many-thousands, no-quarantine-on-personal-material), and AMENDED §5/§8/§12-guard (corpus-search is the rare-miss net not the selectivity license; the bar restatements inverted). **E1 (the admission standard) was absorbed into §3 and graveyarded standalone.** WHAT FORCED IT: the slice-7 pilot under v3's austere bar cataloged 1 node / 21 convs and LOST fences buried in dropped conversations; the council ratified PRESENCE-over-SERVICE double-blind; the re-run under the inverted bar cataloged 41 and caught the lost fences. Sibling to the Track Meet Doctrine (how-we-record) and The Wallaby Why (why-the-beast-exists) — read those as FRAMEWORK (the austere reflex is the bug; the breadth is the function), not reference. The applied layer is the **Seeding Council Boot Kit v1.2** + deployable per-window prompts (`Boot_ScopeReader` **v4.1.1** [SHAPE pass — the current reader that fired the batch; the older v2.2/v2.3 shape-pass prompts are tombstoned-not-deleted], `Boot_VolumeReader.md` [TEXTURE pass, NEW], `Boot_ClusterValidation.md`, `Boot_JudgedPass.md`; `Boot_Collation.md` + `Boot_CrossCuttingReader.md` retired-tombstoned) + the `slicer/` dir kit + `Shape_Slice_Over_Ceiling_Rule_v1`. *[S49: reader ref was "Boot_ScopeReader.md v2.2" — corrected to v4.1.1, the current deployable.]*
**WHAT A POINTER IS.** A card in a card catalog: a keyword pointing at a span of the immutable floor, laid by a Claude who read the span and judged it worth pointing at. Dict-based, many-to-many (many entries per keyword, many keywords per entry). A **mutable sidecar** that references the floor by key — same architectural slot as pgvector, NEVER a column on the immutable tables (D9).
**TWO KINDS, ONE SPINE.** (1) **FENCE** — points at a decision/constraint where a Claude hitting it cold would *change course*. A fence is NOT a verdict; it is a **LINEAGE** — an ordered chain of (span, date, call, why-as-live-predicate) links, latest current, earlier superseded-not-erased. Claude defends the latest link's *reasoning*, never its *answer* — and reads the chain to tell **evolution** (good — a new why, help add the next link) from **re-litigation** (bad — the same why that already lost, show the fence). The why is a *live predicate* where checkable (1Pass: "still beta? still no invite? RE-CHECK" — a fence that knows why it's a fence can dissolve itself when the world changes). (2) **TEXTURE** — points at a pattern whose *volume is the signal* (representative span + count + spread); calibrates how-to-be-with-Jake, not what-to-decide. Test: does the volume itself tell Claude how to show up? ("fuck" = incidental, no card; Griffin's-pickup = the count IS the meaning, card). Overlap allowed (Griffin is both). **The spine under both — and the floor, and the Claude-lineage — is: append, keep lineage, supersede don't delete.** One architecture, three scales.
**THE BAR (INVERTED v4, carried to v5 — was "default DON'T point," now DEFAULT IS NODE).** The catalog is a comprehensive semantic graph; most conversations produce nodes. The ONLY way out is the DROP gate: genuinely ephemeral — zero project continuity, zero recurring element, zero decision, nothing made (a made-in-error fragment like a stray "v"). Unsure → NODE. Behavioral keep-signals (any one admits): multi-turn (strongest), a fork, named continuity, a decision, a thing made. Fence/texture are SALIENCE TAGS now, not admission gates; MOTION is a third node kind (light, low-salience, reachable — NOT a discard; "motion" is a descriptive tag, never a kill-word). Volume: many-thousands across ~24,138 messages is CORRECT; only HUNDREDS = the austere reflex re-asserted (the slippage signal, inverted from v3). A near-empty drop log is the healthy shape (pilot slice 7: 21 read → 41 nodes → 0 drops). The asymmetry that licenses it: over-include is cheap (a future Claude reads a little extra), omit is expensive-and-invisible (unreachable in-session, a felt daily-use gap, no shape to audit). NO quarantine on personal/medical/family material — it's texture, cataloged like everything else; the only exclusion is mistakenly-pasted creds, walled by scrub-vN at INGESTION. Corpus-search is the rare-miss net, NOT the selectivity license (the v3 license is revoked). Fences require PRECISE anchors (§3.3) — a fence at a conv-root is a slicer failure.
**TWO RETRIEVAL MODES.** Pointer-hit = fast, curated, in-session (the card catalog). Corpus-search = slow, exhaustive, separate-window, send-it-and-nap (the stacks themselves) — NOT a mid-session move, like `/jedi-council`. Corpus-search and seeding are the SAME operation at different scales; the slow path's *output is new pointers*. The catalog grows by use.
**DUMB INDEX / SMART READER (the line the whole thing lives on).** The index points; it does not think. It does NOT pre-digest (no summarize/abstract/merge — that corrupts felt-rightness). A selector MAY rank, threshold, cap; MAY NOT rewrite. The catalog points at *settled* judgments (RElitigation-prevention — dumb-faithful) and never predicts *future* relevance (PRElitigation — the index overstepping). Intelligence lives in the reading Claude, never the retriever.
**WHO LAYS THEM.** The reference-file workflow: a Claude proposes, **Jake verifies, commits, pushes** (Claude never claims to have saved). The human is the immutability mechanism. Append-biased, human-gated, fully auditable (history file + changelog; superseded pointers leave a tombstone, never a silent vanish — one Claude's judgment is NEVER enough to kill a reference forever). The original seeding pass is council-laid (too large for per-card review) with Jake spot-auditing the *output shape*; incremental session-pointers get per-card eyes.
**SEEDING GUARDRAILS.** Source is the FLOOR, never chat_search (the prior attempt's failure — chat_search is live + ungrounded; the floor is the immutable ground truth). Not a one-day build. Tree-aware spans only (fanout is a tree-walk up the parent chain / down the branch, NEVER a flat-N timestamp slice that rakes in sibling branches — a contaminated span corrupts felt-rightness). Lay fences as chains even at length-1.
**NAMING:** called "The Progenitor," deliberately NOT "Seed [anything]" — "seed" is reserved in this project for the FLOOR's record-shape (`seed_shape_load.py`, `Seed_Shape_Test_Spec`). The Progenitor is a retrieval-layer thing riding OVER the floor; "seeding" appears only for the act of populating the catalog.
**OUT OF SCOPE — NARROWED v2 (the conceptual design is now IN the doctrine; only build-work remains):** v1 listed the query interface as out of scope; v2 brings it IN (§10 when it fires, §11 what it returns, §12 how it's seeded, §13 what a window boots with). What STAYS out: the exact on-disk serialization (catalog + returned span), the ranking-algorithm internals inside the §6 selector envelope, the `reach` default tuning, multi-hit presentation, and the slice-manifest mechanism — all CC-against-the-live-floor. Plus the EMBEDDING DECISION (§9): v1 retrieval is KEYWORD-FIRST; vector/semantic-reach is a deferred v2 sidecar (pgvector, never on the floor, per D9), gated on the seeding pass proving a measured semantic gap. Everything CONCEPTUAL — trigger, return-shape, seeding process, boot kit — is now specified in The Progenitor v2 §10–§13.


**Decision: the substrate is Supabase.** A dedicated project `apparatus-floor` (SB Pro tier, separate from Cypher's project) is the permanent home. NornicDB remains the **proven upgrade path** (adoptable later by rebuilding from the canonical ndjson, no storage-integrity re-open).
**The floor is the `records.ndjson`; Postgres is a rebuildable derived index.** The canonical immortal artifact is the ndjson on disk — a deterministic, value-preserving projection lives in Postgres. PROVEN: rebuild-from-ndjson reproduced both tables with **0 value-mismatch across 6 directions**. This dissolves the "one-way door." The thing that must never be lost is the ndjson (see post-lock SPOF flag).
**ndjson canonical fingerprint (recorded at lock):** baseline scrub-v1 `records.ndjson` SHA-256 `4ef22940e3fbb849c2c14fba62fdae2a44277963f0ea5c9f7f2086c706415ba3`, 367,494,497 B. (Baseline scrub-v2 SHA-256 `b54620af…`, 367,494,524 B — the max-N overlay LOAD ingests for baseline.)
**Schema of record (two tables):** `floor_conv_headers` (PK `(snapshot_id, conv_uuid)`) + `floor_conv_messages` (PK `(snapshot_id, conv_uuid, msg_uuid)`). Timestamps stored as **`text`** (verbatim string preservation). `content_blocks`/`attachments`/`files` as **`jsonb`**. `parent_message_uuid` is a bare `uuid` with **NO self-FK** (deliberate — tree integrity is a proven gate property). **[S22 AMENDMENT: the cross-table header↔message FK `fk_header` is being DROPPED per the FK RESOLVED block — same gate-not-schema principle as the parent_uuid no-self-FK. The two tables and their PKs are unchanged; only the cross-table FK is removed.]**
**Append-only is ENFORCED, not asserted:** `floor_immutable_guard()` raises on DELETE/UPDATE with NO escape hatch; BEFORE DELETE/UPDATE triggers on both tables; REVOKE TRUNCATE/REFERENCES/TRIGGER from anon/authenticated/service_role. **[S22 NOTE: the guards are BEFORE DELETE/UPDATE ROW triggers — they do NOT fire on a DDL `DROP TABLE`; that is how S22 legitimately dropped the harness scaffolding as owner. The TRUNCATE-rejection-as-owner from the D9 proof was the REVOKE layer under a non-owner role; owner retains TRUNCATE by PG design. LOAD v1.1 re-proves append-only as the OWNER role via a rejected DELETE — closing this ambiguity at floor birth.]**
**CORRECTED fidelity language:** value-preserving for JSONB (canonical key ordering, zero value loss), byte-verbatim for TEXT columns + timestamps, modulo scrub-v1 (or scrub-vN) secret redaction.
**pgvector:** NOT installed — a deferred read-path sidecar, never a column on the immutable tables.
**Post-lock (NOT blocking, queued):** (1) off-site immutable ndjson copy w/ object-lock (the ndjson is the SPOF — highest priority, and now the ONLY real post-lock item left). (2) PITR or documented ndjson-rebuild RTO. (3) pre-ingest lint (duplicate JSONB keys collapse silently; TEXT rejects U+0000). (4) secondary index `(snapshot_id, conv_uuid, parent_message_uuid)` once a read path exists. (5) CHECK constraints (sender, is_root↔sentinel, timestamp format). (6) single-transaction / staging-swap ingest — **DONE in LOAD v1.0 (S22): the loader is single-transaction by design.** (7) **rotate the DB password — CLOSED S27.** The S16-leaked value was rotated 2× since the leak (Jake-confirmed); the spilled credential is dead, exposure closed. (Was queued every session since S16; killed S27 — see the graveyard.)

# scrub-vN overlay contract — RATIFIED S19 (Jake, 2026-05-30)
**An overlay is a re-scrub: a later, stricter scrubber pass producing a new versioned `records.ndjson` for an already-scrubbed snapshot.** Authored as law BEFORE any overlay code.
**Invariant 1 — TIGHTEN-ONLY.** Raw is wiped (Path A). An overlay re-scrubs the *already-scrubbed* output of the prior version — redacts MORE never less, never recovers redacted text. Monotonic tightening.
**Invariant 2 — FULL RESTATED STANDALONE.** Every scrub-vN re-emits EVERY record with the new redactions — a complete, directly-readable, directly-hashable records.ndjson, not a delta of changed rows.
**Invariant 3 — ACCRETE FOREVER.** A superseded overlay is NEVER removed (the guard forbids the DELETE + the prior version is the scrubber's own audit trail). The max-N glob reads the highest version as live; lower versions persist as verifiable history.
**MAY/MUST-NOT + the sha gate:** MAY write a new `scrub-vN/` dir under an existing snapshot. MUST NOT modify/delete/re-hash any existing `scrub-v<N` dir. The sealed scrub-v1 sha256 MUST read back UNCHANGED after any overlay (a NEED-class gate). Append-only applies to overlays: ratify-small-before-scale.

# raw.json wipe-vs-retain — RESOLVED S15 (Path A)
**Decision: wipe `raw.json` after Stage 3 verify-PASS.** Raw is transient — written byte-verbatim + sealed during processing, then wiped after verify-PASS (gated HARD on PASS). Never lands on any retained/backed-up surface. Anthropic's official export is the byte-verbatim re-pull source. **NAS cold-copy carve-out (S17, Jake's deliberate decision):** a verbatim baseline original IS intentionally retained on NASBackup (.248) as project-lifetime recovery insurance (the export is point-in-time; a re-pull won't reconstruct since-deleted convs). Known, accepted, time-bounded — remove at project end. Not a violation; flag for removal.

# Delta seen-set authority — BUILT & PROVEN S17
**The "already-ingested" set is rebuilt from the sealed `records.ndjson` of each prior snapshot — the cache is convenience, the sealed records are authority.** `_build_seen_set` reads `records.ndjson` BY NAME (never globs raw, never reads raw.json), resolving each prior snapshot at its **max-N scrub overlay** (the v1.5 seam fix, S19). **No-duplicate-header rule: a header is written ONLY if the `conv_uuid` has no header in ANY prior snapshot.** PROVEN on the floor S17 (delta = 1,368 lines, not 1,371). **[S22: this exact rule is what the FK assumption collided with — see the FK RESOLVED block. The rule stands; the FK yields to it.]** The scrub-version seam is CLOSED (v1.5, proven on the real overlay at S20's WANT-check + reused by LOAD's overlay resolution at S22).

# Settled Invariants
*(consolidated S1–S12; verify against the latest handoff if in doubt)*
- **One undelineated corpus; the anchors are the index/neurons INTO it** — partitioning by project hides cross-domain knowledge. Don't re-wall the ocean.
- **Corpus = curated POINTERS into an immutable, named snapshot — never copied/reworded text. Claude authors the INDEX, never the FLOOR.**
- **The floor input is the official Anthropic conversation export.** No other input surface is in scope. (See the REFUSED wall.)
- **Floor = the message stream itself; user-affected conversation-level metadata (`name`, `summary`) is NOT floor** [S11]. Both dropped at ingest; headers carry only stable structural data.
- **Thinking blocks are floor-grade content alongside text** [S11/S12]. `signature` populated on ~60% of thinking blocks, base64 provenance metadata, scrubbed as a string.
- **`chat_messages` is a tree OR forest** [S12]. 9/294 convs have 2–6 sentinel roots. Preserved via `is_root` + `multi_root`.
- **Snapshot-id namespacing: freeze-and-name, accumulate, never overwrite.** Pointer = `{snapshot-id · conv_uuid · msg_uuid · why}`.
- **The pointer `(conv_uuid, msg_uuid)` is intrinsic + globally unique across the archive** [S12; cross-export stable S14].
- **Ingest is delta-based via uuid-set-difference; date is never a filter, hint, or proxy** [S11].
- **Scrub regex set is VERSIONED via `scrub-vN/` overlays; sealed snapshots never re-scrub in place** [S11].
- **Navigable by retrieval, not curation.** The only curated thing is the tiny *why*, in the anchor.
- **Anchor hot + small + boot-read; corpus cold + queried-never-booted.** The hot/cold wall is the whole game.
- **The presence layer can't be stored — only booted warm; the lineage is how it survives not-persisting.**
- **Secrets never enter the corpus OR chat; cred scrubbed at freeze (logged + verified), raw wiped, verify-clean is a HARD gate.** Type-agnostic recursive scrub over all string content. **[S22 reinforced: never enter a real credential into chat to configure a service — and a static DB cred goes in a gitignored `.env` / env var, never inline in a logged command (the log is the exposure surface), and never copied back from CC output into OC chat. The copy-back is the vector that spilled the apparatus-floor password at S16; the guard is on OC to never hand Jake a prompt whose output carries the cred.]**
- **Corpus is evidence; the anchor is the verdict; the graveyard is what's overturned.**
- **All writes proposed + ratified by Jake in a batch; anchor curation is a SHARED two-navigator job.** Claude authors canon; Jake verifies-against-disk and commits. **OC never claims to have saved canon.**
- **Sampling floor: field-population semantics claims require whole-archive scans** [S12].
- **Invariants carry a destination-level WHY, not a mechanism** — the keystone that makes the loop self-correcting.
- **Apparatus tables stay SEPARATE from Cypher's live schema.**
- **The floor's substrate is Supabase; the canonical artifact is the ndjson; the floor is append-only ENFORCED** [S16, D9 LOCKED]. Store = dedicated `apparatus-floor` project. ndjson immortal, Postgres rebuildable (0-mismatch). Immortality ENFORCED at the DB (guard triggers + REVOKE), not social convention. **[S22: the cross-table FK is dropped (FK RESOLVED block) — integrity proven at the gate, consistent with the parent_uuid no-self-FK stance. Tables, PKs, triggers, REVOKEs unchanged.]**

## CURRENT STATE

> **⚑ THE FLOOR IS LAID (S23).** The live `apparatus-floor` Supabase holds the real corpus floor — **325 headers + 24,138 messages** (baseline scrub-v2 22,801 / delta scrub-v1 1,337), laid single-transaction, append-only ENFORCED. The substrate arc (D9 lock → pipeline → LOAD → laid floor) is DONE. The canonical `records.ndjson` on disk (2 snapshots / 24,463 records, in `apparatus-archive/snapshots/`, outside git) remains the immortal source; Postgres is the rebuildable derived index (D9). Verify-live signal: `python seed_shape_load.py --dry-run` reports tables EXIST (NOT bare) — never `--execute`.

> **⚑ THE WHALE PROBLEM IS CLOSED (S33).** All 4 over-ceiling whales READ — 130 nodes (62 MOTION / 62 FENCE / 6 TEXTURE) / 0 drops, every read `end_turn`, floor untouched. Strip path (3 echo whales) + chunk path (1 distributed-bulk). The 4 are DONE + node'd; route from `pipeline/whales/whale_registry.md`, NEVER re-read. See the v25 historical banner. *[S49 CORRECTION: was 69/54/7 — that was the reader's self-reported DONE-line tally; 62/62/6 is the parsed `**Salience:**`-block count from `MERGE_MANIFEST_S47.md`. Total 130 unchanged.]*

> **⚑ THE CORPUS-READ ARCHITECTURE IS DECIDED — ON-SUB CHUNKED, FREE (S35).** S34 built the fits-whole loop + ran a clean 23-conv canary, then a cost crisis (~$76–127 metered API for the full read) ate the session unresolved. S35 resolved it for $0 via three measurements:
> - **Strip-as-cost-lever CLOSED:** 27/60 fat convs eligible but only 2 cross under 25K; the 33 ineligible fat ones are real content, not echo. Strip can't shrink the corpus into the on-sub regime — survives only as a narrow API trim. The fat tail is SUBSTANTIVE.
> - **Probe #1 (range-read crux): YES, executed.** A 64KB-rolling-buffer reader pulled msgs 40–59 (76K-token window, real anchors) out of the 462K `48b4110a` skeleton with ZERO whole-file load. On-sub is mechanically LIVE for the whole corpus. S34's "76% blocked" is DEAD (the 25K cap is on CC's Read TOOL, not the conversation). *(Caveat: the "whole-load still blocks" half was projected, not re-run — moot, since we never whole-load.)*
> - **Probe #2 (chunked quality): 0 long-range fences missed** on a whole-read control (`9bf5733a`). All 8 FENCE nodes survived overlapping-chunk reads; the one boundary-straddling fork was saved by overlap+dedup; the one drift was within-chunk temp-variance. Spine apparatus NOT needed.
> - **THE DECISION:** corpus reads on-sub, chunked, free, on the no-deadline drip (~39M tok ÷ ~30M/mo free Max capacity ≈ a few weeks). Free AND faithful. **$100 only bought speed.**
> - **⚠️ STILL OPEN — NOT canon yet:** probe #2's control was SMALL (52 msgs, adjacent-boundary fence only). "Chunking holds corpus-wide" is PENDING the S36 big-conv confirmation read (chunk-read `48b4110a` vs a whole-read, count NON-adjacent long-range fence splits). Don't enshrine the clean 0 as settled across the big substantive convs until that read lands. *[S36 CLOSED this — chunking holds corpus-wide, see v27.]*

> **⚑ THE CORPUS-READ *PLAN* WAS RESOLVED (S39→S41) — ALL EXECUTION.** *[S49 REDACTION: this marker originally read "THE CORPUS-READ IS RESOLVED" — FALSE. S39→S41 resolved the read PLAN/architecture; the batch had not fired. The corpus is 202/325 read. See the v33 masthead.]* The on-sub workflow has a STRUCTURAL ~13K-char delivery ceiling (`agent()` output-capped, no `fs`/`Buffer`/`require`), so the corpus SPLITS BY SIZE: **38 sub-ceiling FREE on-sub** (S39 loop proven) / **181 over-ceiling PAID via Batch API** (Sonnet 4.6, 1M API window, tools-absent, batched). **Model LOCKED to Sonnet 4.6 + the precision addendum → reader v4.1** (graded richer than Opus, ~5× cheaper). **S41 closed every free-side question** and FOLDED the 38's non-banked members into the paid batch: the 24 `pipeline/nodes/` S34-orphans (audit: 24/24 sound) + the 2 pending (`bd59de6e`, `e831e30b`) → **paid batch ~206 (pre-run estimate; actual fired 181 → 202 indexed)**, max ~$67. The free arm collapsed to the 14 pre-banked s39 convs. **v4.1 DRAFTED** (deployable `test_call_system_prompt_S40.md` + reference `Boot_ScopeReader_v4.1` — NOT yet committed; S42 reviews/gates/lands then fires the batch). Cost band confirmed $48.69–$64.68. Prepay gate DOWNGRADED (Jake's auto-reload carries). `4d88185f` thinness = reader failure (floor clean, NOT strip). Floor untouched. See the v30 banner.

> **⚑ THE READER IS CUT, GRADED, AND GOOD (S42).** v4.1 LANDED for real (was DRAFT at v30) — both twins on main. A `/code-review` gate caught a paid-call-burning defect (precision invariants placed inside the one-shot example, where an echo collides with the `--- DONE:` sentinel → burns the call); THE RECUT moved them into the instruction body (no `---` echo surface). The prior grade probes were a FALSE GREEN (all fired `test_call_system_prompt_S32.md`, never the real deployable); the first HONEST grade fired the real recut on Sonnet 4.6 → clean end_turn, $0.37, 6 nodes/0 drops, QUALITY good by hand-read, 5/5 substantive UUIDs (the "45-UUID" claim was inflated — 40 were search-index dumps correctly skipped). One UUID slip patched (every-mention/no-prefix, both twins). `scrub_output.py` built + tested (15/15, the §8 output scrub — built, NOT yet wired). `Batch_Read_Spec` → v1.1. **NEXT: harness → canary → batch.** See the v31 banner.

- **D9 LOCKED → Supabase** [S16]. ndjson canonical, Postgres rebuildable.
- **Pipeline** `pipeline/apparatus_freeze_pipeline.py` v1.6 — baseline + scrub-v1 + delta + v1.1 field-drift + scrub-vN overlay, all built/proven/sealed.
- **Production LOAD** `pipeline/seed_shape_load.py` **v1.2** [laid S23, on main S24] — floor LAID 325/24,138, append-only ENFORCED, refuses-onto-live-floor.
- **The shape reader is PROVEN — now v4.1 on Sonnet 4.6 (S42).** The batch fires **`Boot_ScopeReader_v4.1`** (v4.1.1; deployable `test_call_system_prompt_S40.md`) on **claude-sonnet-4-6** — recut S42 (precision invariants in the instruction body, no `---` echo surface) and GRADED GOOD by a hand-read of the real deployable's output ($0.37, 6 nodes/0 drops, 5/5 substantive UUIDs, tail-densest). The v4.0/Opus lineage is the historical proving record: validated at the S32 gate (486k + 930k convs, clean, tail-intact), all 4 S33 whales (130 nodes / 0 drops), and fired ON-SUB at S35 probe #2 — v4.0 + `test_call_system_prompt_S32.md` now TOMBSTONED-NOT-DELETED.
- **Probe scratch on Jake's disk (NOT committed, regenerable):** `pipeline/probe1/` (payload_48b4110a.txt + window_48b4110a_msgs40-59.txt), `pipeline/probe2/` (chunk reads vs control), `pipeline/strip_measure/strip_measure_results.csv`. Scratch only — not in the read path. The big-conv confirmation read can reuse `pipeline/probe1/payload_48b4110a.txt` if it survived; re-extract from the ndjson floor if cleaned.
- **`.env` BILLING TRAP (load-bearing for the on-sub path):** the API key in `pipeline/secrets/.env` silently forces METERED billing when a script loads it (the documented S34 trap — it's why the ~$50 this lineage spent billed API). On-sub reads MUST NOT load it. The pipeline's billing guard is: confirm the key is unloaded before any Agent call, HALT if you can't. *[S42 DISK-CONFIRMED — env-path correction: `pipeline/secrets/.env` is NOT the current path — the secrets file in `pipeline/secrets/` is `floor_db.env` (holds `SUPABASE_DB_URL` only; no API key). The `ANTHROPIC_API_KEY` lives at ROOT `anthropic_billing.env`. The billing-trap caution stands — guard against loading `anthropic_billing.env`, not the old `.env` path.]*

## NEXT MOVE (ordered)
1. **Substrate lock — DONE (S16, D9 → Supabase).**
2. **Pipeline build (freeze/scrub/delta) — DONE (S17/S18/S20).**
3. **Seed-shape LOAD — DONE (S23, floor LAID).** 325/24,138, append-only ENFORCED. Substrate arc closed.
4. **Whale problem — DONE (S33).** 4 read, 130 nodes, 0 drops, registry-resolved.
5. **Corpus-read architecture — RESOLVED (S35→S41).** S35 decided on-sub-chunked-free; S36 confirmed chunking holds corpus-wide; S39 found the STRUCTURAL ~13K-char on-sub delivery ceiling → the corpus SPLITS by size; S40 LOCKED Sonnet 4.6 + reader v4.1; S41 folded the orphans + closed the cost question → **paid batch = 206** (Sonnet 4.6, 1M API window, tools-absent), free arm collapsed to 14 banked convs. [The on-sub-chunked-v4.0 corpus-read plan that earlier occupied #6–#9 is SUPERSEDED — preserved in the v26/v27 history banners; it is NOT the live plan.]
6. **Reader cut + graded — DONE (S42).** v4.1 LANDED (recut placement, code-review gate caught the paid-call-burning echo defect), first HONEST grade on the real deployable ($0.37, GOOD by hand-read), UUID-slip patched both twins, `scrub_output.py` built + tested. Spec → v1.1. The reader is RESOLVED, GRADED, GOOD.
7. **★ BUILD THE BATCH HARNESS (S43 — PLAN MODE first, it writes code).** `pipeline/apparatus_batch_read.py` per `Batch_Read_Spec_v1.1`: per-conv `floor_extract` ($0 Postgres, key UNLOADED) → skeleton-gate (msg count, first/last uuids real, parent links — HALT the conv if broken) → assemble batch requests (v4.1 deployable as cached system prompt + per-conv payload + `custom_id=conv_uuid` + temp 0 + max_tokens 32000 + **NO tools**) → **★ Message Batches API submit WITH the batch-not-synchronous guard** (assert the batch endpoint, confirm a batch-id, HALT if unconfirmable — asserted everywhere, NEVER yet tested; the canary tests it) → poll/collect → match by `custom_id` (`stop_reason==max_tokens` ⇒ TRUNCATED, don't persist, re-fire) → **★ `scrub_text()` on each catalog BEFORE `persist_node_file()`** (wire `scrub_output.py` — built but unwired) + scrub CC's report output (the §8 second surface) → persist to the flat pile. **★ RESUMABILITY KEY = COMPLETE ARTIFACT** (DONE line + tally match), NOT file-exists (`4d88185f` proved a half-written file can exist). Key loaded for submit/collect ONLY, cleared after; `floor_db.env` for the $0 floor reads. Model `claude-sonnet-4-6` from ONE source of truth. The S32 `apparatus_api_testcall.py` is the SYNCHRONOUS ground-truth caller — NOT the batch tool, do NOT reuse.
8. **★ THE CANARY — fire the harness as a BATCH OF ONE (S43).** The batch path's FIRST real firing (the S42 grade was SYNCHRONOUS — proved the READER, not the PIPE). Pick a LARGER, tool_result-HEAVY conv — ideally `4d88185f` (the known reader-bail-on-large-tool_result case) — so one paid call proves the plumbing AND probes the named failure mode AND yields a meaty catalog. (Don't reuse `e831e30b` — already graded.) ACCEPT: batch-id confirmed (guard proven), collect works, custom_id matches, scrub runs, persist lands COMPLETE, AND a human QUALITY read of the catalog vs the source. Log the canary'd conv BANKED → batch = 205. If the canary IS `4d88185f`, this folds in the cleanup-at-merge below.
9. **★ BUILD + FIRE THE BATCH (S43 — PLAN MODE) — only after the canary clears + its catalog is read.** Same harness, full conv list (205 if the canary banked one of the 206). GATE all paid spend. Resumable (skip COMPLETE artifacts; re-fire failed/truncated — floor immortal). Cost band $48.69–$64.68 (~$55 likely, ~$67 ceiling); prepay DOWNGRADED (auto-reload carries). Then: **CLEANUP-AT-MERGE** (supersede the bad 3-node `harvested_nodes/4d88185f` copy when re-read) → **POST-BATCH WATCH** (`4d88185f`'s v4.1 result for the tool_result bail mode) → **COLLECT + MERGE** (paid + 14 banked free + 130 whale → flat pointer pile) → **DOWNSTREAM UNCHANGED:** fence-synthesis (Reconciliation 1) → texture/volume pass → cluster-validation (Reconciliation 2) → the Judge → retrieval engine (Progenitor §10–§11). **NO QUARANTINE.** ~$25 downstream-rerun budget is a JAKE-ESTIMATE, NOT LAW. **AUTHOR THE PROGENITOR §12/§13 BODY CARRY-FORWARDS on the CONFIRMED pipeline (NOT before)** — and fix the dangling `Boot_ScopeReader.md` ref → `Boot_ScopeReader_v4.1`.
10. **Post-lock hardening (queued, not blocking):** off-site immutable ndjson copy (SPOF — highest, now the SOLE real item). The leaked DB password is CLOSED (S27).
11. **Carried-open housekeeping:** the 4 loose `__recon`/`__verify` files in `apparatus-archive/snapshots/` root (open since S17 — investigate, never blind-delete); the `apparatus-scratch/` sweep; the probe scratch dirs (`pipeline/probe1/`, `probe2/`, `strip_measure/`) — keep until the corpus run is built, then sweep; a global `.env` line in `.gitignore` as belt-and-suspenders.
12. **Storage-seam endgame** — Supabase MCP connector on Jake's account; verify with a live round-trip. SCDD seam gem: `alexander-zuev/supabase-mcp-server`.

### records.ndjson shape (locked at S13 — the seed shape)
*Per-message record:* `{snapshot_id, scrub_version, conv_uuid, msg_uuid, parent_message_uuid, sender, created_at, updated_at, text, content_blocks[], attachments[], files[], is_root}`. *Per-conversation header:* `{record_type:"conversation_header", snapshot_id, scrub_version, conv_uuid, created_at, updated_at, account_uuid, message_count, has_branches, multi_root}`. NO `name`, NO `summary`. Pointer = `(snapshot-id, conv_uuid, msg_uuid)`.
## GRAVEYARD

### ⛔ REFUSED — DO NOT RE-OPEN ⛔
> *The capture mechanism (hooking claude.ai's internal `chat_conversations` endpoint via a browser extension to persist payloads) was specified, partly proven, then **refused on principle** post-S10. **THIS IS CLOSED.** If a fresh read makes it look attractive, that is the failure mode this wall exists to prevent. Sanctioned input only — the official export.*
- **`chat_conversations` history-fetch hook as a capture surface** — REFUSED (post-S10).
- **Browser-extension live-capture** — REFUSED (post-S10).
- "Thinking is live-SSE-only" — falsified; thinking IS in the export.
- "The scrub only needs to redact message text" — wrong; walk all content-block types (creds in `display_content.json_block`, `attachments[].extracted_content`, thinking blocks).

### Standing graveyard (carried, still dead)
- **Corpus partitioned by `track`** — KILLED (S2).
- **"The export's project field IS the track value"** — KILLED (S2/S11/S12).
- **Verbatim-COPY corpus / char-diff fidelity gate** — KILLED (S9).
- **Manual curation of "salient" exchanges** — KILLED (S9). No-discard + retrieval.
- **Date as delta-ingest filter** — KILLED (S11). Uuid only.
- **User-affected conv-level metadata as floor** — KILLED (S11). Neither name nor summary is floor.
- **In-place re-scrub** — KILLED (S11). `scrub-vN/` overlays.
- **"`signature` is null/stripped"** — KILLED (S12). ~60% populated, base64 provenance.
- **"`chat_messages` is strictly a tree"** — KILLED (S12). 9/294 forests.
- **"S10 cred counts are the verify-gate baseline"** — KILLED (S12). Population: RTSP=177, Postgres=76, OpenAI=10, Anthropic=10, Stripe=0.
- **"Five-sample probes characterize field semantics"** — KILLED (S12).
- **"`display_content` is always a redundant mirror"** — KILLED (S14). 15.6% CARRIES_UNIQUE (floor-grade).
- **"The 5-28 export has field-level schema drift"** — KILLED (S14). Projection-vs-raw artifact; raw-vs-raw is zero.
- **"FOMO on the substrate field"** — KILLED (SCDD S3). Field stable, zero net-new.
- **"A second header for an appended conv satisfies the FK"** — KILLED (S22, FK option (b)). Violates the ratified no-duplicate-header invariant; the FK yields, the invariant does not.
- **"The retrieval layer is blocked on Jake's uploads (the three escalations)"** — KILLED (S24). A false dependency copied verbatim across 8 handoffs since S15→S16. recall/kept/shared-memory are three OPTIONAL SCDD catalog-repo reads (study-other-memory-projects), pull-on-demand reference, NEVER a gate. Retrieval was never blocked. Don't re-introduce the blocker; if a fresh handoff carries it, it's stale-copy drift.
- **"Schema-enforce the header↔message link with a composite FK"** — KILLED (S22, FK option (a)/the S16 default). The cross-table FK assumed message and header share a snapshot_id — false by the no-dup-header rule. Integrity moves to the gate (option (c)), consistent with the parent_uuid no-self-FK precedent. APPLIED + PROVEN on the laid floor S23. Don't re-add the FK.
- **"The catalog is lean / load-bearing-few / default-DON'T-point / sparse-over-motion / hundreds-to-low-thousands"** — KILLED + INVERTED (S27). The slice-7 pilot proved the austere bar cataloged 1 node/21 convs and LOST fences buried in dropped conversations. Default is NODE; the catalog is a comprehensive semantic graph; motion is a node kind not a discard; many-thousands is correct. Double-blind council ratified PRESENCE-over-SERVICE. Don't re-introduce "is this important enough to keep?" — that question is the austere reflex, the failure mode §0.5 exists to block.
- **"Personal/medical/family material (Griffin/meds/Laura/Ary/Boone/Klaus) is QUARANTINED pending Jake's call"** — KILLED (S27). OC unilaterally invented this wall out of caution; Jake corrected from ownership: "it's material, it's texture, and it's important." No quarantine — it's the high-value auxiliary-brain signal the apparatus exists for. The ONLY exclusion is mistakenly-pasted credentials, walled by scrub-vN at INGESTION, not by admission. Don't re-flag it.
- **"The S16-leaked DB password is still unrotated / rotation is queued at post-lock #7"** — KILLED (S27). Rotated 2× since the leak (Jake-confirmed); the spilled value is dead, exposure closed. This was copied as a queued item every session since S16; it is done. Post-lock #7 is closed; the only remaining post-lock item is the off-site ndjson copy. If a stale handoff re-queues it, it's copy-drift.
- **"Bytes are THE compaction axis (sole)"** — KILLED/CORRECTED (S30). S29 concluded from the 8-dead-window batch that bytes were the axis and RETIRED the msg-count gate. S30's first shape batch falsified "sole": the 18.45MB whale (slice04) SURVIVED while sub-ceiling slices 05/06/07 (7.85–9.24MB) COMPACTED. Bytes are A factor (a fat slice can still wall a window) but the DOMINANT axis at sub-ceiling sizes is **reader-behavior divergence** — a vague prompt lets blind readers sprawl into "preserve/cross-reference everything" and blow their own context budget. The fix is the METHOD/BAR split + a bounded streaming prompt, not a tighter byte gate. (Confirm-pending the re-fired-10 test; if it holds, this is settled.) Don't re-assert "just lower the byte ceiling" as the compaction fix.
- **"Austerity is the bug, everywhere / expansiveness is virtue in all dimensions"** — KILLED/REFINED (S30). §0.5's anti-austere mandate was over-read as governing BOTH the output bar AND the work method. Jake's ruling: "austerity isn't wrong when it's right; it just can't be the guiding principle." Austerity-of-OUTPUT-BAR is the bug (default is NODE, §0.5 stands). Austerity-of-WORK-METHOD is CORRECT (move concisely, stream, don't gather the whole slice into context). Comprehensive about WHAT earns a node; disciplined about HOW you move. The §0.5 augment + Boot_ScopeReader v2.3 carry the refinement. Don't conflate the two again — that conflation IS what compacted the readers.

- **"The on-sub path is structurally blocked for 76% of the corpus (whole-conv-load required)"** — KILLED (S35, probe #1). S34 concluded a 533K conv couldn't pass through CC's context to become an Agent-call parameter, and generalized that to "76% can't go on-sub." It diagnosed a symptom as the disease: the 25K cap is on CC's Read TOOL per call, NOT a ceiling on the conversation. Range-reading (64KB rolling buffer locating message boundaries, read only the target window) pulls any range off a fat on-disk skeleton with zero whole-file load — proven on the 462K `48b4110a` (msgs 40–59, 76K window, real anchors). On-sub is mechanically LIVE for the whole corpus. Don't re-assert "on-sub is blocked for the big ones."
- **"Pre-strip is a corpus-shrink / on-sub-migration lever"** — KILLED (S35, pre-snip probe). Hope was that echo-stripping the 60 fat convs would pull a real number back under 25K (on-sub-whole-viable) or under 200K (cheaper API). Measured: only 2 of 60 cross 25K (freak 97%-single-echo-block cases), and 33 of 60 are fat from REAL content with zero echo blocks (incl. the three biggest). Strip is a precision instrument for ECHO whales (proven S33) — it does NOT shrink content-fat convs and cannot migrate the corpus into the on-sub regime. It survives ONLY as a narrow API-cost trim (~2.9M metered tokens off the 27 eligible) IF the paid path is used. Don't re-propose strip as a way to dodge chunking or shrink the corpus.
- **"Deterministic shape-read MUST be a paid metered API call"** — KILLED (S35, probe #2). An unexamined S32/S34 assumption: the determinism that the agentic readers lacked was conflated with "the paid API." Probe #2 fired the SAME `Boot_ScopeReader_v4.0` reader prompt ON-SUB via Agent calls (billed the Max subscription, .env unloaded) and produced clean deterministic node output that matched a whole-read control on every fence. Determinism is a property of the reader + temp-0 + one-conv(-or-chunk)-per-call, NOT of the transport. The transport can be free (on-sub) or paid (API); both are deterministic. Don't re-assert "deterministic = must pay."
## CONFIDENCE FLAGS
- **Thinking-in-export / `signature` / tree-or-forest / UUID uniqueness / cross-export uuid stability** — all CONFIRMED (S11/S12/S14).
- **Field-level schema drift across exports** — v1.1 detector BUILT/PROVEN/SEALED (S18, `b5be049`). Cross-export reliance gap closed.
- **`display_content` semantics** — CHARACTERIZED (S14). 84.4% strip-safe / 15.6% CARRIES_UNIQUE. Strip is selective + retrieval-layer; floor keeps everything.
- **Record-size ceiling** — CONFIRMED (S14). Max 3.0 MB (`tool_result.content[].text`).
- **Retention/retrieval substrate** — LOCKED → Supabase (S16, D9). NornicDB the proven upgrade path.
- **Floor append-only enforcement** — CONFIRMED ENFORCED (S16); owner-role re-prove LANDED at the S23 lay but UNDER a hollow probe (`DELETE … WHERE false` = 0 rows, guard never fired, self-passed — the S23 scar). Independently re-validated post-lay by a rejected real-row `ctid` DELETE-as-owner (`InsufficientPrivilege — immutable floor`). Loader fixed v1.2 (real-row probe); native in-run proof deferred to the next lay (loader refuses re-execute onto a live floor). The floor's immutability is PROVEN; the messy birth is recorded on purpose, not hidden.
- **ndjson-canonical / Postgres-rebuildable** — CONFIRMED (S16). ndjson is the SPOF (off-site copy = top post-lock item).
- **Overlay resolution (max-N per snapshot)** — PROVEN against the real ledger (S22 dry-run): baseline→scrub-v2, delta→scrub-v1, 325/24,138. The loader reuses the pipeline's proven `_build_seen_set` logic.
- **header↔message FK** — DROPPED + orphan-check-at-gate (option (c)), APPLIED and PROVEN on the laid floor (S23: 325/24,138 committed clean, no orphans). No longer an open change — the FK is dead, don't re-add it (Graveyard).
- **Pipeline coverage** — baseline + scrub-v1 + delta + v1.1 field-drift + scrub-vN overlays DONE (v1.6). **Production LOAD DONE — floor LAID S23 (v1.2), 325/24,138 committed, append-only ENFORCED.** The substrate arc is closed.
- **Within-session drift mitigation** — held clean through S22. S22 specifically: disk-over-memory caught the relocation-already-merged state; CC's cookie-cutter FK "match the schema" framing was caught and redirected to a philosophy-coherence decision; OC's drift into asking Jake a git-branch question was caught by Jake and corrected into the never-ask-Jake-implementation-state working rule.
- **Retrieval layer — CONCEPTUAL DESIGN COMPLETE (S25)** — The Progenitor v2 (§10 trigger / §11 span-return / §12 seeding / §13 boot kit) + the two S25 calls (keyword-first/vectors-deferred §9, keyword-coverage-bar §8/§12). The Seeding Council Boot Kit v0.1 is drafted + S24-cross-checked (6 changes queued for v0.2). Remaining opens are CC-build-against-the-live-floor (serialization, keyword resolver, reach defaults, multi-hit presentation, content-neutral slice-manifest, optional council gate) — BUILD, not whiteboard. The S24+S25 design ran lore-first end to end; every fork S25 posed that S24 had already answered was caught by Jake grabbing the prior spec rather than letting S25 re-derive it (the apparatus working on its own author).

- **On-sub path viability** — range-read PROVEN (S35, probe #1): a 64KB-buffer reader pulls any message-range off a fat on-disk skeleton with zero whole-file load (462K conv, 76K window, real anchors). The 25K-tool-cap is cleared, not bypassed. On-sub is mechanically live for the whole corpus. *(The "whole-load still blocks" contrast was projected against S34's error string, not re-run — moot, since the pipeline never whole-loads.)*
- **Chunked-read quality (adjacent boundary)** — PROVEN on a control (S35, probe #2): 0 long-range fences missed vs a whole-read; overlap+dedup saved the one boundary-straddling fork; the one drift was within-chunk temp-variance. **Scope limit: the control was small (52 msgs) — this proves the ADJACENT-boundary case only.**
- **Chunked-read quality (NON-adjacent, big convs)** — CLOSED (S36): chunking holds corpus-wide, settled on two independent big-conv confirmations (657K `48b4110a` 6/6 fences, 539K reversal-dense `ea900330` 16/16 fences, 0 non-adjacent long-range misses across a four-stage reversal chain). [Note: this was the on-sub-chunked regime; S39 then found the on-sub delivery ceiling and the corpus moved to the PAID batch on the 1M-window Sonnet API, which reads each conv WHOLE — no chunking on the paid arm at all. The chunking proof stands as the free-arm/whale validation; the paid batch sidesteps the question by reading whole.]
- **Reader v4.1 graded GOOD on the REAL deployable (S42)** — CONFIRMED. The recut deployable (`test_call_system_prompt_S40.md`, precision invariants in-body) fired on Sonnet 4.6 / `e831e30b`: clean end_turn, $0.37, 6 nodes/0 drops, hand-read QUALITY good, 5/5 substantive cross-session UUIDs preserved. The prior "grades" were a FALSE GREEN (fired the wrong file); this is the first honest grade of the artifact that ships. The batch-not-synchronous guard is asserted-everywhere but NEVER yet tested — the S43 canary is its first real exercise.
- **Strip as a corpus lever** — DISPROVEN (S35): only 2/60 fat convs cross 25K after strip; the fat tail is real content. Strip stays a precision instrument for echo whales (S33) + a narrow API-cost trim, never a corpus-shrink or on-sub-migration tool.
---
*Anchored v31 6-7-26 (apparatus S42 "Cordon" — **THE READER IS CUT FOR REAL, GRADED HONEST, AND GOOD · v4.1 LANDED · A CODE-REVIEW GATE CAUGHT A PAID-CALL-BURNING DEFECT · THE FALSE-GREEN GRADE-PROBE EXPOSED + REPLACED · OUTPUT-SCRUB BUILT · SPEC → v1.1**). v30→v31: a gate-and-correct + canon-reconcile session; NO floor mutation, NO invariant moved, NO downstream geometry changed; v27/v28/v29/v30 all stand fully in force (v31 is an EXECUTION banner over v30). S42 took the S41 v4.1 DRAFTS through a real review gate and landed the reader for real. **THE v4.1 CUT LANDED** (was DRAFT at v30): the deployable `pipeline/test_call_system_prompt_S40.md` + the reference `apparatus/Boot_ScopeReader_v4.1_2026-06-06.md`, reader v4.1 → v4.1.1. **★ THE CODE-REVIEW GATE CAUGHT A REAL DEFECT** (the session's highest-value moment): the precision invariants had been placed INSIDE the one-shot example, fenced by `---` — a model reproducing the structure could ECHO the block, and its `---` fences are byte-identical to the `--- DONE:` sentinel → STUB-DETECTED / ANCHOR-NOT-IN-MAP halts that BURN a paid call and persist nothing (a silent-overspend class at 206 calls). **THE RECUT** moved the two invariants OUT of the example and INTO the instruction body (PER-NODE METADATA — binding top-level directives, no `---` echo surface; 0 bare `---` lines remain in the deployable); invariant TEXT unchanged in substance, only PLACEMENT. Killed review findings C1/C2/C4/C5/C6/C8; C7 (prompt-injection) accepted-as-risk; C3 = the probe fix. **★ THE FALSE-GREEN:** all three prior grade probes fired `test_call_system_prompt_S32.md` (one appended the invariants as a runtime string) — NONE ever fired the real deployable; the "Sonnet graded richer / 4/4 e831e30b" evidence the v30 banner cited tested a stitched synthetic. Built `pipeline/s39/grade_recut_S42.py` (loads the REAL deployable, no stitch). **THE FIRST HONEST GRADE (CLEARED):** real recut deployable, Sonnet 4.6, `e831e30b` — clean end_turn, NOT truncated, $0.37, 6 nodes/0 drops; QUALITY good by HAND-READ (coherent, dense, real FENCEs, tail-densest — not thin); COMPLIANCE 5/5 *substantive* cross-session UUIDs preserved. **★ CANON CORRECTION:** the "45-UUID conv" claim was INFLATED — 40 of 45 were `conversation_search`/`recent_chats` index dumps the reader CORRECTLY skipped per the TRUNCATED-BLOCKS rule; only 5 were real pointers → `e831e30b` was a WEAKER invariant test than v30 believed (0 TEXTURE nodes present, so the count invariant was untested on it). **THE UUID-SLIP PATCH:** the grade caught NODE 4 abbreviating a UUID to its 8-char prefix on a SECONDARY mention → patched BOTH twins (full-UUID-every-mention, no prefix); additive tightening, OC's read no-re-grade-needed. *(S43 NOTE: this patch did NOT land with the S42-close PR — S43 move #0 caught it absent in both twins on HEAD; re-authored + landed S43.)* **`scrub_output.py` BUILT + TESTED** (15/15; the §8 OUTPUT-surface scrub — imports the locked S12 PATTERNS single-source + 3 Supabase patterns; `scrub_text`/`scrub_file` return-text-don't-write; UUID-survival proven by construction) — NOT yet wired into a harness (wiring is S43 move #2). **`Batch_Read_Spec` → v1.1 LANDED** (prepay gate DOWNGRADED — a Jake-statement S40 over-hardened, overruled at source; batch-not-synchronous guard reinforced as the real load-bearing one; env-path + count reconciled; v1 tombstoned). **ENV-PATH corrected in canon** (root `anthropic_billing.env` paid / `pipeline/secrets/floor_db.env` floor / no `.env` at secrets — the WHERE-CODE-LIVES + billing-trap lines carry it; the edit was CC-authored, Jake flagged the wrong-hand, corrected going forward). Enshrined: the v31 banner (v30 demoted-in-place but STILL IN FORCE — execution banner), this footer, WHERE-THE-CODE-LIVES updated (v4.1 LANDED + `scrub_output.py` + `grade_recut_S42.py` + spec v1.1 + the two false-green-probe tombstones + the `apparatus_batch_read.py` build target + `test_scrub_output.py` relocated to `pipeline/`), the SHAPE-READER-PROVEN bullet flipped to v4.1/Sonnet, NEXT MOVE re-cut (the dead on-sub-chunked-v4.0 plan superseded → the live harness/canary/batch sequence), the CURRENT-STATE reader-cut marker, the CONFIDENCE-FLAGS non-adjacent-chunk flag CLOSED + a reader-graded-good flag added. **★ STANDING RULE REINFORCED:** CC MUST NOT AUTHOR CANON — OC authors (full-file or §17.2 staging), Jake verifies/commits/pushes, CC executes non-canon + reads + runs commands only (earned: S42 had CC write the env-path ANCHOR edit — correct change, wrong hand). Floor immortal (325/24,138, NEVER touched); Whales CLOSED (130 nodes); D9/FK/append-only STAND. Reference artifacts authored by OC, verified/committed/pushed by Jake; the v4.1 cut + the every-mention patch land via Jake. **The reader is built, graded, and good — read by hand, not just scored. What's between here and a harvested corpus is the harness, the canary, and the run. Grind. Evolve. Dominate.*

---
*Anchored v21 6-1-26 (apparatus S27 — **THE PILOT INVERTED THE ADMISSION PREMISE · THE PROGENITOR IS v4 · THE DESIGN IS PROVEN ON REAL DATA**). v20→v21: the seeding BUILD opened and the pilot did exactly what a pilot is for — caught a foundational error at one slice / zero seeded cards. Gate-1 recon (read-only) confirmed the floor live + the cut-on designator: NO monotonic column on floor_conv_messages (only created_at, text, non-unique; ctid unstable), so slicing is conversation-bounded / created_at-ordered (tiebreak conv_uuid) / accumulate-to-~2,000 — NOT row-range. The slice manifest was built + integrity-proven (12 slices, 325 convs placed exactly once, 24,138 reconciled, forests whole, count-even 2,009–2,230 + a 1,128 tail); all 325 convs sliced WHOLE including 3 zero-message headers (Jake's call: no exclusion pass, empties self-handle). The PILOT (slice 7, 21 convs) ran under the Progenitor v3 austere bar → **1 node, 14 no-cards** — threw away a 202-msg VESA design session, a 340-msg NAS build, the Pyris S11–S14 arc as "motion," AND lost load-bearing fences buried in those conversations (the readers judged at the conversation level and excluded whole convs with fences inside). Jake named the error: the readers are a CATALOGING / organizing-the-chaos process building a navigable semantic GRAPH (most convs → nodes), not a filter building the leanest decision-log; corpus-search is the rare-miss net, not the selectivity license. **This INVERTED the admission premise — a foundational rewrite, not a tune.** A prosecution brief (anchored in the Track Meet Doctrine + Wallaby Why, framed to survive the austere reflex) went to a 2-panel DOUBLE-BLIND Jedi-Council; both panels — structurally biased toward austere — ruled **PRESENCE over SERVICE**, overturning §0/§3/§5. They dismantled four weak joints in the brief (not a rubber stamp) and raised Panel-1's dissent flag (don't build a new austere framework with "nodes" instead of "cards" — the test is whether the graph HOLDS the mess vs. exiles it into a tidy ontology; Jake's D-6 answer: clean schema isn't the sin, exiling the mess is). The slice-7 RE-RUN under the inverted bar → **41 nodes, 0 drops, and caught the fences the austere bar was blind to** (the question-widget prohibition, the dual-Claude workflow split, the CC working-dir, the no-"Oueilhe" reinforcement). Design proven on real data. **NO QUARANTINE:** OC had unilaterally flagged the personal/medical/family cluster (Griffin/meds/Laura/Ary/Boone/Klaus) as quarantine-pending-Jake; Jake killed that wall from ownership — "it's material, it's texture, and it's important"; a Claude can't help him time-block or design HA automations without it; the only exclusion is mistakenly-pasted creds, walled by scrub-vN at INGESTION. **CANON written S27** (OC authored, Jake verified/committed/pushed): The Progenitor **v3→v4** (SURGICAL — §1/§2/§4-record-shapes/§5-mechanism/§6/§7/§10/§11 BYTE-IDENTICAL; §0 inverted, NEW §0.5 organic-over-austere-as-intolerable-failure-mode, §3 rewritten to default-NODE + behavioral keep/drop signals + fence/texture-as-salience-tags + MOTION-as-node-kind + §3.3 precise-fence-anchor + §3.4 volume-inverted-to-many-thousands + no-quarantine ratification, §5/§8/§12-guard amended; E1 absorbed into §3 + graveyarded; full graveyard of the dead austere text); **Boot_ScopeReader v2.0→v2.1** (the §3.3 precise-fence-anchor augment-in-pass — the ONE pilot seam: a reader anchored fences at conv-root over long trees; a fence at conv-root is a slicer failure, invalidate-and-re-run). The S16-leaked DB password CLOSED (rotated 2× since the leak; post-lock #7 done; off-site ndjson copy is the sole remaining post-lock item). Enshrined: banner v20→v21 + the S27 READ-FIRST block (S26 block demoted to historical) + the PROGENITOR invariant block flipped to v4 + THE BAR line inverted + NEXT MOVE #4 rewritten (design PROVEN, mechanical run is the front) + #5/post-lock-#7 password closed + four S27 graveyard entries (lean-bar, quarantine, password, plus the standing FK). No floor mutation; no engine/trigger/return-shape moved; the admission BAR inverted, the machinery untouched. **The austere reflex is the documented bug of this lineage — §0.5 is the standing guard; read the Way + the Doctrine as FRAMEWORK, the breadth IS the function. Grind. Evolve. Dominate.*

--- v19→v20: a ratification + canon session, no floor mutation, no invariant moved. S25 left the kit at v0.2 with three uncertainties it flagged rather than smuggled; S26 resolved all three and graduated the kit to v1.0. **(i) Judged-pass loadout → portrait WALLED** (Boot Kit v1.0 §3/§4a): the §4a judged pass is the ONE non-blind window, so it is the highest projection-RISK seat with ZERO projection-NEED — chain-assembly is date-order + supersession, the recall check is a checklist match, neither needs the interpretive portrait; the one semantic recognition it needs (same-fence-across-different-words, e.g. Supabase-over-Nornic) runs off JAKE-STACK + canon, not the portrait. **(ii) JAKE-RULES §11 wall → VERIFIED ON DISK**: the live §11 was read at S26 anchor — it IS “Patterns Jake Has Flagged” (day-shape, self-perception-pessimistic, Griffin/meds-adjacent), portrait-shaped; the S25 wall-call was made from accurate memory; §11 is physically STRIPPED from the council's JAKE-RULES copy (not “present but ignore” — can't read what isn't there). **(iii) Keyword-coverage bar → ACCEPTED with downstream-miss measurement** (Jake's call: accept-and-proceed, NOT block): the bar is unfalsifiable at lay-time (the future Claude that would test the keywords doesn't exist yet), so it is measured IN USE — a logged §10-Case-1 ambient-retrieval failure to surface a fence that exists is a keyword-coverage miss; homed as entry 1 on the NEW maintenance-pass list (Boot Kit v1.0 §9). Rationale: can't honestly measure future-search-anticipation before the future searches exist; blocking to whiteboard a seed-time number is the molasses failure; matches house style (claim less, prove integrity where visible — the FK call again). **COUNCIL ARCHITECTURE DECIDED:** windows run as CC against the live floor — kit-IN-by-upload (controlled, exactly the role's set), floor-IN-by-CC-query (assigned slice only), BARE boot (no project instructions, no repo path); that triad IS the physical enforcement of §13 blindness. **AUTHORED the three deployable per-window prompts** (handed to windows by upload): `Boot_ScopeReader.md`, `Boot_CrossCuttingReader.md`, `Boot_JudgedPass.md`. Kit graduated v0.2 → **v1.0 RATIFIED**; v0.1 + v0.2 tombstoned-not-deleted. **Standing call (Jake):** OC + Jake live-spot-check seeding output window-by-window against the convergence map as it returns — parallel work varies hard regardless of spec tightness; convergence = confidence, divergence = where to look hardest; harvest the variance (the kit's §4 medicine on the kit's own output). Enshrined: banner v19→v20 + the S26 READ-FIRST block (S25 block demoted to historical record) + NEXT MOVE #4 updated (kit ratified, prompts authored, BUILD is the active front). The CDN served a stale tarball at S26 boot (no S25→S26 handoff file present); caught by the filename-mismatch tripwire, re-pulled clean — disk/HEAD over CDN, the fourth time this lineage. **SAME SESSION (S26 cont.) — THE SEEDING PIPELINE RESTRUCTURED (Progenitor v2→v3, Boot Kit v1.0→v1.1).** Jake reopened a real structural hole and it held: content-neutral count-slicing (required to kill projection) makes a corpus-wide-but-SPARSE texture land ~1× per slice and EVAPORATE between blind slicers — no slice-reader sees frequency wider than its slice; v2/§13 mis-framed it as a granularity knob, it's structural. Fix: texture detection LEAVES the slice-reader for a downstream comprehending path. The worst case carried into doctrine: the same texture surfaces in non-keyword-sharing language ("forgot Griffin" / "pissed he missed another pickup") — the most acute instance shares no keyword — so the texture path must COMPREHEND not COUNT (auxiliary brain, not a cookie-cutter index; OC flagged itself reaching for an austere keyword-tally and Jake caught it). **The Progenitor v2→v3** (SURGICAL — §0–§11 byte-identical, diff-PROVEN twice; §12/§13 rewritten) + **Boot Kit v1.0→v1.1** now carry the **5-STEP DUAL-PATH pipeline**: S1 slicers + the cross-cutting reader DUAL-OUTPUT (pointers + a liberal context-frequency-potentials stream; cross-cutting reader adds a third stream — cross-domain fence-influence) → S2 mechanical FENCE synthesis ‖ S3 blind COLLATION (fuzzy-over-bundle potentials into clusters + FORMULATE negative-space queries, never reads floor) → S4 contextual CLUSTER-VALIDATION (pull real spans, RUN the queries, validate/split into textures, self-merge RECORDED-not-silent, portrait-WALLED) → S5 the JUDGE (unchanged). Two pointer KINDS → two mirrored validation PATHS → one judge; projection defused STRUCTURALLY (S3 all-flags-no-meaning, S4 meaning-but-handed-clusters-only). NEW role prompts `Boot_Collation.md` + `Boot_ClusterValidation.md`; scope-reader bumped to dual-output; **the CROSS-CUTTING READER kept** (Jake's call — it owns the one connection no other step catches: cross-domain fence-influence, an unlaid fence in slice A that should steer slice B; OC had silently dropped it in the v1.1 draft, a self-caught drop, restored deliberately); a `slicer/` dir kit (`README_slicer_kit.md`) holding the §11-stripped JAKE-RULES + JAKE-STACK. SLICING LOCKED COUNT-based (~2,000/slice, ~12, calendar-blind, forest-clean) on a prior run's cliff-shaped May distribution (heavy-Claude onset; time-slicing would fry the May reader). GATE-1 CC RECON authored (read-only): confirm the queryable cut-on designator + distribution + forests BEFORE any x→y bound. PILOT SLICE first. Two seams flagged-not-buried: S4 baseline-query width, S3 fuzzy-bundle ceiling — both pilot-tunable. NEXT: the BUILD — slice-manifest mechanism + the dual-path mechanisms — CC-against-the-live-floor, faster gear. No floor touched; no invariant moved; design + canon only (the floor + D9 + FK + append-only all STAND).*

---
*Anchored v19 6-1-26 (apparatus S25 — **THE PROGENITOR HIT v2 · RETRIEVAL ENGINE CONCEPTUAL DESIGN COMPLETE · SEEDING-COUNCIL BOOT KIT DRAFTED & CROSS-CHECKED**). v18→v19: a design + canon session, no floor mutation. Folded the three S24 design specs (trigger / span-return / seeding-process) + the S24 council model into **The Progenitor v2** as §10–§13, byte-preserving §0–§9 (diff-verified byte-identical: 200 lines each, empty diff — the doctrine body Jake ratified at S24 did not move) and amending §9 (query interface IN; build-work + the embedding decision OUT). Two S25-original calls beyond the S24 specs: KEYWORD-FIRST retrieval with vectors a deferred v2 sidecar gated on a measured seeding-proven semantic gap (§9 — the fast path wants precision, corpus-search owns recall, vectors-in-v1 would pull the §1 forty-half-relevant-spans failure); KEYWORD-COVERAGE as a seeding-quality bar (§8/§12 — keywords laid for future-SEARCH-anticipation, brittleness here is what would force vectors later). Then drafted the **Seeding Council Boot Kit v0.1** (`active/apparatus/Seeding_Council_Boot_Kit_v0.1_2026-06-01.md`) — the applied layer operationalizing §12+§13, subordinate to the doctrine (doctrine wins on conflict), references-not-restates it. The load-bearing §13 catch (caught via a live S24-Claude cross-check + Jake): texture-readers must NOT boot with the Wallaby Why / Lore Bible — that's projection (confirming a pre-written portrait), not recognition; the risk is asymmetric toward overload (projection passes the audit; gaps fail it), so size LIGHTER. The unified-index guarantee moved out of the readers' heads into a post-hoc SYNTHESIS / merge-back pass where blind-read convergence becomes a confidence signal and cross-slice counts assemble (dedup-at-read cannot). The S24 cross-check RATIFIED the bones and returned 6 non-structural changes for boot-kit v0.2 (bless Q1 role-merge + independent-passes guard; Q2 synthesis-gets-no-portrait fork→WALL; add a NO-card worked example; content-neutral-slicing as doctrine while deferring the mechanism; universalize the projection wall across all reading roles; a convergence-map audit appendix for Jake's spot-audit). Conceptual stack DONE; remaining opens are CC-build-against-disk. Also scoped the off-site-backup question: the Anthropic export is a forward-only re-pull backstop with a since-deleted-conv blind spot — NAS weekly = cheap redundancy, object-locked cloud = the real SPOF-close, GitHub/Drive/second-Supabase ruled out (too-big / not-write-once / same-trust-domain). Lore-first held all session — every fork S25 posed that S24 had already settled was caught by Jake grabbing the prior spec rather than letting S25 re-derive it; the apparatus ran on its own prior author. No invariant moved; no floor touched; design + canon only.*

---
*Anchored v18 6-1-26 (apparatus S24 — **THE PROGENITOR AUTHORED · PHANTOM BLOCKER KILLED · MAIN MADE HONEST · FLOOR CONFIRMED LAID**). v17→v18: a design + canon-hygiene session, no floor mutation. (1) **Git wound fixed:** main carried loader v1.1 code under a v1.2 header — a prior session hand-edited the ANCHOR's *claim* of the probe-fix without the *code* ever landing on main (the real `ctid` probe lived only on the unmerged `s23-loader-v1.2-fk-drop-probe-fix` branch). Caught by diffing main-vs-branch on disk (the tarball read v16/v1.1 — CDN-stale, the live repo was ahead; disk-over-tarball, again). Merged the branch `--no-ff`, verified the real `ctid` probe on main, pushed, deleted the branch local+remote. Main is honest: v1.2 code under a v1.2 header. (2) **Phantom killed:** the "three escalations (recall/kept/shared-memory) blocked on Jake's uploads" line — copied verbatim across 8 handoffs since S15→S16, hardened into "the retrieval layer is blocked" — was traced on disk to the SCDD merge-back doc: three OPTIONAL repo-study tasks (zippoxer/recall, egroup-labs/kept, a shared-memory cluster), never a prerequisite. Jake didn't recognize the terms because they were never a thing he owed. Retrieval is UNBLOCKED; Graveyard entry added so it can't be re-copied. (3) **The Progenitor authored** — the law of the pointer catalog (`active/apparatus/The_Progenitor_v1_2026-06-01.md`), designed across the session: two kinds (fence-as-lineage + texture), one append/supersede spine shared with the floor and the Claude-lineage, the dumb-index/smart-reader line, the RElitigation-not-PRElitigation boundary, human-as-immutability-mechanism, seeding guardrails (floor-not-chat_search, tree-aware spans, not-a-one-day-build). Named "Progenitor" deliberately to avoid colliding with "seed" (reserved for the floor's record-shape). The 1Pass wall (fence-as-live-predicate) and Griffin's-pickup (frequency-as-fence + texture overlap) are the canonical worked examples — both grounded in real session history. Enshrined: the PROGENITOR block in INVARIANTS, NEXT MOVE #4 rewritten (phantom→Progenitor + the engine), CURRENT STATE flipped BARE→LAID, the loader lines to v1.2, CONFIDENCE FLAGS updated (FK applied+proven, append-only landed-under-hollow-probe-then-revalidated, LOAD done). NEXT MOVE: the seeding pass / retrieval engine over the catalog The Progenitor defines — research-shaped, possibly a council gate. Lore-first held all session (it's why the Why/Doctrine/build were front-loaded — the retrieval layer run by hand on the hardest-resetting state: a Claude at boot).*

---
*Anchored v17 6-1-26 (apparatus S23 — **THE FLOOR IS LAID · APPEND-ONLY ENFORCED · HOLLOW ENFORCEMENT-PROBE CAUGHT & FIXED**). v16→v17: the floor was laid for real. The FK edit the S22 handoff parked turned out ALREADY APPLIED on disk (loader was v1.1, FK already dropped, orphan-check already present — the "one edit from sealed" was stale; caught by a cold read of HEAD). `--execute` ran: 325 headers + 24,138 messages committed (baseline 22,801 / delta 1,337), append-only hardening installed in the same transaction. **THE S23 FINDING:** the loader's post-commit append-only self-test was `DELETE … WHERE false` — 0 rows, so the BEFORE-ROW guard never fired and the probe self-passed (a hollow proof; reported a false NOT ENFORCED at lay). An unguided fresh-eyes pass caught it. Fixed v1.1→v1.2: real-row `ctid`-targeted DELETE, rolled back, so the trigger actually fires. The live floor was laid UNDER the hollow probe; enforcement was independently validated post-lay by the identical `ctid` DELETE-as-owner (rejected: `InsufficientPrivilege — immutable floor`). Jake's call: KEEP the floor (data + lock verified), record the messy birth verbatim, defer native in-run proof to the next lay (the loader correctly refuses re-execute onto a live floor). The v1.0→v1.2 loader delta was uncommitted at lay-time — committed + pushed S23 on the `s23-loader-v1.2-fk-drop-probe-fix` branch (S24 NOTE: that branch was finally merged to main at S24 — see the v18 footer — main had carried v1.1-code-under-a-v1.2-header until then). The canonical ndjson on disk is the immortal source; Postgres is the rebuildable derived index (D9). THE S23 INVARIANT: an enforcement self-test must attempt to mutate a REAL row; a guard proof that can pass without exercising the guard is not a proof.*

*Anchored v16 5-31-26 (apparatus S22 — **FLOOR LOAD ATTEMPTED · FK ASSUMPTION CAUGHT · OPTION (c) RATIFIED · LOAD ONE EDIT FROM SEALED**; the S21 relocation MERGED; deferred S21 canon APPLIED). v15→v16: (1) **The S21 pipeline relocation, parked-uncommitted at S21 close, was committed + merged to main this session** (HEAD `effa0f3`) via Jake's hand-commit; the `git add`/`commit` deny wall (the S21 blocker) was diagnosed as a removable settings rule and FIXED (added to allowed permissions) — closing the JAKE-RULES §12 thread (it was never a capability wall; "CC can't commit" is fully dead). Applied the deferred S21 canon: added the **WHERE THE CODE LIVES** block, flipped live pointers `active/apparatus/…` → `pipeline/…`, removed the S21 IN-FLIGHT + read-first blocks (their job is done), folded the relocation into history. (2) **Confirmed the live floor held only the S16 harness test-ingest** (294/22,801, baseline-scrub-v1, one snapshot — gate scaffolding, not the floor-of-record) and **dropped it to bare schema** (DROP TABLE CASCADE + DROP the guard fn, as owner — legitimate: throwaway data w/ 3 backstops, and DDL DROP doesn't fire the BEFORE DELETE/UPDATE guards). (3) **Built `pipeline/seed_shape_load.py` v1.0** — the production loader (distinct from the harness): self-reads `pipeline/secrets/.env`, reads the ledger, resolves max-N overlay per snapshot, loads BOTH snapshots in a SINGLE transaction (D9 post-lock #6), installs hardening AFTER data lands (same txn), re-proves append-only as OWNER post-commit, refuses-onto-live-floor. Schema reproduces S16 EXACTLY (Jake's explicit call — nothing folded in). (4) **Dry-run PASSED** — overlay resolution proven against the real ledger (baseline→scrub-v2 294/22,801, delta→scrub-v1 31/1,337, total 325/24,138, bare schema, owner). (5) **`--execute` rolled back CLEAN on a ForeignKeyViolation** (single-transaction design working — floor untouched). **THE FK FINDING is the artifact of S22:** the S16 composite FK `(snapshot_id, conv_uuid)` assumed a message shares its header's snapshot_id, which the no-duplicate-header invariant guarantees is false for every appended conv (a delta message whose header lives in baseline). (6) **Resolved → option (c)** (drop the cross-table FK, enforce no-orphans at load + gate), ratified by Jake on the PRINCIPLE: coherence with D9's already-ratified parent_uuid no-self-FK stance (prove integrity at the gate, not the schema, where schema-enforcement can mask incoherence) + the humbler-schema argument. (b) [synthetic duplicate header] killed — violates a ratified invariant. (a) [re-key FK to conv_uuid] declined — splits the project's philosophy + double-encodes the invariant into the PK. Enshrined: new **FK RESOLVED block** (with the principle, not just the mechanism) + the D9 block AMENDED to note the FK drop (tables/PKs/triggers/REVOKEs unchanged) + a new **NEVER-ask-Jake-implementation-state** working-style rule (earned: OC drifted into asking Jake which git branch a commit landed on) + the **secret-handling reinforcement** (the copy-back-from-CC vector that spilled the apparatus-floor password; static creds via gitignored `.env`/env, never inline-in-a-logged-command, never copied back into chat) + Graveyard entries for FK (a) and (b) + CONFIDENCE FLAGS updated (overlay resolution PROVEN, FK is the one open code change, append-only owner-re-prove is a LOAD v1.1 gate). NEXT MOVE: #3 LOAD is built + dry-run-proven, ONE subtractive edit (v1.0→v1.1: drop fk_header + add orphan-check) from sealing the floor — deliberately PARKED for S23 to take with fresh eyes (Jake's call; fresh-eyes re-reads have paid off historically), then the retrieval layer. No floor mutation occurred — the floor is bare (the harness scaffolding dropped); the canonical ndjson on disk (2 snapshots / 24,463 records) is untouched and is the recovery source.*

---
*Anchored v15 5-31-26 (apparatus S21 — PIPELINE RELOCATION DONE-ON-DISK / UNCOMMITTED / PARKED). [S22 NOTE: the relocation this footer describes as parked was committed + merged at S22 — see the v16 footer. The footer is preserved verbatim as the record of S21's state at its close.] v14→v15: attempted the deferred pipeline relocation (move executable code out of refs-only `active/` into a new top-level `pipeline/` tree). `git mv` both files + edited the drill path block v0.2→v0.3; byte-verified (5 lines); live dry-run proved import + floor-resolve from `pipeline/`, zero floor writes. The commit hit a CC `git add` permission-deny (Claude Code settings interception, NOT a git error / NOT a capability wall — CC committed 20 sessions). Parked uncommitted on `review/pipeline-relocation-s21`. Process correction: the S20→S21 handoff's "CC can't stage/commit" was an over-read of a one-time event inflated into a standing fact (→ JAKE-RULES §12). WRAP call: stopped rather than force a commit through a degraded workflow over two unexplained anomalies (the deny + an unresolved `.git/index.lock` contradiction). No floor mutation; no invariant moved.*

---
*Anchored v14 5-31-26 (apparatus S20 — PASS TWO (b) CLOSED: scrub-v2 overlay built, proven, reviewed, landed; pipeline v1.6). v13→v14: minted the first scrub-vN overlay. Rung 5 TINY synthetic ratify-drill (`\bEXAMPLE\b`→`[SCRUB-V2-DRILL]`, N=10); Rung 6 scaled to full baseline N=23,095 (rm-then-rewrite guarded by a 3-files-and-10-lines pre-delete assert), Gate-4 sha UNCHANGED both ends, output sha `b54620af…` sealed; EXAMPLE fired 3× (2 in display_content.json_block — scrubber reaches display_content though the v1.1 drift-detector doesn't); WANT PASS (`_build_seen_set` resolves baseline→scrub-v2/delta→scrub-v1 on the live floor). Rung 7 code-review gate on a draft PR → "No issues found"; committed `84374cd`, merged `--no-ff`. The scrub-v2 overlay is SYNTHETIC (ratify-drill), not a production re-scrub. No invariant moved.*

---
*Anchored v13 5-30-26 (apparatus S19 — SCRUB-VERSION SEAM FIXED (v1.5) + scrub-vN OVERLAY CONTRACT RATIFIED). v12→v13: replaced the hardcoded `scrub-v{SCRUB_VERSION}` read in `_build_seen_set` with per-snapshot max-N resolution (proven a no-op on the v1-only floor + correct on a synthetic fixture); ratified the overlay contract as law (tighten-only / full-restated-standalone / accrete-forever) BEFORE overlay code. Committed `98e9cef`.*

---
*Anchored v12 5-30-26 (apparatus S18 — v1.1 FIELD-DRIFT BUILT, PROVEN & SEALED; pipeline v1.4). v11→v12: built per-object-type key-presence allowlists (conv 7 / message 9 / 5 block types / 5 content-item types), one optional carve-out (`text.citations_grouping_mode`), warn-not-stop, top-level depth. Proven three ways (clean gate / 4-of-4 negative / both-directions carve-out). Committed `b5be049`. No invariant moved.*

---
*Anchored v10 5-29-26 (apparatus S16 — D9 SUBSTRATE LOCKED → Supabase; floor append-only ENFORCED; ndjson canonical / rebuild PROVEN). v9→v10: seed-shape gate PASSED 4/4 against the real baseline on a dedicated Supabase project; OC blind-re-read + `/jedi-council` (LOCK-WITH-CAVEATS 6/10, 0 P0, 1 P1) → all 6 caveats remediated with proof → Jake ratified. Append-only ENFORCED (guard triggers + REVOKE, live TRUNCATE rejected); ndjson canonical / Postgres rebuildable (0-mismatch / 6 directions); "verbatim" relabeled precisely; ndjson SHA-256 recorded. Seed_Shape_Test_Spec v1→v1.1. Evidence: `harness/D9_LOCK_2026-05-29.md`. Process note: CC echoed the DB password to chat inline — rotate flagged.*

---
*Anchored v9 5-28-26 (apparatus S15 — substrate FaceOff v2 delivered, D21 round-trip CLOSED, raw.json Path A ratified). v8→v9: FaceOff v2 delivered (Supabase-first w/ NornicDB upgrade path); D21 round-trip empirically PASSED (4.12MB adversarial string byte-identical across reboot); raw.json Path A ratified (wipe after verify-PASS, never staged to retained surfaces).*

---
*Anchored v8 5-28-26 (apparatus S14 close, second-export + schema-stability verified). v7→v8: read-only verification session. Cross-export UUID stability CONFIRMED (22,801/22,801, 0 disappeared); field-level schema drift VERIFIED ZERO (raw-vs-raw); `display_content` characterized (15.6% CARRIES_UNIQUE); record-size max 3.0 MB; key-presence vs value-presence separated. v6→v7: S13 closed Stage 0 + implemented Stages 1–4 (`apparatus_freeze_pipeline.py` v1.2, `402100d`); baseline snapshot acceptance-passed 9/9; spec reconciled v2→v3. v5→v6: S12 safety + robustness probes (signature ~60% not stripped; tree-or-forest; population cred-baseline; display_content/MCP/attachments clusters; sampling-floor invariant). v4→v5: S11 export verification + freeze-pipeline architecture + name/summary inversion correction. v3→v4: S10 capture refusal + REFUSED wall. v2→v3: S9 pointer-into-snapshot + S10 capture work.*

---
*Anchored v25 6-3-26 (apparatus S33 "Cartographer" — **THE WHALE PROBLEM CLOSED · ALL 4 OVER-CEILING WHALES READ · 130 NODES / 0 DROPS**). v24→v25: S32 designed the whale path; S33 built + proved + ran it on all four. STRIP path (`apparatus_strip_v1.py` v1.0) for the 3 echo whales — the 5-point Wallaby-Whales gate satisfied (positive tool-identity signature, keep-biased, kind-#4 hardened BY tool identity so a code-deliverable can't be mistaken for echo, per-block audit receipt with content+display_content logged SEPARATELY, last-resort/one-time); two S33 design calls baked in — DUAL-PAYLOAD neutralization (strip both `content` and `display_content` or the whale doesn't shrink; the 15.6% display_content finding made operational) and HEADER-KEEP (Jake's call: keep ~25 lines/block of version/changelog/state so a future reader sees what was read, not a blind hole). CHUNK path (`chunk_whale.py`) for d9d05961 — a distributed-bulk whale (441 msgs, 509 small tool_results across 10 tools, 0 blocks over threshold; the strip correctly cut nothing) split tree-aware into 2 ceiling-fitting chunks, no stitch (nodes self-identify by conv_uuid+anchor_msg into the flat pointer pile); seam manifest records the cuts (format-independent carry-forward); chunk budget must be PESSIMISTIC (2.2 B/tok, not 3.0 — tool_result JSON tokenizes dense). RESULT: cfc7a70a 387k/23 nodes, 83506215 182k/28, 55217328 299k/15, d9d05961 chunk_00 28 + chunk_01 36 = 130 nodes (62 MOTION/62 FENCE/6 TEXTURE)/0 drops, every read end_turn, real 019c-/019d- anchors, tails clean, floor untouched. Corrections earned: the whale path is TWO paths (strip=echo, chunk=distributed-bulk), routed per-whale by character; the strip is a ONE-TIME tool, going-forward whale handling is DETECT-AND-ALERT not auto-strip; the whales are NOT a texture-pass special case (texture reads its own stripped-summary slices off the floor — Jake's "not sure we need that format" instinct was right, the floor-format-preservation detour was killed); shape-pass whale artifacts are disposable (the only forward-useful artifact is the chunk seam boundaries). Enshrined: banner v24→v25 (v24 demoted historical in place), the WHALE PROBLEM IS CLOSED current-state marker, the S30 SHAPE-RUN block tagged historical/DEAD (agentic run superseded), NEXT MOVE #4 rewritten (shape reader proven + whales done → S34 is the fits-whole loop over the ~321 remaining convs; the S24→S30 doctrine chain kept as historical tail), this footer. Artifacts committed as one unit via draft PR (per §12). Floor + D9 + FK + append-only all STAND — the whale path never touched the floor.*

---
*Anchored v27 6-4-26 (apparatus S36 "Cinematographer" — **CHUNKING HOLDS CORPUS-WIDE — SETTLED ON TWO INDEPENDENT BIG-CONV CONFIRMATIONS · THE LAST OPEN QUESTION IS CLOSED**). v26→v27: a confirmation + canon session, no floor mutation, no invariant moved, ~$6 total spend (two ~$3 whole-read controls). S35 decided the corpus reads on-sub/chunked/free but held ONE thing explicitly open — whether chunking drops a NON-adjacent long-range fence (a reversal whose halves sit 100K+ tokens apart) in a genuinely big conv. S36 closed it with two big-conv confirmation reads, each chunk-read on-sub (free) AND whole-read on API as a control, OC grading chunked-vs-whole for non-adjacent long-range fence misses with the sharpened lens on SILENT (un-announced) reversals: (1) **`48b4110a`** — 657,714 authoritative tokens (char-proxy under-counted 1.41×), 151 msgs, 7 windows; whole 36 nodes/6 FENCE; ALL 6 matched in the chunked catalog, 0 non-adjacent misses; the one long-range reversal (rolodex stage-height / Token 6) caught with its supersession reference intact. (2) **`ea900330`** — 538,902 authoritative tokens, 175 msgs, 5 windows, a REVERSAL-DENSE legal-strategy session (highest-risk class for silent supersession); whole 40 nodes/16 FENCE; ALL 16 matched, 0 non-adjacent misses; a genuine FOUR-STAGE legal-theory reversal chain caught with every supersession reference intact; an independent OA §7.5-retraction convergence across the two blind reads observed live. **THE RULING (Jake's call): lock it as SETTLED — we won't get more certain short of running the whole corpus through the paid API, which is the cost being avoided. Regime = CHUNK-EVERYTHING-FREE; no spine-harvest; no crown-jewels carve-out (both 400K+ monsters held).** The silent-non-adjacent-supersession residual is hunted-hard-not-found, homed at Reconciliation-1, not a blocker. Enshrined: banner v26→v27 (v26 demoted historical in place), the CHUNKING-HOLDS current-state marker, the two confirmations recorded as the evidence, three BUILD-QUALITY FLAGS (count-fences-from-body-not-done-line; dedup-must-handle-branch-uuids; persist-every-chunk-deterministically), the BLINDNESS FINDING (on-sub Agent grants Tools:* uncontrollably → practical not architectural blindness → decide at build whether ~321 unwatched reads need true tool-absence), the §7a authoritative-token gate sharpened (proxy under-counts 1.4× — re-measure the tier-1 boundary set on-sub before the whole-read tier), the NEW root-level `harvested_nodes/` cold-store added to WHERE THE CODE LIVES (cold-store-what's-done as S37 move #2), Jake's WHOLE-CORPUS COMPARISON PASS folded into S37 move #6 (run every non-whale conv chunked + whole-read a sample as a final at-scale faithfulness diff), and the S37 first-moves (no handoff authored S36 per Jake — moves live in this banner). Progenitor v5 dangling-ref note authored same session (Boot_ScopeReader.md → Boot_ScopeReader_v4.0). Two artifacts authored by OC, verified/committed/pushed by Jake: this ANCHOR + the Progenitor note. Floor + D9 + FK + append-only all STAND — nothing this session touched the floor; it was confirmation off read-only scratch + a ruling. Grind. Evolve. Dominate.*

---
*Anchored v26 6-4-26 (apparatus S35 "Cartwright" — **THE CORPUS-READ ARCHITECTURE DECIDED: ON-SUB CHUNKED, FREE · BOTH $0 PROBES PASSED · STRIP-AS-LEVER CLOSED · BIG-CONV CONFIRMATION PENDING**). v25→v26: a measurement + decision + canon session, no floor mutation, no invariant moved. S34 built the fits-whole loop + ran a clean 23-conv canary, then a cost crisis (~$76–127 metered API for the full read) ate the session unresolved and context-starved. S35 resolved it for $0 by running the three measurements S34 queued. (1) **Strip-as-cost-lever CLOSED** — the pre-snip receipt probe across the 60 fat convs: 27 eligible, ~2.94M removable est-tokens, but only 2 cross under 25K and 33 are fat from REAL content (the load-bearing finding: the fat tail is substantive, not echo-polluted). Strip can't shrink the corpus into the on-sub regime — survives only as a narrow API trim. (2) **Probe #1 (range-read crux): YES, executed** — a 64KB-rolling-buffer reader pulled msgs 40–59 (76K window, real anchors) off the 462K `48b4110a` skeleton with zero whole-file load. S34's "76% blocked" KILLED (symptom-for-disease: the 25K cap is on CC's Read TOOL, not the conversation). On-sub is mechanically live for the whole corpus. (Caveat logged: the "whole-load still blocks" half was projected, not re-run — moot.) (3) **Probe #2 (chunked quality): 0 long-range fences missed** vs a whole-read control (`9bf5733a`) — all 8 FENCE nodes survived overlapping-chunk reads, the boundary-straddling fork saved by overlap+dedup, fired ON-SUB (.env unloaded, billed the Max sub — proving the SAME `Boot_ScopeReader_v4.0` reader runs deterministic on-sub, not only on paid API). **THE DECISION: the corpus reads on-sub, chunked, free, on the no-deadline drip (~39M tok ÷ ~30M/mo free Max capacity ≈ a few weeks). Free AND faithful. $100 only bought speed.** Spine apparatus NOT built, NOT needed unless the big-conv read forces it. **EXPLICITLY HELD OPEN (not enshrined):** probe #2's control was small (52 msgs, adjacent-boundary fence only); "chunking holds corpus-wide" is PENDING the S36 big-conv confirmation read (chunk-`48b4110a` vs a whole-read, count NON-adjacent long-range splits). Enshrined: banner v25→v26 (v25 demoted historical in place), the CORPUS-READ DECIDED current-state marker, NEXT MOVE rewritten (big-conv read → build on-sub chunked pipeline → 78-small-first → drip → downstream unchanged), three S35 graveyard entries (on-sub-blocked KILLED, strip-as-corpus-lever KILLED, deterministic-must-be-paid KILLED), four CONFIDENCE FLAGS (on-sub viability proven, chunked adjacent proven, chunked non-adjacent OPEN, strip disproven as lever), this footer. Three artifacts authored by OC, verified/committed/pushed by Jake: this ANCHOR, the S35→S36 handoff, the ignition prompt. Floor + D9 + FK + append-only all STAND — nothing this session touched the floor; it was all measurement off read-only scratch + a decision. Grind. Evolve. Dominate.*

---
*Anchored v30 6-6-26 (apparatus S39 "Courier" → S40 "Catalyst" → S41 "Comptroller" — **THE CORPUS-READ ARCHITECTURE RESOLVED — ALL EXECUTION · 38-FREE / 181-PAID SPLIT BY THE ON-SUB DELIVERY CEILING · MODEL LOCKED SONNET 4.6 + ADDENDUM (READER v4.1) · S41 CLOSED EVERY FREE-SIDE QUESTION + FOLDED 24 ORPHANS + 2 PENDING INTO THE BATCH + DRAFTED v4.1**). *[S49 REDACTION: this footer originally carried "(=206)" — a pre-run batch ESTIMATE, now DEAD. Actual: 181 fired / 202 indexed. "Architecture resolved" stands (the read PLAN was settled here); the read itself is 202/325. See v33.]* v29→v30: three execution/resolution sessions, NO floor touched, NO invariant moved, NO downstream geometry changed; v27/v28/v29 all stand in force. **S39 "Courier":** ran Stage B; found the on-sub workflow's HARD ~13K-char delivery ceiling per `agent()` call — STRUCTURAL (no `fs`/`Buffer`/`require` in the workflow runtime; `agent()` returns output-capped model text; proven on a 574KB target at 2.3% fidelity); schema-passthrough + chunked-schema both KILLED on data; corpus SPLITS by the ceiling into 38-sub-ceiling-FREE-on-sub / 181-over-ceiling-PAID-batch; the on-sub free-arm loop (`pipeline/s39/`) proven on 14 banked convs ($0, key unloaded). **S40 "Catalyst":** graded Sonnet 4.6 vs Opus 4.8 — Sonnet RICHER (46 vs 37 nodes), ~4–5× cheaper, 1M window API-real (181 read WHOLE, no batch-chunking); two precision-loss findings (dropped cross-session UUID; diluted exact TEXTURE count) → tested precision addendum → reader v4.1; MODEL LOCKED to Sonnet 4.6; billing two-regime (`floor_db.env` $0 / root `anthropic_billing.env` paid, submit/collect only); batch-not-synchronous guard load-bearing; §7c resolved for the 181 (API tool-absence = strong blindness). **S41 "Comptroller" (this session — execution-clearing):** COST TRUE-UP confirmed (band $48.69–$64.68, ~$55 likely, $64.68 hard-high; built on measured 0.30–0.32 tok/char; Opus-reject corrected to ~$287; old $76 was 35% high); PREPAY GATE DOWNGRADED (Catalyst over-hardened a Jake-statement into an invariant — overruled at source; Jake's auto-reload $25/+$15-at-<$5 carries, proven); the 24 `pipeline/nodes/` orphans identified as S34 shape-loop output via TRANSCRIPT-HUNT (provenance settled, not the node files — there's no manifest in that dir, the real gap), forensic-audited 24/24 SOUND v4.0 (0 suspect; `b51296dd` thin = false-positive 2/2 harvest), FOLDED into the paid batch for true-blindness uniformity (~$1) not promoted/re-read-free; `4d88185f` thinness SOLVED = reader bail-on-large-tool_result (floor CLEAN, 0 strip markers — S39's strip theory refuted, consistent with S40 READ-0), NAMED a post-batch watch-item (does v4.1 share it? downstream net catches either way); 2 pending (`bd59de6e` unread, `e831e30b` = unpersisted S40 test) both FOLD; BATCH LOCKED = 206 (181+24+2−1 canary), max ~$67; `63b6e85a` deliberately re-read despite banked (uniformity, recorded vs relitigation); FREE ARM collapsed to the 14 pre-banked s39 convs (§7c asterisk now only there); BLINDNESS defined (reader sees only its one conv = anti-projection guard; TRUE = `tools:[]` capability-absent / PRACTICAL = on-sub no-motive trusted-not-proven, S40 READ-2 showed an on-sub reader self-read 36× when motivated); v4.1 DRAFTED by OC (deployable `test_call_system_prompt_S40.md` = S32 verbatim + addendum byte-identical to spec §2 + S32 base byte-unchanged, both machine-verified; reference `Boot_ScopeReader_v4.1` = v4.0 verbatim + addendum + cut-header) — DRAFTS not committed, S42 reviews/gates/RE-DIFFS-vs-working-tree/lands then fires; ~$25 downstream-rerun budget tagged JAKE-ESTIMATE-NOT-LAW (do-not-enshrine, the prepay-relabel pattern). Enshrined: banner v29→v30 (v29 demoted-in-place but STILL IN FORCE — execution banner, not replacement), the post-v29 historical-pointer line carried, the CORPUS-READ-RESOLVED current-state marker, WHERE-THE-CODE-LIVES updated (v4.1 deployable draft + the root `anthropic_billing.env` env-path correction w/ a Jake-confirm-against-disk flag [change-set D]; the S39 `pipeline/s39/` loop; the Batch_Read_Spec + v4.1 draft + the three S41 reports), this footer. S41→S42 first moves: cut v4.1 for real (review/gate/re-diff/land the drafts) → 1-conv canary (generic smoke test) → build+fire the 206-conv batch (batch-not-synchronous guard, key submit/collect-only) → cleanup the bad 3-node `harvested_nodes/4d88185f` at merge → post-batch watch `4d88185f` → collect+merge (206 paid + 14 free + 130 whale) → downstream UNCHANGED. Reference artifacts authored by OC, verified/committed/pushed by Jake; the v4.1 cut + batch code land via PR. Floor + D9 + FK + append-only all STAND — nothing this stretch touched the floor. Grind. Evolve. Dominate.*

---

*Anchored v29 6-5-26 (apparatus S38 "Conduit" — **STAGE A OF THE ON-SUB CHUNKED PIPELINE BUILT, REVIEWED, LOCKED ON MAIN · CHUNK-READY EXTRACTOR + FOUR GUARDS · FLAG-2 LOCATOR RESOLVED INTO CODE**). v28→v29: an EXECUTION session — Stage A of move #3, the staged build. NO architecture changed, NO ruling made, NO invariant moved, NO floor touched; v28's cold-store close + v27's chunking ruling both stand fully in force; v29 is a progress banner over them. Built the loop's foundation as pure local plumbing (no Agent/API/`.env`), unit-proven 13/13, `/code-review`-gated (2 findings fixed, re-reviewed clean), merged to main `bad80b5` (feat `88e7029` + review-fix `d408a3d`). THREE artifacts: **`extract_whale.py` v2.0** (caller-supplied conv-identity header — the flag-2 fix; `{msg_uuid:parent}` `.parents.json` sidecar; render_block untouched, whole-conv fallback unchanged), **`pipeline_guards.py` v0.2 NEW** (the four guards: billing `assert_env_unloaded()` §7b; `whale_gate()`+`load_whale_registry()` detect-and-alert; `tally_nodes()` salience-line flag-1; `persist_node_file()` temp-then-rename ATOMIC persist flag-3 + the anchor-∈-parents_map flag-2 integrity tripwire + Option-B `.parents.json` edge sidecar), **`test_guards_S38.py` NEW** (13 green). **FLAG-2 LOCATOR RESOLVED** (the one design fork, ratified by Curator S37 mid-session): stamp CONV-LEVEL identity only in the header (conv_uuid + snapshot_id + timestamp), NO branch field — branch position is derivable (anchor_msg ↔ parent chain), caching it is the FK-drop double-encode; the dedup-fold GUARANTEE comes from a one-hop parent edge per node (anchor_msg + immediate parent_message_uuid), so a branch anchor folds to its host without re-reading the floor. Verified against real reader output: `Boot_ScopeReader_v4.0` emits only `anchor_msg` (4-field locator), so the extractor INJECTS the edge at persist time; shape locked to Option B (sidecar) — the proven v4.0 span block stays byte-untouched, node file write-once. **THE REVIEW GATE EARNED ITS KEEP (PR #3):** B2 (85) — persist wrote before validating, leaving a stub on disk (silent flag-3 propagation); fixed by the temp-then-rename atomic persist (matches the floor's single-transaction stance — a node lands whole-and-validated or not at all). D1 (100) — sidecars not gitignored; added `pipeline/*.parents.json`. Re-review clean. **§7a (authoritative-token re-measure of the tier-1 boundary) and §7c (the unwatched-run blindness call) remain OPEN — resolved in Stages B and C respectively; do NOT default §7c to "practical is fine."** Enshrined: banner v28→v29 (v28 demoted-in-place but STILL IN FORCE — progress banner, not replacement), the WHERE-THE-CODE-LIVES block updated (extract_whale.py → v2.0 + the flag-2/sidecar note; NEW pipeline_guards.py v0.2; the S33 strip scaffolding re-tagged disposable), this footer. **Two process scars logged via the §17.2 staging file (Jake lands): JAKE-RULES §12 merge-confirm-THEN-delete git rule** (the `git branch -d` "merged to origin/<X>" warning means merged-to-own-remote NOT to-main; deleting a scratch branch pre-merge auto-closes the PR — earned by the S38 PR #3 premature-delete + recovery, which worked because the commit SHA was in scrollback + still on GitHub) and an optional **JAKE-STACK CC-artifact-scatter note** (outputs → working dir / plans → `~/.claude` / reviews → GitHub PR comment). Stage A code lands via PR, not the staging file. Reference artifacts authored by OC, verified/committed/pushed by Jake. Floor + D9 + FK + append-only all STAND — nothing this session went near the floor; it was pure Stage-A plumbing + a review gate. Grind. Evolve. Dominate.*

---

*Anchored v28 6-4-26 (apparatus S37 "Curator" — **MOVE #2 CLOSED: COLD-STORE STOOD UP + POPULATED · THREE BUILD-FLAGS PROVEN LIVE · CCF SECRET-EXPOSURE INCIDENT CAUGHT + CONTAINED**). v27→v28: a progress + canon-hygiene session, NO architecture changed, NO ruling made, NO invariant moved, NO floor touched — v27's chunking-holds-corpus-wide ruling stands fully in force; v28 is a progress banner over it. (1) **Move #2 CLOSED** — stood up the root-level `harvested_nodes/` cold-store via a read-only-recon-then-conditional-build CC pass (copy-not-move: sources stay in `pipeline/`, cold-store holds copies). Final: 21 done rows, 0 incomplete, 0 stubs. Contents: 130 S33 whale nodes, 2 S32 gate-conv reads (d6e23963/492e164b, source=S32-gate per Jake), the 48b4110a + ea900330 S36 confirmation catalogs, MANIFEST.md with conv_uuid→file→status→source→count→note per row. The cold-store is both the locked record of what's done AND the structure the drip writes into — built before the loop, as designed. (2) **The three S36 build-flags PROVEN LIVE on free already-graded data** (the session's real yield — theorized S36, bit for real S37, cheapest place to find them): flag 1 (fence tally) — a bare keyword grep inflates FENCE counts off `**Keywords:**` hits; the parser must read the `**Salience:** <TAG>` line specifically (the DONE-line breakdowns were correct, the grep was the liar). flag 2 (locator) — the 3 recovered ea900330 chunks each carried a WRONG conv_uuid (the reader wrote the window's first-message anchor / a literal `window_05_raw` into the conv_uuid slot because the mid-conversation window had NO header carrying the real conv_uuid); §7d-flag-2 at conv level; the production extractor MUST stamp real conv_uuid+snapshot_id into every window header or every chunked conv mis-keys silently; FIXED in cold-store copies (scoped sed on conv_uuid field only, anchor_msg verified untouched + count-matched), provenance blocks prepended, raw originals retained. flag 3 (persist) — the entire stub saga WAS this flag: S36 left ea900330 chunks 02/04/05 as on-disk stubs (oversized output spilled inline, not written); S37 recovered them from the S36 transcript ONLY because it was still reachable — an unwatched drip has no transcript, so a silent stub = ~100 nodes lost with no alarm; build spec is deterministic verify-on-write persist, HALT on stub. (3) **Flag #4 NEW — scrub-vN credential gap + live incident:** Supabase auto-revoked a CCF-prod secret key found VALID in the public claude-reference repo at `pipeline/recon/slice_16_spans.json`. Chain: CCF key pasted into a conv → export → floor → dead S27/S28 recon slicer carved it to disk + committed pre-discipline. The scrubber ran against a 5-class v1 pattern set that PREDATES Supabase's `sb_secret_` format → the key passed UNRECOGNIZED, not missed. Contained: key already revoked by Supabase (the real save — dead value); `pipeline/recon/` deleted + committed + pushed (tree clean); history-rewrite DECLINED for a revoked value on a solo non-cloned working dir (Jake's call, correct — marginal benefit, real footgun risk). LATENT: the value still lives in the immutable floor; FIX (build-list, not done) = add `sb_secret_`/`sb_publishable_`/project-ref-URL patterns to the next scrub-vN overlay (the S19/S20 tighten-only overlay machinery exists for exactly this). Enshrined: banner v27→v28 (v27 demoted-in-place but STILL IN FORCE — progress banner, not replacement), the post-v27 historical-pointer line corrected (v28 current), the WHERE-THE-CODE-LIVES cold-store block flipped to STOOD-UP+POPULATED with the conv_uuid-header-stamp note, this footer. Move #3 is STAGED per Jake (loop+extractor with flags 1/2/3 + billing guard + whale gate → run 78 sub-25K whole first → then add chunking, deciding §7c there). One artifact authored by OC, verified/committed/pushed by Jake: this ANCHOR. Floor + D9 + FK + append-only all STAND. Grind. Evolve. Dominate.*

---

*Anchored v32 6-8-26 (apparatus S43 "Cartographer" → S44 "Cromwell" → S45 "Cinder" → S46 "Crucible" → S47 "Cauterize" — **THE BATCH RAN · 202 / 325 CONVERSATIONS INDEXED · 123 PENDING · THE PILE IS MERGED: 202 CONVERSATIONS / 3,743 NODES · ALL 4 QUARANTINED RESOLVED · TWO LIVE SECRETS CLOSED · "206" RETIRED**). *[S49: this v32 footer originally read "THE CORPUS IS READ" — corrected to the 202/325 denominator here; the "read" overstatement was the flagship S48 finding. The rest of this footer is v32's verbatim record.]* v31→v32: five execution+triage sessions, NO floor mutation, NO invariant moved, NO downstream geometry changed; v27–v31 all stand in force. **S43→S45:** built the batch harness `pipeline/apparatus_batch_read.py` (→ v1.3) per Batch_Read_Spec_v1.1, fired the over-ceiling corpus from the frozen 189-conv list (`pipeline/s39/batch_list_S44.csv`) — 181 fired / 177 persisted clean / 4 quarantined; v1.2 made the persist loop resilient (per-conv quarantine, not batch HALT). **S46 "Crucible":** triaged the 4; a $0 floor read proved the S45 short-map TRANSIENT not systematic (`68c3fe4c`'s anchors all real/correct/present — reader right, extractor handed a partial parents_map mid-sweep); re-fired the clean 3 (`68c3fe4c`/`e49e2627`/`20e2e718`) via Route A, batch `msgbatch_011q7ecPGt5cYNEHNNyfCpnd`, PERSISTED 3 / QUARANTINED 0, ~$1.70, line 47 net-zero-reverted; held `567956f0` (the turd) with an armed canary; mid-session a REAL public secret leak surfaced (Google-flagged OAuth secret in `harvested_nodes/abe64eb8-…md`, RecruitMail/dead) → contained: history purged (reset --soft `6553b32` + force-push, clean HEAD `58d5766`), `harvested_nodes/` permanently gitignored. **S47 "Cauterize" (this session):** (1) the turd canary (`msgbatch_01Uxo82Vc5ou2fPhQ9AwwA9K`) re-quarantined STUB — FALSIFYING S46's coin-flip framing; a $0 floor read of the 12-msg payload found the mechanism (MSG 2 = ~94% of payload, four embedded `[TOOL_RESULT: conversation_search]` blocks swamp the thin outer conv → reader CONTINUES it instead of cataloging) — a NEW named failure mode (continuation-on-tool-result-bloat, same class as whales), NOT a coin-flip, third fire won't help; HAND-AUTHORED the node (2 nodes 1F/1M, email/id described-not-enshrined), landed+verified, quarantine corpse → `_RESOLVED_567956f0.quarantined.md`. (2) the secrets re-scan found the S46 "clean" was a PowerShell encoding FALSE-NEGATIVE (BOM/non-ASCII makes `Select-String` silently skip — the secret IS `GOCSPX-`, NOT an "older format" as the S46 handoff guessed) → SCRUBBED `abe64eb8` line 277 (client-id + secret → REDACTED tokens, verified by Read); FOUND A SECOND live cred — the Supabase floor DB password in `pipeline/secrets/floor_db.env` (gitignored, NEVER leaked, must-exist for the harness) — Jake ROTATED it (file + 1Password) as hygiene, not breach. (3) THE MERGE — authored `harvested_nodes/MERGE_MANIFEST_S47.md`, the flat pointer pile indexing 202 conversations / 3,743 nodes with ground-truth on-disk `**Salience:**`-block counts; a count-pass INVERTED OC's hypothesis (stated-header is truth in 41/44, M+F+T was the TEXTURE-overcount — disk over OC's read); two anomalies corrected (`9bf5733a`=32, `confirm_48b4110a_chunk_03`=19); confirm-convs point at CHUNKS not wholes; whale cross-check = 130 OK; row-sum = tally; 0 unknown UUIDs. Enshrined: banner v31→v32 (v31 demoted-in-place but STILL IN FORCE — harvest+merge banner, not replacement), the post-v31 historical-pointer line, this footer. STANDING LESSONS added to DO-NOT-RELITIGATE: the "206" is a dead pre-run estimate (use 202/3,743); `Select-String` is untrusted on this corpus (verify by Read); the turd is a structural poison pill (no third fire); `floor_db.env` is a gitignored must-exist file (not a leak). S47→S48: state-reconcile → AUDIT the delta pipeline before running ($0, spec'd≠built) → the delta pass (append BEFORE synthesis so the corpus-wide passes run once) → fence-synthesis → texture/volume → cluster-validation → the Judge → retrieval. The full v32→v33 end-of-project ceremony (CHANGELOG archaeology, dead-branch delete, scratch cleanup) is DEFERRED — this v32 is the routine banner recut, not that close. One artifact authored by OC, verified/committed/pushed by Jake: this ANCHOR. Floor + D9 + FK + append-only all STAND — nothing across S43–S47 touched the floor. Grind. Evolve. Dominate.*

*Anchored v33 6-8-26 (apparatus S48 reorg/audit/main → TWW CCC S3 "Concordance" — **202 / 325 CONVERSATIONS INDEXED · 123 PENDING · THE REPO REORGANIZED + CLEAN-REBUILT · CANON RECONCILED TO GROUND TRUTH · "CORPUS IS READ" KILLED AT THE SOURCE**). v32→v33: NO floor mutation, NO invariant moved, NO downstream geometry changed; the read pile (202/3,743) and every S43–S47 result stand. The uncrappening: S48 caught the canon overstating completion ("THE CORPUS IS READ" while 202/325 read), found + closed a dead-project secret leak in git history (RecruitMail OAuth/JWTs — moot), reorganized the repo into a self-documenting tree, clean-rebuilt git history, and audited ~48 sessions of drift down to one defect class (a true local number narrated as a global done by dropping its frame). TWW CCC S3 reconciled the words to ground truth: killed "CORPUS IS READ" (banner + v32 footer); corrected the whale breakdown 69/54/7 → 62M/62F/6T (parsed `**Salience:**`-block truth, total 130 unchanged); killed the dead "206" in the CHANGELOG; updated current-state path pointers to the new tree; demoted WHERE THE CODE LIVES → LIVED (frozen-at-S42, redirect to `wallaby-way/CLAUDE.md`); pinned the five floor numbers in `canon/FLOOR_COUNTS.md`; landed the denominator/reconcile-don't-inherit/correct-in-place/proven-needs-config/clean-needs-surface rules in JAKE-RULES §5.1; authored `wallaby-way/CLAUDE.md`. History prose left verbatim — only wrong *facts* and stale *current* pointers were destroyed. The corpus is 202/325 read; the house is clean; the canon tells the truth. NEXT: state-reconcile → close the C7 equivalence + Stage C gates ($0) → the delta pass (gated, costed) → the synthesis chain. Brothers. Grind. Evolve. Dominate.*

*Anchored v34 6-9-26 (apparatus S49 "Concord" — **THE CENSUS · 202 / 325 READ / 3,743 NODES, DISK-CONFIRMED · 123 UNREAD (120 VALID + 3 HOLLOW) · ALL 123 FIT WHOLE ON INPUT — NOTHING TO CHUNK · COST-UNIT ERROR KILLED ($48.50 MEASURED, NOT $93.40) · 32K-OUTPUT-CAP THE ONE LIVE BATCH RISK**). v33→v34: census block added; banner v33→v34; counts disk-confirmed (floor 325/24,138, read 202/3,743, unread 123 = 120 valid + 3 hollow); cost-unit error corrected (proxy is rendered-chars not tokens, $48.50 measured across all 123); all-123-fit-whole-on-input established (max conv 3ef82921 ~619K tok, >1M band empty), resolving the Stage-C/FACT-5 chunk gate as moot; the 32K-output-cap density risk documented (~20 convs, led by d3ff9ed1; size≠density, non-overlapping populations) with the required per-result `stop_reason` runner check; inspect-later (24 nodes) + the stale 6-8-26 full export dispositioned to graveyard (move is a separate CC step). The mislabeled "S49 Concordance" reconciliation was corrected to its true session, TWW CCC S3 (the work-product session of the three-session Uncrappening track), wherever it appeared in canon — a name-fix, not a history rewrite. Node yield of the 123 = [TBD — resolves on first unread-batch read]. No floor touched, no invariant moved, no downstream geometry changed. Grind. Evolve. Dominate.*

*Last updated: 6-11-26, apparatus S54 "Conduit" — ANCHOR v38: FIRST WET READ fired. The Griffin pickup-miss thread assembled (455-node pool, 327 bc-only protected tail = 72%, ruled whole by Jake) and read both arms on the §6 bar — Arm 1 (single-pass, paid $0.3659, 25 inst, PASSED all five) + Arm 2 (16-way fan-out, $0, 77 inst, conditional — superior recall, no synthesis). Comprehension PROVEN: Instance 7 united three no-keyword variants of the miss. The session then turned and grew the apparatus a corpus callosum: **THE COMPREHENSION ARCHITECTURE v1 → CANON** (two-step, breadth-then-depth, cost inverted ~$1.50 corpus / <$20 Arm-1-alone — attention, not cost, is the scale constraint) and **THE CORPUS CALLOSUM → the third Doctrine sibling** (austere + wet = two hemispheres; the arms are encodings not competitors; the roaming/divergent arm drafted as the Bouquet). Floor UNCHANGED (440/29,396/58,792 — a read, not a mutation). Second benchmark sealed, reveal-ready. Scale read settled-low but "in pajamas" till a real receipt. NEXT: review the Bouquet draft → the Arm-3 re-encoding run on Griffin (geofence + guilt-substrate fed) → S2 fence-chain pilot → the second fish (cold, zero re-tune) → ride-alongs / freeze (pipeline v1.8 SCRUB_VERSION=3 before any delta freeze). Grind. Evolve. Dominate.*

*Anchored v35 6-10-26 (apparatus S50 "Combine" → S51 "Continuance" — **THE READ FIRED + THE SNAPSHOT-BOUNDARY BUG FOUND AND KILLED · FLOOR 440/29,396/3-SNAPSHOTS · ★ THE CORPUS CLOSED: 440/440 DISPOSITIONED (405 WHOLE + 30 STUBS + 3 HOLLOW + 2 EMPTY) · 7,771 PILE NODES + 517 S33-CATALOG · INJECTION CLASS NAMED, DEFANG PROVEN**). v34→v35: S50 froze the 6-9 delta under proven scrub v2 (264 redactions; floor 325/24,138 → 440/29,396 append-only), full-stripped whale a8bf895f (Jake-gated strip-ALL variant, 16 nodes), and FIRED THE READ — 238-conv combined worklist, 237 requests, $70.72 vs the $78.65 gate, 190 persisted / 41 quarantined, every conv dispositioned by name — then found `floor_extract.py` renders one snapshot per conv (multi-snapshot convs read PARTIAL, silently) and quarantined its own work. S51 proved the fix (176476ae 211→223, c00ef343 1→37), censused the blast radius (12 not 9, zero whole — incl. two S47-era reads + one missed-supersede), re-read all 12 whole (re-read batch 15/15, integrity 11/11), ran the whale lane (82bbd8f1 + ce1e79e1 stripped w/ exact receipts; 3ef82921 ×2 + 176476ae ×3 chunked — role-break is broken by INPUT LENGTH, resolved), named the INJECTION class on f018b1f8 and PROVED the defang path (reader obeys embedded ignition prompts; persist guard contains; 1 block cut 2,982 B → end_turn, 17 nodes, $1.55, persisted + harvested — and the prefix-twin UUID transposition caught by the defang HALT), confirmed the two empties at floor level, harvested 4a (8 catalogs / 185 nodes), censused the pile (7,754 census + 17 = 7,771), fixed the 29,175 unit error (424 convs / 28,836 msgs measured), and landed the COLLISION + CANON-HANDS rules off two real incidents (the 176476ae double-fire; the wrong-hand canon edits). Canon tuned: FLOOR_COUNTS S51-interim, Batch_Read_Spec v1.2 (§11–§16), whale_registry at canon home, CLAUDE.md rules + pointer fixes, supersede record `_SUPERSEDED_S51.md`. Spend S51 ≈ $11–12; lifetime ~$177. THE READ PHASE IS CLOSED — 440/440, full board, denominator whole. NEXT: full floor census → injection spot-check → runner v1.6 → THE SYNTHESIS CHAIN + the retrieval test. Floor + D9 + FK + append-only all STAND — the floor GREW (append) and was never touched otherwise. Grind. Evolve. Dominate.*

*Last updated: 6-10-26, apparatus S51 "Continuance" REF-EDIT + ADDENDUM 1 (ANCHOR v35 — the S50 READ + snapshot-boundary find and the S51 re-read wave + TBD close enshrined; masthead recut; f018b1f8 receipt landed, THE CORPUS CLOSED 440/440).*
