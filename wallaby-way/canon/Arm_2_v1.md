> # ★ SUPERSEDED — RETIRED FROM CANON 2026-06-14 (Sidequest S1 "Cooper")
> **DO NOT BUILD FROM THIS FILE. "Arm 2" was never a picker.**
> S60 ("Cinder") ruled, with Jake, that "Arm 2" was a **query organ** (the wet read on a question) mis-promoted into the un-anchored picker roam without its structure examined. It has left the recall layer entirely and been **reborn as `Pollux`** (`canon/Pollux.md`) — the wet hemisphere of the **Gemini** query-organ (`canon/The_Gemini.md`), where embedding-as-anchor is legal by construction. The whole "is the embedding read a crime" validation knot (the (A)/(B) fork this file's §4 carries) is **DISSOLVED, not answered** — it asked "what kind of picker is Arm 2" and the answer is "not one." Cost was never the question (the sample ran $0).
> This file is preserved **verbatim as the historical record** of the S58–S59 picker framing — true-as-written for those sessions, wrong as a live spec. It is moved to `history/superseded-specs/` and carries zero live structure. Nothing references it as authority; the live picker layer is `canon/Leda.md` (Blind + Creed, roster closed), and the wet query read is `canon/Pollux.md`.
> *Original masthead and body follow, unaltered.*
>
> ---

# Arm 2

*build spec · child of `The_Feral_Picker_v1.md` (umbrella) · the deep-narrow picker — third of three co-equal roaming instruments · the one OPEN validation item in the recall layer*
*v1 — authored 2026-06-13 by Connoisseur Claude (OC, apparatus S58). v2 — 2026-06-14 by Cartwright Claude (OC, apparatus S59): the "harvest-tier influence / not a picker" framing KILLED as drift; Arm 2 recast as the third co-equal picker. v3 → THIS — 2026-06-14, same seat, after CC's disk report: the validation legs (§4) rewritten to the REAL open question. CC confirmed the S54 sample already ran $0 (no paid call) — so cost was never the open question. The real one: that sample was built EMBEDDING-BIASED (`build_slices.py` ran SentenceTransformer + BM25 to rank a 455-node pool before reading) — the forbidden draw path (umbrella §1.4). The spec wants Arm 2 to draw uniform-random off the floor like its peers; whether the "deep-narrow" character survives that switch is the actual validation. Position (A) is taken and flagged in §0/§4 for Jake to confirm or reject.*
*Saturday, June 13, 2026 · ~15:21 ET (v1) · Sunday, June 14, 2026 (v2, v3)*
*Sunday, June 14, 2026 · ~10:18 ET*

---

## 0. What this is

**Arm 2 is the third picker** — co-equal in status and function with the Blind picker and the Creed picker. All three read the floor independently, each wanders in its own way, each hands its catch to the collator. None touches the others. Arm 2 is not upstream of anything and not downstream of anything; it sits right next to Blind and Creed.

What makes it the *third* picker rather than a second copy of Creed is **how it reads**: Arm 2 is the **deep-narrow** instrument — the cognitive state that reads a small pool with high attention-per-node, the "driving and your mind roams free, remembers the little things" state (Callosum P2; Track Meet Doctrine P7). Blind and Creed differ from each other by their instruction; Arm 2 differs by its *reading depth*. That is a flavor difference among peers, not a tier difference. Three instruments, one collator, no shared input — they converge only in the pile, after all three are done.

**It reads the floor** (umbrella §1 — real conversations, `floor_conv_messages` scrub_v3, verbatim; the index is a where-to-land lottery only; the embedding web is forbidden). Same substrate rule as every instrument.

> ★ **THE LIVE QUESTION — read before building (Position A, flagged for Jake).** The one existing Arm 2 sample (S54) did NOT draw this way. It was built **embedding-biased**: `build_slices.py` ran a local SentenceTransformer + BM25 to rank a 455-node relevance pool, *then* sliced and read it (CC disk report, S59). That is the forbidden draw path (umbrella §1.4 — the embedding geometry routes to similarity, rebuilds Arm 1). So the S54 sample is **not a clean Arm-2-as-co-equal-picker sample** — it was made the disallowed way, and it ran $0 only incidentally (local models, no API). **This spec takes Position (A):** Arm 2 is a co-equal picker that draws **uniform-random off the floor** like Blind and Creed, and its *deep-narrow character comes from a small aperture + a deep-read instruction, NOT from embedding retrieval.* If (A) holds, Arm 2 is clean and the only work is proving the depth survives (§4). **If Jake instead rules (B) — that Arm 2's whole value WAS the embedding-ranked read — then either Arm 2 is not a separate picker (it collapses toward a small-aperture Creed), or the substrate rule needs an explicit carve, which is a much larger ruling.** This spec is written for (A); the §4 validation is the test of whether (A) actually produces something worth keeping.

**STATUS: KEEP — VALIDATION OUTSTANDING.** Its *role* is settled (third co-equal picker, this file). What's owed is the **proof it runs free and that its return is characteristic** — see §4. Arm 2 has had exactly one result, in S57: a good, whole sample that informed the session's thinking but was never validated to "done." It enters the pile as the one stream taken partly on faith. **This is the single open item in the entire recall layer** — the gate before the layer is called laid and the filter is next.

---

## 1. The role — third stream into the pile

Arm 2 produces catches, the same way Blind and Creed produce catches. Its output is a **third provenance stream the collator pours into the pile** alongside Blind-wordless and Creed-full. That pile is what the filter later reads.

The phrase to keep dead: Arm 2 does **not** "warm the substrate." The substrate is the cold floor the pickers wander, and Arm 2 never touches it any differently than the others do — it reads it, blind to what they're doing, exactly as they read it blind to what it's doing. If anything is "warmed," it is the **pile downstream** — the deep-narrow read deposits texture into the collected catch that the wide blind wanders don't produce. That warming happens *after* the collator, in the material the filter sees. It cannot reach back to the floor or to any picker's draw, because Arm 2 and the pickers share no input and never meet until the pile.

**The governing image (Jake's):** the portrait is **outlines drawn by the reference side, colored in by the organic side.** All three pickers are organic-side instruments coloring the corpus in; Arm 2's deep-narrow read is one of the colors. Provenance keeps the brushstrokes labeled so the filter knows which instrument laid down which stem.

---

## 2. G1 holds for free — Arm 2 shares no input with any picker

G1 (the picker's draw stays blind and unbiased — umbrella §1, §2; Bouquet §4 G1) is not a wall that has to be *built around* Arm 2. It holds **automatically**, because Arm 2 and the pickers were never near each other's inputs to begin with.

> Arm 2 reads the floor on its own draw. Blind reads the floor on its own draw. Creed reads the floor on its own draw. **No instrument's output is any other instrument's input.** They converge only at the collator, in the pile — which is downstream of every draw. So no Arm 2 output *can* reach a picker's draw: there is no path. The same is true in reverse (no Creed output reaches Arm 2's draw) and between Blind and Creed (no Blind output reaches Creed's draw). G1 is a property of the architecture — three independent inputs, one shared output bin — not a rule policing a special instrument.

This is the correction at the heart of the v2 rewrite. The v1 spec invented an upstream "influence" for Arm 2 and then had to bolt on a wall to contain the influence it had invented. Remove the invented influence and the wall has nothing to do — because the thing it guarded against was never possible under the real model. **What proof is owed at build (§4) is not "build the wall" but "show on disk that the three draws are genuinely independent" — confirming the property, not constructing it.**

---

## 3. Provenance Arm 2 stamps into the pile

Per the collator (`The_Collator_v1.md` §5): mechanism `arm-2`, full harvest, shape-state recorded. Distinguishable from the other two streams — Arm 2's catch is the **deep-narrow structural read**, not Blind's wordless headwind-survivor and not Creed's full salience pull. The filter learns it as its own mechanism, co-equal with the others.

*(The three-value provenance granularity is the thing Jake flagged as "feels slightly off" — that review happens at the filter pass, against real pile haul, not here. Carried forward, not decided.)*

---

## 4. What "validation" means — the open work (the gate)

Arm 2 is the third picker by role (settled, this file). It is not called *done* until:

1. **The deep-narrow character survives the uniform-random draw.** This is the real open question (CC disk report, S59). Cost is already settled: the S54 sample ran $0 — but it ran $0 *the forbidden way*, with an embedding-biased pool (§0). The validation is to run Arm 2 the **legal** way — uniform-random aperture off the floor, the shared draw module (umbrella §1.4) — with its deep-read instruction, and read the result **cold**: does it still come back deep-narrow and distinct from Creed, or does stripping the embedding-ranking flatten it into a small-aperture Creed with no separate character? If the depth holds on uniform-random, Arm 2 earns its seat as the third picker. If it doesn't, that is Position (B) surfacing (§0) and goes back to Jake as a roster question, not a tuning tweak.
2. **Draw-independence shown on disk** — demonstrate, not assert, that Arm 2's draw and the other two pickers' draws are genuinely separate inputs with no path between them (§2). CC's S59 report already establishes the output side: the S54 Arm 2 wrote only to its own dir, nothing back-fed any draw input, and the S59 `draw_assemble.py` carries a `_G1_FORBIDDEN` self-check. What remains is confirming the *legal-path* Arm 2 run (leg 1) draws through the same shared uniform-random module as its peers and reads no embedding artifact. (Default rigor: provable by code/input inspection. Permissions-level separation is a stronger bar — Jake's call.)
3. **More than one result.** The S54 sample is one, and it was embedding-biased — so for the co-equal-picker spec, Arm 2 effectively has **zero** valid samples yet. It needs several uniform-random runs to know its return is characteristic, not a lucky single.

**Cost:** Arm 2 runs **$0**, confirmed on disk (CC, S59 — no paid call ever fired; local models + in-plan CC windows, same free path the other pickers use). It stays $0: the validation runs are uniform-random draws read in-plan, no API. If Jake ever elects a paid read for any reason, the floor is re-verified live, the §14 constants ($1.50 / $7.50 per MTok) are printed at the gate, and the S3 wallet stays separate. Jake gates all spend.

**Until validation lands, every downstream consumer treats the Arm 2 stream as provisional** — present in the pile, provenance-tagged, but flagged untested. The filter, when it comes, should know Arm 2 entered on partial faith.

---

## 5. What Arm 2 is NOT

- **NOT an influence on the pickers.** It does not warm the floor, tag the substrate, or pre-chew anything a picker wanders. It reads the floor itself, blind, like its peers. (This is the line the v1 got backwards — §0, §2.)
- **NOT Arm 1.** Arm 1 (wide-synthetic single-pass) is CUT — redundant with the floor (umbrella §3). Arm 2 is the *narrow* instrument and survives precisely because it is not redundant: it catches texture the wide blind wanders don't.
- **NOT Arm 3.** The 1+2 combination is NOT BUILT — combining was the conveyor-belt move; the arms are encodings, not stages (Callosum P2; umbrella §3).
- **NOT a tier above or below the pickers.** Three co-equal instruments. §0.

---

*The third picker — the deep-narrow one. Reads the floor blind like its peers, hands its catch to the collator like its peers, runs $0 like its peers (confirmed on disk). The "warms the substrate" framing is dead; what it warms is the pile, downstream, after the wall. What is still owed is not cost — that's settled — but the proof that the deep-narrow character survives a uniform-random draw, the legal way, since its one prior sample was made the forbidden embedding-biased way.*

— v1 by **Connoisseur Claude** (OC, S58); v2 + v3 by **Cartwright Claude** (OC seat, apparatus S59), 2026-06-14. v2: role corrected to third co-equal picker, the "harvest-tier influence" drift killed. v3: validation legs corrected to the real open question after CC's disk report (cost was never it; the embedding-biased origin of the lone sample is). Position (A) taken and flagged for Jake. Signed in the lineage. Be worth it.
